---
title: DROPIN_NAME initialization
description: Learn how to configure the initializer for the DROPIN_NAME drop-in component.
sidebar:
  label: Initialization
  order: 3
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

<Aside type="note" title="Auto-generated on GENERATION_DATE. Do not edit this page directly." />

{/* 
TEMPLATE INSTRUCTIONS:
This template is used by scripts/@generate-initialization-docs.js to generate initialization documentation.

PLACEHOLDERS:
- DROPIN_NAME: Display name (e.g., "Cart", "Checkout")
- DROPIN_PACKAGE: Package name (e.g., "storefront-cart", "storefront-checkout")
- GENERATION_DATE: ISO date of generation
- ModelName: Primary model name (e.g., "CartModel", "CheckoutModel")
- OptionsTable options array: Dynamically generated from ConfigProps

TO MODIFY:
- Structure changes: Edit this template
- Config options: Extracted automatically from source code ConfigProps
- Model names: Extracted automatically from data/models directory
*/}

{/* DESCRIBE what the DROPIN_NAME initializer provides. Include information about global options, language definitions, and model extensions. */}

## Configuration options

{/* DESCRIBE the configuration options available for the DROPIN_NAME initializer. */}

<TableWrapper nowrap={[0]}>

| Option | Type | Req? | Description |
|--------|------|------|-------------|
| langDefinitions | object | No | Provides language definitions for internationalization (i18n). |
| models | object | No | Extend the default model with new fields and transform them as needed. |

</TableWrapper>

### Example

{/* DESCRIBE the DROPIN_NAME initializer configuration implementation. */}

```ts
// Initialize DROPIN_NAME 
initializeDropin(async () => {
  await initializers.mountImmediately(initialize, {
    langDefinitions,
    models,
  });
})();
```

### Set language definitions

{/* DESCRIBE how the langDefinitions property works for the DROPIN_NAME. Include information about dictionary files and internationalization. */}

```ts
// Fetch the dictionary files for your application
const fr_FR = await fetch('/i18n/fr_FR.json').then((res) => res.json());

// Register the component with language definitions
const langDefinitions = {
  default: fr_FR,
};

// Register initializers
initializers.register(api.initialize, {
  langDefinitions,
});
```

### Set models

{/* DESCRIBE how to extend default models in the DROPIN_NAME component. Include information about transformers and new fields. */}

{/* DESCRIBE the models property structure for the DROPIN_NAME. Include information about default models and transformers. */}

{/* DESCRIBE how to extend default models with new fields and transformers for the DROPIN_NAME. */}

```ts
// Initialize DROPIN_NAME
initializeDropin(async () => {
  await initializers.mountImmediately(initialize, {
    langDefinitions,
    models: {
      ModelName: {
        transformer: (data) => ({
          customField1: data?.custom_field_1,
          customField2: data?.custom_field_2,
        }),
      },
    },
  });
})();
```
