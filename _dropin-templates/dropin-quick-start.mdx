---
title: DROPIN_NAME Quick Start
description: Quick reference and getting started guide for the DROPIN_NAME drop-in.
sidebar:
  label: Quick Start
  order: 2
---

import { Aside } from '@astrojs/starlight/components';
import Link from '@components/Link.astro';

CUSTOM_INTRO

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: DROPIN_VERSION</strong>
</div>

CUSTOM_SECTIONS_BEFORE

**New to drop-ins?** Start with the [Using drop-ins](/dropins/all/quick-start/) tutorial for step-by-step guidance.

**Need to customize?** See the [Initialization](/dropins/DROPIN_SLUG/initialization/) page for advanced configuration options.

## Getting started

The DROPIN_NAME drop-in is included in the <Link href="https://github.com/hlxsites/aem-boilerplate-commerce" text="Commerce boilerplate" />—no installation needed. For complete instructions on importing and using drop-ins in your blocks, see the [Using drop-ins](/dropins/all/quick-start/) guide.

## Quick reference

**Import paths:**
- Initializer: `import '../../scripts/initializers/INITIALIZER_NAME.js'`
- Containers: `import ContainerName from 'DROPIN_PACKAGE/containers/ContainerName.js'`
- Provider: `import { render } from 'DROPIN_PACKAGE/render.js'`

**Package information:**
- Package: `DROPIN_PACKAGE`
- Version: DROPIN_VERSION (verify compatibility with your Commerce instance)
- Example container: `CONTAINER_EXAMPLE`

### Example in boilerplate

The boilerplate includes working examples that use the DROPIN_NAME drop-in. Look for blocks with names like `commerce-INITIALIZER_NAME` (for example, `commerce-cart`, `commerce-checkout`) in the `blocks/` directory.

<Aside type="tip" title="Start with working code">
Before creating a new block, review existing blocks in the <Link href="https://github.com/hlxsites/aem-boilerplate-commerce/tree/main/blocks" text="boilerplate repository" /> to see how they import and configure the DROPIN_NAME drop-in. You can copy and adapt these patterns for your needs.
</Aside>

### Quick example

This example shows the basic pattern for using the DROPIN_NAME drop-in. For complete details and advanced patterns, see the [Using drop-ins](/dropins/all/quick-start/) guide.

```js
// 1. Import initializer (handles all setup)
import '../../scripts/initializers/INITIALIZER_NAME.js';

// 2. Import the container you need
import CONTAINER_EXAMPLE from 'DROPIN_PACKAGE/containers/CONTAINER_EXAMPLE.js';

// 3. Import the provider
import { render as provider } from 'DROPIN_PACKAGE/render.js';

// 4. Render in your block
export default async function decorate(block) {
  await provider.render(CONTAINER_EXAMPLE, {
    // Configuration options - see Containers page
  })(block);
}
```

<Aside type="tip">
For container-specific configuration options and examples, see the [Containers](/dropins/DROPIN_SLUG/containers/) page. To customize the initializer, see the [Initialization](/dropins/DROPIN_SLUG/initialization/) page.
</Aside>

CUSTOM_SECTIONS_AFTER

## Next steps

Now that you understand the basics, explore these resources:

- [Using drop-ins](/dropins/all/quick-start/) - Complete guide with examples and patterns.
- [Containers](/dropins/DROPIN_SLUG/containers/) - Available UI components and their configuration options.
- [Initialization](/dropins/DROPIN_SLUG/initialization/) - Customize initializer settings and data models.
- [API functions](/dropins/DROPIN_SLUG/functions/) - Control drop-in behavior programmatically.
- [Slots](/dropins/DROPIN_SLUG/slots/) - Extend containers with custom content.
- [Events](/dropins/DROPIN_SLUG/events/) - Listen to and respond to drop-in state changes.

{/* This documentation is auto-generated from: REPO_URL */}

