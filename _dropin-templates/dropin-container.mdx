{/*
  Template for Drop-in Container Documentation
  
  This template is used by scripts/@generate-container-docs.js to generate container documentation
  for each container from the source repositories.
  
  Placeholders used in this template:
  - DROPIN_NAME → Display name (e.g., "Cart", "Checkout")
  - DROPIN_PACKAGE → Package name (e.g., "cart", "checkout")
  - CONTAINER_NAME → Container name (e.g., "MiniCart", "OrderSummary")
  - CONTAINER_DISPLAY_NAME → Display name for container (e.g., "Mini Cart")
  - DROPIN_VERSION → Version number (e.g., "1.5.1")
  - CONTAINER_DESCRIPTION → Brief description of what the container does
  - CONFIGURATIONS_TABLE → Generated markdown table of all Props
  - SLOTS_CONTENT → Generated slots sections with examples
  - USAGE_EXAMPLE → Basic usage example with code
  - REPO_URL → GitHub repository URL
  
  The script handles:
  - Extracting container Props from TypeScript interfaces
  - Generating configuration tables with types and descriptions
  - Extracting and documenting all slots
  - Creating basic usage examples
  
  The template and script must be kept in sync.
*/}

---
title: DROPIN_NAME - CONTAINER_DISPLAY_NAME Container
description: Learn about the CONTAINER_NAME container in the DROPIN_NAME drop-in.
sidebar:
  label: CONTAINER_DISPLAY_NAME
  order: auto
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

CONTAINER_DESCRIPTION

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: DROPIN_VERSION</strong>
</div>

## Configuration

The `CONTAINER_NAME` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
CONFIGURATIONS_TABLE

</TableWrapper>

## Slots

SLOTS_CONTENT

## Usage

The following example demonstrates how to use the `CONTAINER_NAME` container:

USAGE_EXAMPLE

{/* This documentation is auto-generated from: REPO_URL */}
