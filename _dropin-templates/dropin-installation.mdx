---
title: DROPIN_NAME installation
description: Learn how to install the DROPIN_NAME drop-in component into your site.
sidebar:
  label: Installation
  order: 2
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Tasks from '@components/Tasks.astro';
import Task from '@components/Task.astro';
import Callouts from '@components/Callouts.astro';
import Diagram from '@components/Diagram.astro';
import Vocabulary from '@components/Vocabulary.astro';

{/* DESCRIBE the DROPIN_NAME drop-in component installation requirements and environment compatibility. Include information about runtime vs build-time environments and bundler support. */}

## Step-by-step

Use the following steps to install the DROPIN_NAME component:

:::note[Edge Delivery Service]
These steps are specific for Edge Delivery Services projects, and may not necessarily be the same for other frameworks such as a static HTML or React framework.
:::

<Tasks>

<Task>

### Install the packages

{/* DESCRIBE the package installation requirements for the DROPIN_NAME. Include information about required packages and dependencies. */}


    ```bash frame="none"
    npm install @dropins/tools @dropins/storefront-DROPIN_PACKAGE
    ```

:::note[Install @dropins/tools]
All drop-in components require the `@dropins/tools` package. This package contains the libraries that drop-in components need to initialize and communicate, including `fetch-graphql`, `event-bus`, and `initializer` utilities.
:::

</Task>

<Task>
### Map the packages

{/* DESCRIBE how to configure the importmap for the DROPIN_NAME. Include information about directory paths and CDN options. */}

This example shows an `importmap` added to the `head.html` file.

```json title="head.html"
<script nonce="aem" type="importmap">
{
  "imports": {
    "@dropins/storefront-account/": "/scripts/__dropins__/storefront-account/",
    "@dropins/storefront-auth/": "/scripts/__dropins__/storefront-auth/",
    "@dropins/storefront-cart/": "/scripts/__dropins__/storefront-cart/",
    "@dropins/storefront-checkout/": "/scripts/__dropins__/storefront-checkout/",
    "@dropins/storefront-order/": "/scripts/__dropins__/storefront-order/",
    "@dropins/storefront-payment-services/": "/scripts/__dropins__/storefront-payment-services/",
    "@dropins/storefront-pdp/": "/scripts/__dropins__/storefront-pdp/",
    "@dropins/storefront-recommendations/": "/scripts/__dropins__/storefront-recommendations/",
    "@dropins/storefront-wishlist/": "/scripts/__dropins__/storefront-wishlist/",
    "@dropins/storefront-personalization/": "/scripts/__dropins__/storefront-personalization/",
    "@dropins/storefront-product-discovery/": "/scripts/__dropins__/storefront-product-discovery/",
    "@dropins/tools/": "/scripts/__dropins__/tools/"
  }
}
</script>
```

With the `importmap` defined, you can now **import the required files** from these packages into your DROPIN_NAME block as described in the next step.

</Task>

<Task>
### Import the required files

{/* DESCRIBE the required imports for the DROPIN_NAME. Include information about tools, API functions, provider, and containers. */}

```js title="DROPIN_NAME.js"
// GraphQL Client
import * as mesh from '@dropins/tools/fetch-graphql.js';

// component tools
import { initializers } from '@dropins/tools/initializer.js';

// drop-in component functions
import * as pkg from '@dropins/storefront-DROPIN_PACKAGE/api.js';

// Drop-in component provider
import { render as provider } from '@dropins/storefront-DROPIN_PACKAGE/render.js';

// Drop-in component containers
import Container from '@dropins/storefront-DROPIN_PACKAGE/containers/Container.js';
```

<Vocabulary>

### GraphQL client

Enables the ability to set and get endpoints and headers, as well as fetches data and configurations.

### Drop-in component tools

The Initializer is responsible for setting up event listeners and initializing a module with the given configuration.

### drop-in component functions

Enables the DROPIN_NAME API functions and more.

### Drop-in component provider

Renders the DROPIN_NAME UI.

### Drop-in component containers

Structural elements that are responsible for managing and displaying the UI for the DROPIN_NAME.

</Vocabulary>

</Task>

<Task>
### Connect to the endpoint

{/* DESCRIBE how to connect the DROPIN_NAME to the API endpoint. Include information about endpoint configuration and required headers. */}

```js title="DROPIN_NAME.js"
// Set endpoint configuration
mesh.setEndpoint('https://<graphql-service-endpoint>/graphql');
```

#### Request headers

{/* DESCRIBE the header configuration requirements for the DROPIN_NAME. Include information about authentication headers and store-specific features. */}

```js title="DROPIN_NAME.js"
// Set the customer token. This method is specific to @dropins/storefront-DROPIN_PACKAGE package.
pkg.setFetchGraphQlHeader('commerce-auth', '<token>');

// Set store code header. This method is specific to the @dropins/tools package.
mesh.setFetchGraphQlHeader('store', '<default>');
```

:::note[Authentication]
Note that this setting is specific for GraphQL Mesh, so refer to the [Commerce GraphQL Service](https://developer.adobe.com/commerce/webapi/graphql/usage/headers/#request-headers) for direct connections. Visit the [user authentication](/dropins/user-auth/) drop-in component for instructions on setting up Authentication features.
:::

</Task>

<Task>
### Register and load the drop-in

{/* DESCRIBE how to register and load the DROPIN_NAME. Include information about mounting and debugging capabilities. */}

```html title="index.html" ins={"1":5} ins={"2":8}
<script type="module">
  // more code above...

  // Mount Initializers (must be called after all initializers are registered)
  return initializers.mountImmediately(initialize, { langDefinitions });
</script>
```

<Callouts square color="var(--sl-color-green)">

1. This function registers the DROPIN_NAME to be loaded on the page by the `initializers.mount` function.
1. This event handler triggers the initializers.mount function to load the DROPIN_NAME after the page has loaded.

</Callouts>

</Task>

<Task>
### Render the drop-in

{/* DESCRIBE how to render the DROPIN_NAME on the page. Include information about minimal configuration requirements. */}

```js title="DROPIN_NAME.js"
// Render DROPIN_NAME
provider.render(Container, {
  // Add configuration options here
})(document.getElementById('your-DROPIN_NAME-element'));
```

{/* DESCRIBE how to test the DROPIN_NAME installation. Include information about verification steps and troubleshooting. */}

</Task>

</Tasks>

## Summary

{/* DESCRIBE the installation pattern for drop-in components. Include a summary of the key steps. */}
