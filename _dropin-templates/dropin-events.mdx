---
title: DROPIN_NAME Data & Events
description: Learn about the events used by the DROPIN_NAME and the data available within the events.
sidebar:
  label: Events
  order: 5
---

{/*
  ⚠️ TEMPLATE USAGE GUIDE ⚠️
  
  This template is used by scripts/generate-events-docs.js to generate event documentation.
  
  WHAT THE SCRIPT USES FROM THIS TEMPLATE:
  - Section headings and descriptive text (used as-is)
  - Import statements
  - The REPEAT_FOR_EACH_EVENT block (replicated for each event)
  - Placeholders: DROPIN_NAME, DROPIN_DISPLAY_NAME, EVENT_NAME, etc.
  
  WHAT THE SCRIPT GENERATES INDEPENDENTLY:
  - Table contents between START/END markers (example rows are for reference only)
  - Event-specific data (names, descriptions, payloads from source repos)
  
  TO CHANGE:
  - Section text/descriptions → Edit this template
  - Table structure (columns, links, formatting) → Edit scripts/generate-events-docs.js
  - Both template and script must be kept in sync for table structure changes
*/}

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

The **DROPIN_DISPLAY_NAME** drop-in uses the [Event Bus](/sdk/reference/events/) to emit and listen to events for communication between drop-ins and external integrations. For common events shared across multiple drop-ins (such as `locale`, `error`, `authenticated`, etc.), see the [Common Events Reference](/sdk/reference/common-events/).

## Events reference

{/* EVENTS_TABLE_START */}
{/* 
  NOTE: The table structure below is GENERATED by scripts/generate-events-docs.js
  The example row shows the expected format but is replaced during generation.
  To change table formatting (columns, links, etc.), update the script, not this template.
*/}
<TableWrapper nowrap={[0]}>

| Event | Direction | Description |
|-------|-----------|-------------|
| [event/example](#eventexample-emits) | Emits | Example description |

</TableWrapper>
{/* EVENTS_TABLE_END */}

## Event details

The following sections provide detailed information about each event, including its direction, data payload structure, and usage examples.

{/* REPEAT_FOR_EACH_EVENT */}
EVENT_HEADING

EVENT_DESCRIPTION

#### Data payload

EVENT_PAYLOAD_SECTION

#### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const EVENT_LISTENER_VAR = events.on('EVENT_NAME', (data) => {
  console.log('EVENT_NAME event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
EVENT_LISTENER_VAR.off();
```
{/* END_REPEAT */}
