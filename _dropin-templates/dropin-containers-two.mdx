---
title: ContainerTwo container
description: Learn about the ContainerTwo container and configurations for the DROPIN_NAME drop-in component.
sidebar:
  label: ContainerTwo
  order: 3
---

import { Steps } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';
import Diagram from '@components/Diagram.astro';

{/* DESCRIBE what the ContainerTwo container does and its main purpose for the DROPIN_NAME drop-in component. Include key capabilities like customization, instances, and automatic handling of states. */}

## Container rendering

<Diagram caption="ContainerTwo container">
  ![ContainerTwo container](@images/placeholder.webp)
</Diagram>

## Import

```javascript
import ContainerTwo from '@dropins/storefront-DROPIN_PACKAGE/containers/ContainerTwo.js';
```

## Configurations

{/* DESCRIBE the configuration options available for the ContainerTwo initializer. */}

<TableWrapper nowrap={[0]}>

| Option | Type | Req? | Description |
|--------|------|------|-------------|
| OPTION_NAME | TYPE | Yes/No | DESCRIPTION of what this option does and its default value if applicable |
| OPTION_NAME | TYPE | Yes/No | DESCRIPTION of what this option does and its default value if applicable |

</TableWrapper>

## Basic usage

```javascript
await provider.render(ContainerTwo, {
  OPTION_NAME: VALUE_OR_FUNCTION,
  OPTION_NAME: VALUE_OR_FUNCTION,
  onCallback: (data) => {
    // DESCRIBE what the callback does with the data
  }
})($container);
```

## Slots

The `ContainerTwo` container supports customization slots for adding or modifying content within specific sections. Each slot receives a context object (`ctx`) that provides:

- **`ctx.data`**: Current component data and state
- **`ctx.appendChild(element)`**: Method to add elements to the slot
- **`ctx.replaceWith(element)`**: Method to replace slot content
- **`ctx.onChange(callback)`**: Method to listen for data changes

### Available slots

| Slot | Purpose | Required |
|------|---------|----------|
| SLOT_NAME | DESCRIPTION of what this slot does | Yes/No |
| SLOT_NAME | DESCRIPTION of what this slot does | Yes/No |
| SLOT_NAME | DESCRIPTION of what this slot does | Yes/No |

### Basic slot usage

```javascript
slots: {
  SLOT_NAME: (ctx) => {
    const ELEMENT_NAME = document.createElement('ELEMENT_TYPE');
    // DESCRIBE what content/elements are added to this slot
    ctx.appendChild(ELEMENT_NAME);
  },
  
  SLOT_NAME: (ctx) => {
    const ELEMENT_NAME = document.createElement('ELEMENT_TYPE');
    // DESCRIBE what content/elements are added to this slot
    ctx.appendChild(ELEMENT_NAME);
  }
}
```

### Dynamic content updates

Slots can respond to data changes using the `ctx.onChange` method:

```javascript
slots: {
  SLOT_NAME: (ctx) => {
    const ELEMENT_NAME = document.createElement('ELEMENT_TYPE');
    // DESCRIBE initial content setup
    ctx.appendChild(ELEMENT_NAME);
    
    // Update when data changes
    ctx.onChange((next) => {
      // DESCRIBE how the content updates when data changes
    });
  }
}
```

## Features

The container automatically handles:

- **FEATURE_NAME**: DESCRIPTION of what this feature does
- **FEATURE_NAME**: DESCRIPTION of what this feature does
- **FEATURE_NAME**: DESCRIPTION of what this feature does
- **FEATURE_NAME**: DESCRIPTION of what this feature does

## Styling

You can customize the `ContainerTwo` container appearance by overriding CSS classes. The quickest way to identify classes to override is to inspect the container UI in your browser's developer tools.

### CSS class override example

```css title="ContainerTwo.css"
.CONTAINER_NAME-COMPONENT_NAME__ELEMENT_NAME {
  CSS_PROPERTY: VALUE;
}

.CONTAINER_NAME-COMPONENT_NAME__ELEMENT_NAME {
  CSS_PROPERTY: VALUE;
}

.CONTAINER_NAME-COMPONENT_NAME__ELEMENT_NAME {
  CSS_PROPERTY: VALUE;
}
```

## Advanced configuration

```javascript
await provider.render(ContainerTwo, {
  OPTION_NAME: VALUE_OR_FUNCTION,
  scope: 'SCOPE_NAME',
  OPTION_NAME: VALUE_OR_FUNCTION,
  onCallback: (data) => {
    // DESCRIBE what the callback does with the data
  },
  slots: {
    SLOT_NAME: (ctx) => {
      // DESCRIBE custom slot implementation
    },
    SLOT_NAME: (ctx) => {
      // DESCRIBE custom slot implementation
    }
  }
})($containerElement);
```

## Best practices

<Steps>
1. **PRACTICE_NAME**: DESCRIPTION of the best practice
1. **PRACTICE_NAME**: DESCRIPTION of the best practice
1. **Element creation**: Always create DOM elements and append them using `ctx.appendChild()`
1. **CSS styling**: Use BEM naming convention and design tokens for maintainability
1. **Performance**: Use lazy loading for off-screen containers
1. **Error handling**: Let the container handle errors and updates automatically
</Steps>

## Troubleshooting

### Common issues

<Steps>
1. **ISSUE_NAME**: DESCRIPTION of the issue and how to resolve it
1. **ISSUE_NAME**: DESCRIPTION of the issue and how to resolve it
1. **ISSUE_NAME**: DESCRIPTION of the issue and how to resolve it
1. **ISSUE_NAME**: DESCRIPTION of the issue and how to resolve it
</Steps>
