---
title: DROPIN_NAME Installation
description: Package information and requirements for installing the DROPIN_NAME drop-in.
sidebar:
  label: Installation
  order: 2
---

import { Aside } from '@astrojs/starlight/components';

{/* 
TEMPLATE INSTRUCTIONS:
This simplified template links to the shared installation guide while providing drop-in-specific details.

PLACEHOLDERS:
- DROPIN_NAME: Display name (for example, "Cart", "Checkout")
- DROPIN_PACKAGE: Full package name (for example, "@dropins/storefront-cart")
- DROPIN_VERSION: Package version (for example, "1.5.1")
- CONTAINER_EXAMPLE: Name of a primary container for examples
- REPO_URL: Source repository URL
- CUSTOM_INTRO: Optional custom introductory content from enrichment
- CUSTOM_SECTIONS_BEFORE: Optional custom sections before installation link
- CUSTOM_SECTIONS_AFTER: Optional custom sections after installation link
*/}

CUSTOM_INTRO

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: DROPIN_VERSION</strong>
</div>

CUSTOM_SECTIONS_BEFORE

## Getting started

The DROPIN_NAME drop-in is included in the [Commerce boilerplate](https://github.com/hlxsites/aem-boilerplate-commerce)—no installation needed.

**See the [Using drop-ins](/dropins/all/installing/) guide** for complete instructions on importing and using drop-ins in your blocks.

## Package details

Use this information when working with the DROPIN_NAME drop-in:

- Package name: `DROPIN_PACKAGE`
- Current version: DROPIN_VERSION
- Example container: `CONTAINER_EXAMPLE`
- Initializer: `scripts/initializers/INITIALIZER_NAME.js`

### Quick example

This example shows the basic pattern for using the DROPIN_NAME drop-in:

```js
// 1. Import initializer (handles all setup)
import '../../scripts/initializers/INITIALIZER_NAME.js';

// 2. Import the container you need
import CONTAINER_EXAMPLE from 'DROPIN_PACKAGE/containers/CONTAINER_EXAMPLE.js';

// 3. Import the provider
import { render as provider } from 'DROPIN_PACKAGE/render.js';

// 4. Render in your block
export default async function decorate(block) {
  await provider.render(CONTAINER_EXAMPLE, {
    // Add configuration options here
  })(block);
}
```

CUSTOM_SECTIONS_AFTER

## Next steps

Now that you understand the basics, explore these resources:

- [Using drop-ins](/dropins/all/installing/) - Complete guide with examples.
- [API functions](/dropins/DROPIN_NAME/functions/) - Control drop-in behavior.
- [Slots](/dropins/DROPIN_NAME/slots/) - Customize the UI.
- [Events](/dropins/DROPIN_NAME/events/) - Integrate with other components.

{/* This documentation is auto-generated from: REPO_URL */}

