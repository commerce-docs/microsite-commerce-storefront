---
description: Build command preference—use build:prod-fast, never full build
alwaysApply: true
---

# Build Commands

Never run a full build (`npm run build` or `pnpm run build`). Always use `build:prod-fast` instead:

```bash
npm run build:prod-fast
# or
pnpm run build:prod-fast
```
