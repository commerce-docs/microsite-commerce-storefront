---
import type { Props } from '@astrojs/starlight/props';
const PAGE_TITLE_ID = '_top';

const isIframePage = Astro.props.entry.data.iframe;
---


{!isIframePage && <h1 id={PAGE_TITLE_ID}>{Astro.props.entry.data.title}</h1>}

{!isIframePage && <style>
  h1 {
    margin-top: 0;
    font-size: var(--sl-text-h1);
    line-height: var(--sl-line-height-headings);
    font-weight: 600;
    color: var(--sl-color-white);
  }
</style>}

{isIframePage && 
<style>
  .content-panel.content-panel {
    padding: 0;
    border-top: none;
    background: transparent;
  };
  .main-frame {
    padding-top: 0;
  }
  </style>
}
