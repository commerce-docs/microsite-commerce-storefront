---
title: Spacing
description: Spacing is the distance between elements on the page. It is used to create a harmonious arrangement with consistent visual balance and predictable rhythm to make the user interface easier for users to scan and identify relevant groups.
---

import { Block, DesignSwatchGroup, TokenDefinitions } from './_designBlocks.jsx';
import spacingData from '@data/spacing.json';
import { getTokenData } from './_getTokenData';

export const tokenData = getTokenData(spacingData);
export const tokenNames = tokenData.map((token) => token.name);

Use an incremental system to create harmonious arrangements with consistent visual balance and predictable rhythm to make the user interface easier for users to scan and identify relevant groups.

<TokenDefinitions tokenData={tokenData} />

<Block>
  <DesignSwatchGroup property="width" tokens={tokenNames} />
</Block>
