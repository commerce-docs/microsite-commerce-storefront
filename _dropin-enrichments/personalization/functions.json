{
  "fetchPersonalizationData": {
    "description": "The `fetchPersonalizationData` can be used to request the customer group, applied segments, and cart rules from Adobe Commerce based on the cart ID.",
    "usage": "const {\n  groups,\n  segments,\n  cartRules\n} = await fetchPersonalizationData(cartId);"
  },
  "savePersonalizationData": {
    "description": "The `savePersonalizationData` function saves the personalization data to a cookie and emits the `personalization/updated` event.",
    "usage": "savePersonalizationData({\n    segments: ['MQ==', 'Mg=='],\n    groups: ['Mw=='],\n    cartRules: ['NA==', 'NQ==']\n});"
  },
  "getPersonalizationData": {
    "description": "The `getPersonalizationData` function retrieves the saved personalization data from a cookie.",
    "usage": "const {\n  groups,\n  segments,\n  cartRules\n} = getPersonalizationData();"
  },
  "getStoreConfig": {
    "description": "The `getStoreConfig` function returns information about the store configuration related to personalization.",
    "usage": "const {\n  shareActiveSegments,\n  shareCustomerGroup,\n  shareAppliedCartRule,\n  customerAccessTokenLifetime\n} = await getStoreConfig();"
  }
}

