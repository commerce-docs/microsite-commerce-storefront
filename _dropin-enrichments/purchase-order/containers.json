{
  "ApprovalRuleDetails": {
    "parameters": {
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      },
      "className": {
        "description": "CSS classes for custom styling."
      },
      "approvalRuleID": {
        "description": "ID of the approval rule to display."
      },
      "routeApprovalRulesList": {
        "description": "Function to generate URL for navigating back to approval rules list."
      }
    }
  },
  "ApprovalRuleForm": {
    "parameters": {
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      },
      "className": {
        "description": "CSS classes for custom styling."
      },
      "approvalRuleID": {
        "description": "ID of the approval rule to edit. Omit for creating new rules."
      },
      "routeApprovalRulesList": {
        "description": "Function to generate URL for navigating back to approval rules list."
      },
      "onSubmit": {
        "description": "Callback triggered when form is submitted. Use for custom success handling or navigation."
      },
      "onChange": {
        "description": "Callback triggered when form values change. Use for real-time validation or tracking."
      }
    }
  },
  "ApprovalRulesList": {
    "parameters": {
      "className": {
        "description": "CSS classes for custom styling."
      },
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      },
      "initialPageSize": {
        "description": "Initial page size options for table pagination."
      },
      "routeCreateApprovalRule": {
        "description": "Function to generate URL for creating new approval rules."
      },
      "routeEditApprovalRule": {
        "description": "Function to generate URL for editing approval rules. Receives rule ID."
      },
      "routeApprovalRuleDetails": {
        "description": "Function to generate URL for viewing approval rule details. Receives rule ID."
      },
      "setColumns": {
        "description": "Callback to customize table columns. Use to show/hide or reorder columns."
      },
      "setRowsData": {
        "description": "Callback to transform row data before display. Use for custom formatting or filtering."
      },
      "skeletonRowCount": {
        "description": "Number of skeleton rows shown while loading."
      }
    }
  },
  "CompanyPurchaseOrders": {
    "parameters": {
      "className": {
        "description": "CSS classes for custom styling."
      },
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      },
      "initialPageSize": {
        "description": "Initial page size options for table pagination."
      },
      "routePurchaseOrderDetails": {
        "description": "Function to generate URL for viewing purchase order details. Receives PO ID."
      },
      "setColumns": {
        "description": "Callback to customize table columns. Use to show/hide or reorder columns."
      },
      "setRowsData": {
        "description": "Callback to transform row data before display. Use for custom formatting or filtering."
      },
      "skeletonRowCount": {
        "description": "Number of skeleton rows shown while loading."
      }
    }
  },
  "CustomerPurchaseOrders": {
    "parameters": {
      "className": {
        "description": "CSS classes for custom styling."
      },
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      },
      "initialPageSize": {
        "description": "Initial page size options for table pagination."
      },
      "routePurchaseOrderDetails": {
        "description": "Function to generate URL for viewing purchase order details. Receives PO ID."
      },
      "setColumns": {
        "description": "Callback to customize table columns. Use to show/hide or reorder columns."
      },
      "setRowsData": {
        "description": "Callback to transform row data before display. Use for custom formatting or filtering."
      },
      "skeletonRowCount": {
        "description": "Number of skeleton rows shown while loading."
      }
    }
  },
  "PurchaseOrderApprovalFlow": {
    "parameters": {
      "className": {
        "description": "CSS classes for custom styling."
      },
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      }
    }
  },
  "PurchaseOrderCommentForm": {
    "parameters": {
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      },
      "className": {
        "description": "CSS classes for custom styling."
      }
    }
  },
  "PurchaseOrderCommentsList": {
    "parameters": {
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      },
      "className": {
        "description": "CSS classes for custom styling."
      },
      "visibleRecordsLimit": {
        "description": "Maximum comments shown initially. Additional comments revealed via 'Show More'."
      }
    }
  },
  "PurchaseOrderConfirmation": {
    "parameters": {
      "purchaseOrderNumber": {
        "description": "Purchase order number to display in confirmation."
      },
      "routePurchaseOrderDetails": {
        "description": "Function to generate URL for viewing purchase order details."
      }
    }
  },
  "PurchaseOrderHistoryLog": {
    "parameters": {
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      },
      "className": {
        "description": "CSS classes for custom styling."
      },
      "visibleRecordsLimit": {
        "description": "Maximum history entries shown initially. Additional entries revealed via 'Show More'."
      }
    }
  },
  "PurchaseOrderStatus": {
    "parameters": {
      "className": {
        "description": "CSS classes for custom styling."
      },
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      }
    },
    "slots": {
      "PurchaseOrderActions": {
        "description": "Customize action buttons for purchase order operations (approve, reject, cancel, place order)."
      }
    }
  },
  "RequireApprovalPurchaseOrders": {
    "parameters": {
      "className": {
        "description": "CSS classes for custom styling."
      },
      "withHeader": {
        "description": "Shows/hides header section. Set false when embedding in layouts with their own headers."
      },
      "withWrapper": {
        "description": "Wraps container in styled wrapper. Set false for custom styling or embedded use."
      },
      "initialPageSize": {
        "description": "Initial page size options for table pagination."
      },
      "routePurchaseOrderDetails": {
        "description": "Function to generate URL for viewing purchase order details. Receives PO ID."
      },
      "setColumns": {
        "description": "Callback to customize table columns. Use to show/hide or reorder columns."
      },
      "setRowsData": {
        "description": "Callback to transform row data before display. Use for custom formatting or filtering."
      },
      "skeletonRowCount": {
        "description": "Number of skeleton rows shown while loading."
      }
    }
  }
}
