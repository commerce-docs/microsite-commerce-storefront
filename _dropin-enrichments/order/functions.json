{
  "cancelOrder": {
    "description": "The `cancelOrder` function calls the [`cancelOrder`](https://developer.adobe.com/commerce/webapi/graphql/schema/orders/mutations/cancel-order/) mutation. You must pass an order ID and reason.",
    "parameters": {
      "orderId": {
        "description": "The ID of the order to cancel.",
        "type": "string",
        "required": true
      },
      "reason": {
        "description": "The reason for canceling the order.",
        "type": "string",
        "required": true
      },
      "onSuccess": {
        "description": "The callback function to execute when the order is successfully canceled.",
        "type": "Function",
        "required": false
      },
      "onError": {
        "description": "The callback function to execute when an error occurs.",
        "type": "Function",
        "required": false
      }
    }
  },
  "confirmCancelOrder": {
    "description": "The `confirmCancelOrder` function confirms the cancellation of an order using the provided order ID and confirmation key.",
    "events": "Emits the `order/data` event with the updated order information after confirming the cancellation.",
    "parameters": {
      "orderId": {
        "description": "The ID of the order to cancel.",
        "type": "string",
        "required": true
      },
      "confirmationKey": {
        "description": "A key generated when a guest requests to cancel an order.",
        "type": "string",
        "required": true
      }
    }
  },
  "getAttributesForm": {
    "parameters": {
      "formCode": {
        "description": "One of \"customer_account_create\", \"customer_account_edit\", \"customer_address_create\", \"customer_address_edit\".",
        "type": "string",
        "required": true
      }
    }
  },
  "getAttributesList": {
    "description": "The `getAttributesList` function is a wrapper for the [`attributesList`](https://developer.adobe.com/commerce/webapi/graphql/schema/attributes/queries/attributes-list/) query. You must pass an attribute code to retrieve the list. The system default values are `CUSTOMER`, `CUSTOMER_ADDRESS`, `CATALOG_PRODUCT` and `RMA_ITEM`.",
    "parameters": {
      "entityType": {
        "description": "The entity type for which to retrieve the list of attributes.",
        "type": "string",
        "required": true
      }
    }
  },
  "getCustomer": {
    "description": "The `getCustomer` function is a wrapper for the [`customer`](https://developer.adobe.com/commerce/webapi/graphql/schema/customer/queries/customer/) query. You must pass a customer ID to retrieve the customer data."
  },
  "getCustomerOrdersReturn": {
    "description": "The `getCustomerOrdersReturn` function returns details about the returns a customer has requested. It is a wrapper for the [`customer`](https://developer.adobe.com/commerce/webapi/graphql/schema/customer/queries/customer/) query.",
    "parameters": {
      "pageSize": {
        "description": "The number of orders to return at a time.",
        "type": "number",
        "required": false
      }
    }
  },
  "getGuestOrder": {
    "description": "The `getGuestOrder` function is a wrapper for the [`guestOrder`](https://developer.adobe.com/commerce/webapi/graphql/schema/orders/queries/guest-order/) query.",
    "parameters": {
      "number": {
        "description": "The order number.",
        "type": "string",
        "required": true
      },
      "email": {
        "description": "The email address associated with the order.",
        "type": "string",
        "required": true
      },
      "lastname": {
        "description": "The last name associated with the order.",
        "type": "string",
        "required": true
      }
    }
  },
  "getOrderDetailsById": {
    "description": "The `getOrderDetailsById` function is a wrapper for the [`customer`](https://developer.adobe.com/commerce/webapi/graphql/schema/customer/queries/customer/) query. You must pass an order ID to retrieve the order details.",
    "parameters": {
      "orderId": {
        "description": "The ID of the order to retrieve.",
        "type": "string",
        "required": true
      },
      "returnRef": {
        "description": "Whether to return the reference.",
        "type": "boolean",
        "required": false
      },
      "queryType": {
        "description": "The type of query to perform. Use orderData as the value",
        "type": "QueryType",
        "required": false
      },
      "returnsPageSize": {
        "description": "The number of returns to return at a time.",
        "type": "number",
        "required": false
      }
    }
  },
  "getStoreConfig": {
    "description": "The `getStoreConfig` function returns information about the storefront configuration. It is a wrapper for the [`storeConfig`](https://developer.adobe.com/commerce/webapi/graphql/schema/store/queries/store-config/) query."
  },
  "guestOrderByToken": {
    "description": "The `guestOrderByToken` function retrieves a guest order using a token generated by Adobe Commerce. It is a wrapper for the `guestOrderByToken` query.",
    "parameters": {
      "token": {
        "description": "A token for the order assigned by Adobe Commerce.",
        "type": "string",
        "required": false
      },
      "returnRef": {
        "description": "The reference to return.",
        "type": "string",
        "required": false
      }
    }
  },
  "reorderItems": {
    "description": "The `reorderItems` function allows a logged-in customer to add all the products from a previous order into their cart. It is a wrapper for the [`reorderItems`](https://developer.adobe.com/commerce/webapi/graphql/schema/orders/mutations/reorder-items/) mutation.",
    "parameters": {
      "orderNumber": {
        "description": "The order number to reorder.",
        "type": "string",
        "required": true
      }
    }
  },
  "requestGuestOrderCancel": {
    "description": "The `requestGuestOrderCancel` function is simmilar to the `cancelOrder` function, but it is used for guest orders.\nThe token is a unique value generated using guest's email, order number and postcode",
    "parameters": {
      "token": {
        "description": "The token for the order assigned by Adobe Commerce.",
        "type": "string",
        "required": true
      },
      "reason": {
        "description": "The reason for canceling the order.",
        "type": "string",
        "required": true
      },
      "onSuccess": {
        "description": "The callback function to execute when the order is successfully canceled.",
        "type": "Function",
        "required": false
      },
      "onError": {
        "description": "The callback function to execute when an error occurs.",
        "type": "Function",
        "required": false
      }
    }
  },
  "requestReturn": {
    "description": "The `requestReturn` function takes the `RequestReturnProps` form as an argument and initiates the process of returning items from an order. It is a wrapper for the [`requestReturn`](https://developer.adobe.com/commerce/webapi/graphql/schema/orders/mutations/request-return/) mutation.",
    "parameters": {
      "form": {
        "description": "The form data for the return request.",
        "type": "RequestReturnProps",
        "required": true
      }
    }
  }
}
