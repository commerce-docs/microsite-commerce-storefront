{
  "$schema": "merchant-block-configurations",
  "description": "Enhanced descriptions for merchant-configurable properties that cannot be automatically extracted from source code or READMEs.",
  "configurations": {
    "product-list-page": {
      "urlpath": {
        "description": "Specifies the category path to display products from. Use this to create category-specific product listing pages. For example, `/women/dresses` shows products from the dresses category.",
        "when_to_use": "Set this when creating a dedicated category page. Leave empty for search result pages.",
        "example": "/women/shoes"
      }
    }
  }
}

