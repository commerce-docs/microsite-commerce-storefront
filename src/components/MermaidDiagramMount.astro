---
// Using src= on the script tag instead of inline module because inline scripts resolve
// relative imports against the document URL (causing 404s). The ?url suffix gives a real module URL.
import mermaidMountSrc from './diagram/mermaid-global-mount.ts?url';
---

<script type="module" src={mermaidMountSrc}></script>
