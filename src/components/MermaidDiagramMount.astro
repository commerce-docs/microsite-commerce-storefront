---
// Inline `<script type="module">` without `src` resolves relative imports against the
// document URL (404 under /get-started/.../diagram/). `?url` yields a real module URL.
/// <reference types="vite/client" />
import mermaidMountSrc from './diagram/mermaid-global-mount.ts?url';
---

<script type="module" src={mermaidMountSrc}></script>
