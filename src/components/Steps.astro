---
// import { processSteps } from './rehype-steps';
// const content = await Astro.slots.render('default');
// const { html } = processSteps(content);
---

<div class="ac-steps">
  <slot />
</div>

<style is:global>
  .ac-steps.ac-steps {
    --bullet-size: 2.5rem;
    --bullet-margin: 0.375rem;

    list-style: none;
    padding-inline-start: 0;
  }

  .ac-steps.ac-steps {
    counter-reset: step;
    padding-left: 1.5rem;
    margin-left: 0.75rem !important;
    border-left: 1px solid var(--sl-color-gray-5);
    overflow-wrap: break-word;
    margin-bottom: 2rem;
  }
  .ac-steps.ac-steps > p {
    margin-top: 0;
    margin-bottom: 1rem;
    margin-left: 0rem;
  }
  .ac-steps.ac-steps > ol {
    padding-left: 1.1rem;
    line-height: 1.5;
    margin-top: 0;
    margin-bottom: 1rem;
  }

  .ac-steps.ac-steps > ul {
    padding-left: 1.1rem;
    line-height: 1.5;
    margin-top: 0;
    margin-bottom: 1rem;
  }

  .ac-steps.ac-steps > li {
    margin-top: 0;
    margin-bottom: 0;
    margin-left: 0rem;
  }

  .ac-steps.ac-steps > h4 {
    margin-left: -0.25rem;
  }

  .ac-steps.ac-steps > * {
    margin-left: -0.25rem;
  }

  .ac-steps.ac-steps > h3 {
    counter-increment: step;
    color: var(--sl-color-gray-1);
    letter-spacing: -0.02rem;
    margin-top: 1.75rem;
    margin-bottom: 0;
    line-height: 0;
  }

  .ac-steps.ac-steps > h3::before {
    display: inline-block;
    border: 6px solid var(--sl-color-black);
    text-align: center;
    vertical-align: middle;
    content: counter(step);
    border-radius: 99rem;
    margin-top: -0.25rem;
    margin-bottom: 0;
    margin-left: -2.55rem;
    margin-right: 0.15rem;
    font-size: 1rem;
    height: var(--bullet-size);
    width: var(--bullet-size);
    line-height: 1.6rem;
    /* background-color: #cb361d; */
    color: var(--sl-color-gray-2);
    background-color: var(--sl-color-gray-6);
    box-shadow: inset 0 0 0 1px var(--sl-color-gray-5);
  }
</style>
