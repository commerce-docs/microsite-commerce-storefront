<div class="conceptContainer concept not-content">
  <slot />
</div>

<style>
  .conceptContainer {
    padding-left: 0.5rem;
    margin-left: 0.5rem;
    /* border-left: 1px solid var(--sl-color-gray-5); */
    overflow-wrap: break-word;
  }
  .conceptContainer > :global(p) {
    margin-top: 0;
    margin-bottom: 1rem;
  }

  .concept > :global(h3) {
    color: var(--sl-color-gray-1);
    font-size: 1.1rem;
  }

  .concept > :global(h3::before) {
    display: inline-block;
    border-color: var(--sl-color-black);
    text-align: center;
    vertical-align: baseline;
    color: var(--sl-color-gray-7);
    content: '💡';
    border-radius: 9999px;
    margin-top: 0;
    margin-bottom: 0;
    margin-left: -1.1rem;
    margin-right: 4px;
    height: 1rem;
    width: 1rem;
  }
</style>
