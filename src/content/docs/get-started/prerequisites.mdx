---
title: Prerequisites
description: Services, tools, and access you need before building an Adobe Commerce storefront on Edge Delivery Services.
sidebar:
  order: 2
---

import { Aside } from '@astrojs/starlight/components';
import LinkCard from '@components/LinkCard.astro';
import ProseFlow from '@components/ProseFlow.astro';

<ProseFlow variant="article">

Before you run the boilerplate setup or follow the B2C/B2B guide, confirm you have access to the services and tools listed here. Missing any of these will block you at a specific step — it's faster to check now.

## Required Adobe services

You need active access to all three:

- **Adobe Commerce** (2.4.7 or later recommended) — your backend catalog, pricing, and order management. See the [compatibility package](/setup/configuration/storefront-compatibility/install/) for version-specific requirements.
- **Edge Delivery Services** — the delivery and rendering layer. Provisioned through your Adobe contract; coordinate with your Adobe account team if you don't have access.
- **Adobe I/O** — required for API Mesh, Commerce eventing, and Experience Platform integrations. Access is provisioned per-organization.

<Aside type="tip" title="Licenses">
See [Licensing](/licensing/) for the full list of entitlements required for B2C and B2B storefronts.
</Aside>

## Development tools

| Tool | Minimum version | Notes |
|------|----------------|-------|
| Node.js | 18.x LTS | 20.x or later recommended |
| npm or pnpm | npm 9 / pnpm 8 | The boilerplate uses pnpm |
| Git | 2.x | Required for the boilerplate workflow |
| GitHub account | — | The boilerplate is a GitHub template; you create your project from it |

## Local environment

- A code editor (VS Code recommended — the boilerplate includes an `.editorconfig`)
- Access to a terminal / shell (bash or zsh)
- The [AEM Sidekick browser extension](https://chromewebstore.google.com/detail/aem-sidekick/ccfggkjabjahcjoljmgmklhpaccedipo) for previewing and publishing Edge Delivery pages

## Commerce backend access

You need credentials and endpoint information before you can configure the storefront:

- Commerce GraphQL endpoint URL
- Commerce REST API base URL
- An API key or integration token with catalog, cart, checkout, and customer read/write scopes
- (B2B only) B2B module enabled on the Commerce instance

## Next steps

Once you have these in place, read [Core concepts](/get-started/core-concepts/) for a plain-English map of how the pieces fit together, then follow [Build a B2C storefront](/get-started/create-storefront/) or [Build a B2B storefront](/dropins-b2b/).

</ProseFlow>
