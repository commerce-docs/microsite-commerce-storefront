---
title: Storefront SDK
description: The Drop-in SDK gives you the components, design tokens, and framework to build and customize your Adobe Commerce storefront.
---

import Aside from '@components/Aside.astro';
import CardGrid from '@components/CardGrid.astro';
import Link from '@components/Link.astro';
import LinkCard from '@components/LinkCard.astro';

The Drop-in SDK is the toolkit behind every Commerce drop-in. It gives you the same components, design tokens, and framework that Adobe uses to build the cart, checkout, product detail, and account experiences. Your customizations match the drop-ins already in your storefront, whether you add a single button to a block or build a custom Commerce experience from scratch.

The SDK ships as the <Link href="https://www.npmjs.com/package/@dropins/tools" text="@dropins/tools package on npm" />, where you can always check the current version.

## What the SDK gives you

The design system provides ready-made UI components and design tokens. Components are the visual building blocks, such as buttons, inputs, and cards. Design tokens are the color, type, and spacing values that keep those components consistent, so rebranding your storefront stays quick.

The framework handles everything around the UI: it mounts components into blocks, renders them, passes data between them through events, and loads them with live data from the Commerce backend.

## Explore the SDK

Open whichever topic answers your current question. If you are new to the SDK, start with the components you plan to render.

<CardGrid columns={2}>
  <LinkCard
    noborder
    icon="puzzle"
    title="Components"
    description="Browse the buttons, inputs, cards, and other UI building blocks you render into blocks."
    link="/sdk/components/overview/"
    rightIcon="right-arrow"
  />
  <LinkCard
    noborder
    icon="pencil"
    title="Design system"
    description="Color, typography, spacing, shape, and grid tokens that keep components on-brand."
    link="/sdk/design/"
    rightIcon="right-arrow"
  />
  <LinkCard
    noborder
    icon="open-book"
    title="Reference"
    description="Core APIs for events, rendering, initialization, slots, GraphQL, and reCAPTCHA."
    link="/sdk/reference/"
    rightIcon="right-arrow"
  />
  <LinkCard
    noborder
    icon="document"
    title="Utilities"
    description="Small helpers for forms, cookies, debouncing, and deep merges."
    link="/sdk/utilities/"
    rightIcon="right-arrow"
  />
  <LinkCard
    noborder
    icon="rocket"
    title="CLI"
    description="Run the Elsie CLI to scaffold drop-ins and generate GraphQL types."
    link="/sdk/cli/"
    rightIcon="right-arrow"
  />
</CardGrid>

<Aside type="note">
If the SDK does not cover what you need, reach out to the Commerce team.
</Aside>
