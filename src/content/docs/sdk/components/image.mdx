---
title: Image
description: Use Image to display product photos and visual content, with optional responsive srcset generation from URL parameters.
---

import OptionsTable from '@components/OptionsTable.astro';

Use `Image` to display visual content like product photos and illustrations. When you pass `params`, it generates a responsive `srcset` from the source URL, and it adds a loaded class once the image finishes loading.

## Import

```js
import { Image } from '@dropins/tools/components.js';
```

## Props

<OptionsTable
compact
  options={[
    ['Prop', 'Type', 'Req?', 'Description'],
    ['src', 'string', 'Yes', 'Image URL.'],
    ['params', 'ResolveImageUrlOptions', 'No', 'Parameters appended to the image URL to generate a responsive srcset.'],
    ['sizes', 'string', 'No', 'The sizes attribute for responsive images.'],
    ['onLoad', '(e: any) => void', 'No', 'Callback fired when the image finishes loading.'],
  ]}
/>

`Image` also accepts standard image HTML attributes (for example, `alt`, `width`, `height`, and `loading`, which defaults to `lazy`).

## Example

Render a product image with explicit dimensions and a responsive srcset:

```js
import { Image, provider as UI } from '@dropins/tools/components.js';

await UI.render(Image, {
  src: '/media/catalog/product/shoe.jpg',
  alt: 'Running shoe',
  width: 384,
  height: 288,
  params: { width: 384 },
})(document.querySelector('.image'));
```

## Related

- [Image Swatch](/sdk/components/imageswatch/) — a selectable, image-based option built on Image.
- [Content Grid](/sdk/components/contentgrid/) — lay out images in a responsive grid.
