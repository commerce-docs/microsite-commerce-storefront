---
title: Picker
description: Let shoppers select a single option from a styled dropdown, with variants, sizes, an optional icon, and a floating label.
---

import TableWrapper from '@components/TableWrapper.astro';

Use `Picker` to let a shopper select one option from a dropdown. It supports primary and secondary variants, two sizes, an optional icon, and a floating label.

## Import

```js
import { Picker } from '@dropins/tools/components.js';
```

## Props

<TableWrapper nowrap={[0]}>

| Prop | Type | Req? | Description |
|---|---|---|---|
| `options` | `PickerOption[]` | No | The selectable options. Each option has a `value`, an optional `text` label, an optional `icon`, and an optional `disabled` flag. |
| `value` | `PickerValue` | No | The currently selected value. |
| `defaultOption` | `PickerOption` | No | The option selected by default. |
| `name` | `string` | No | Field name, used to map the value in a form. |
| `placeholder` | `string` | No | Text shown when no option is selected. |
| `variant` | `'primary' \| 'secondary'` | No | Visual style. Defaults to primary. |
| `size` | `'medium' \| 'large'` | No | Picker size. Defaults to medium. |
| `floatingLabel` | `string` | No | Floating label text. |
| `icon` | `VNode` | No | Icon rendered inside the picker. |
| `disabled` | `boolean` | No | Disable the picker. |
| `disableWhenSingle` | `boolean` | No | Disable the picker when only one option is available. |
| `error` | `boolean` | No | Show the error state styling. |
| `id` | `string` | No | Element id. |
| `handleSelect` | `(event: Event) => void` | No | Called when the shopper selects an option. |

</TableWrapper>

`Picker` also accepts standard HTML select attributes (for example, `className`), except `value`, `size`, and `icon`.

## Example

```js
import { Picker, provider as UI } from '@dropins/tools/components.js';

await UI.render(Picker, {
  name: 'sortBy',
  variant: 'primary',
  placeholder: 'Select an option',
  options: [
    { value: 'option1', text: 'Option 1' },
    { value: 'option2', text: 'Option 2', disabled: true },
    { value: 'option3', text: 'Option 3' },
  ],
  handleSelect: (event) => yourApplySort(event),
})(document.querySelector('.sort-by'));
```

## Related

- [MultiSelect](/sdk/components/multiselect/) — select multiple options.
- [Icon](/sdk/components/icon/) — provide the `icon` prop.
