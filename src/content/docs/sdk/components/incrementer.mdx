---
title: Incrementer
description: Use Incrementers to adjust numeric values with plus and minus buttons. Set `showButtons` to `false` to hide the increase/decrease buttons for a compact design.
tableOfContents: false
iframe:
    src: "components-incrementer--overview"
---

import IFrame from '@components/IFrame.astro'

<IFrame src={frontmatter.iframe.src} />
