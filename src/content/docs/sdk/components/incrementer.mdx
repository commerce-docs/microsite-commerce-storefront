---
title: Incrementer
description: Use Incrementers to adjust numeric values with plus and minus buttons. Set `showButtons` to `false` for a compact design without the increase/decrease buttons.
tableOfContents: false
iframe:
    src: "components-incrementer--overview"
---

import IFrame from '@components/IFrame.astro'

<IFrame src={frontmatter.iframe.src} />
