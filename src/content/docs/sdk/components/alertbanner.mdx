---
title: AlertBanner
description: Surface a dismissible banner that notifies shoppers and prompts them to take action, with variants for brand, neutral, success, and warning states.
---

import OptionsTable from '@components/OptionsTable.astro';

Use `AlertBanner` to notify a shopper and prompt them to take action. It renders a dismissible banner with a message, an optional icon, and an optional action button.

## Import

```js
import { AlertBanner } from '@dropins/tools/components.js';
```

## Props

<OptionsTable
compact
  options={[
    ['Prop', 'Type', 'Req?', 'Description'],
    ['variant', "'brand' | 'neutral' | 'success' | 'warning'", 'Yes', 'The banner variant.'],
    ['message', 'VNode', 'Yes', 'The banner message.'],
    ['onDismiss', '() => void', 'Yes', 'Handler called when the shopper dismisses the banner.'],
    ['icon', 'VNode', 'No', 'The banner icon.'],
    ['action', '{ label: string; onClick: (event: Event) => void }', 'No', 'Optional action with a label and an onClick handler.'],
  ]}
/>

`AlertBanner` also accepts standard `<div>` HTML attributes (for example, `className`), except `icon` and `action`.

## Example

Render a banner with a message, an icon, an action, and a dismiss handler. Build the `message` and `icon` VNodes with `h`.

```js
import { AlertBanner, Icon, provider as UI } from '@dropins/tools/components.js';
import { h } from '@dropins/tools/preact.js';

await UI.render(AlertBanner, {
  variant: 'success',
  icon: h(Icon, { source: 'InfoFilled' }),
  message: h('span', {}, 'Your changes were saved.'),
  action: {
    label: 'Undo',
    onClick: () => yourUndoChanges(),
  },
  onDismiss: () => yourHideBanner(),
})(document.querySelector('.alert-banner'));
```

## Related

- [InLineAlert](/sdk/components/inlinealert/) — for inline messages tied to a specific field or section.
- [Icon](/sdk/components/icon/) — pass an `Icon` as the `icon` prop.
