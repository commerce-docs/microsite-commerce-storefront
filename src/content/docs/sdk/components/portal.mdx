---
title: Portal
description: Mount UI into a separate DOM node outside the parent tree so overlays, tooltips, and popovers escape layout constraints safely.
tableOfContents: false
iframe:
    src: "components-portal--overview"
---

import IFrame from '@components/IFrame.astro'

<IFrame src={frontmatter.iframe.src} />