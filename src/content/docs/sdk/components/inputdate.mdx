---
title: InputDate
description: Capture a date with an accessible field that supports keyboard entry, a date picker, and validation, building on the Input component.
---

import TableWrapper from '@components/TableWrapper.astro';
import Aside from '@components/Aside.astro';

Use `InputDate` to capture a date in a form. It builds on the [Input](/sdk/components/input/) component, adding date entry and field-specific validation.

## Import

```js
import { InputDate } from '@dropins/tools/components.js';
```

## Props

<TableWrapper nowrap={[0]}>

| Prop | Type | Req? | Description |
|---|---|---|---|
| `label` | `string` | No | Displayed field label. |
| `name` | `string` | No | Form field identifier. |
| `value` | `string` | No | The date value in `YYYY-MM-DD` format (for example, 2023-07-29). Displayed to the shopper in their browser's locale format (for example, 07/29/2023 in en-US). |
| `error` | `string` | No | Field-specific error message. |

</TableWrapper>

`InputDate` inherits the remaining [Input](/sdk/components/input/) props (such as `onChange`, `onBlur`, `disabled`, and `placeholder`), except `error` and `value`, which it redefines above.

<Aside type="caution">
Don't pass your own `onFocus` handler. `InputDate` uses `onFocus` internally to open the date picker, and a handler you pass replaces that behavior instead of running alongside it.
</Aside>

## Example

```js
import { InputDate, provider as UI } from '@dropins/tools/components.js';

await UI.render(InputDate, {
  name: 'birthdate',
  label: 'Birthdate',
  value: '2023-07-29',
  onChange: (event) => yourSaveBirthdate(event.target.value),
})(document.querySelector('.birthdate'));
```

## Related

- [Input](/sdk/components/input/) — the base text input `InputDate` extends.
- [Field](/sdk/components/field/) — wraps inputs with labels and validation messages.
