---
title: How to brand drop-ins
description: How to override the Base design tokens to match your brand.
---

import { Tabs, TabItem, Code } from '@astrojs/starlight/components';
import Summary from '@components/Summary.astro';
import Diagram from '@components/Diagram.astro';
import CodeExample from '@components/CodeExample.astro';
import Steps from '@components/Steps.astro';

<Diagram caption="This is the caption that can be long in order to explain the graphic.">
  ![Diagram for how to brand drop-ins](./images/brand-bigpicture.svg)
</Diagram>

<CodeExample
  code={`
    .button {
      background-color: var(--color-primary);
      color: var(--color-on-primary);
    }
  `}
  lang="css"
  title="example.css"
  mark='{"1":1}'
></CodeExample>

```json title="example.json" {"1. Provide the value prop here:":5-6} del={"2. Remove the disabled and active states:":8-10} ins={"3. Add this to render the children inside the button:":12-15}
{
  "name": "dropins.dev",
  "version": "0.1.0",
  "license": "Apache-2.0",

  "description": "Documentation for Adobe's collection of drop-in components for your commerce storefront.",
  "author": "Adobe, Inc.",

  "type": "module",
  "scripts": {
    "astro": "astro",

    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "prettier --write  \"**/*.{js,jsx,ts,tsx,md,mdx,astro}\" && eslint --fix \"src/**/*.{js,ts,jsx,tsx,astro}\"",
    "clean": "rm -rf dist && rm -rf .astro && rm -rf node_modules && pnpm install",
    "scrub": "rm -rf dist && rm -rf .astro && rm -rf node_modules && rm -rf pnpm-lock.yaml && rm -rf ~./.pnpm-store && pnpm install"
  }
}
```

{/*
import colorJson from '@data/colors.json?raw';
export const fileName = 'colors.json';
export const mark = '{"1":5} del={"2":8} ins={"3":12}';
export const collapse = 'collapse={1-2,12-200}';

<CodeExample code={colorJson} lang="json" title={fileName} mark={mark} /> */}

<Steps>
## Open `styles.css`

Open the `styles.css` file in the boilerplate project and find the `<styles>` section

</Steps>
