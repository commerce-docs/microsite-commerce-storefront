---
title: DROPIN_NAME configurations
description: Learn about the configuration variables for the DROPIN_NAME dropin.
---

import Diagram from '@components/Diagram.astro';
import Callouts from '@components/Callouts.astro';
import OptionsTable from '@components/OptionsTable.astro';
import Aside from '@components/Aside.astro';

This section describes the configuration options for the DROPIN_NAME.

## Configuration options

The DROPIN_NAME provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['example-option', 'string', 'Yes', 'The unique identifier for the dropin.'],
  ]}
/>

_Describe each configuration option with it's own three-level heading and paragraph as shown below for `example-option`._

### example-option

The `example-option` property is a string that [does something]. The property's value is used to [do something].

[Insert example option settings here.]

```js
configuration example
```
