---
title: Checkout functions
description: Learn about the API functions provided by the Checkout dropin.
---

import Aside from '@components/Aside.astro';

This topic provides the details and instructions you need to use the functions provided by the Checkout dropin.

## estimateShippingMethods

The `estimateShippingMethods` function returns a list with the available shipping methods for the given criteria. The criteria could be a combination of fields: country, region, region identifier and zip code.

```ts
import { estimateShippingMethods } from '@/checkout/api/estimateShippingMethods';
```

### Example

See the following example for usage details:

```ts
import { estimateShippingMethods } from '@/checkout/api/estimateShippingMethods';
. . .

// By country code and region name
const input = {
  criteria: {
    country_code: 'US',
    region_name: 'FL',
  },
};
const estimateShippingMethods = estimateShippingMethods(input);

// By country code and region ID
const input = {
  criteria: {
    country_code: 'US',
    region_id: 18,
  },
};
const estimateShippingMethods = estimateShippingMethods(input);

// By country code and postal code
const input = {
  criteria: {
    country_code: 'US',
    zip: '80000',
  },
};
const estimateShippingMethods = estimateShippingMethods(input);

// By country code, region and postal code
const input = {
  criteria: {
    country_code: 'US',
    region_name: 'FL',
    zip: '80000',
  },
};
const estimateShippingMethods = estimateShippingMethods(input);

// By country code, region, region id and postal code
// This scenario is specific for those regions which have more than a region ID
// associated (i.e. Armed Forces region 'AE' with some regions IDs); if the region has
// just a region ID,
const input = {
  criteria: {
    country_code: 'US',
    region_name: 'FL',
    region_id: 18,
    zip: '80000',
  },
};
const estimateShippingMethods = estimateShippingMethods(input);
```

## fetchAddressFormFields

The `fetchAddressFormFields` query retrieves a list of EAV attributes associated with customer register address frontend form.

See [attributesForm query](https://developer.adobe.com/commerce/webapi/graphql/schema/attributes/queries/attributes-form/) for more details.

```ts
import { fetchAddressFormFields } from '@/checkout/api/fetchAddressFormFields';
```

### Example

See the following example for usage details:

```ts
import { fetchAddressFormFields } from '@/checkout/api/fetchAddressFormFields';

const addressFormFields = fetchAddressFormFields();
```

## getCart

A function that returns checkout data for a cart ID.

```ts
import { getCart } from '@/checkout/api/getCart';
```

### Example

See the following example for usage details:

```ts
getCart();
```

## getCheckoutData

Add function description...

### Example

See the following example for usage details:

{/* Add useful example of using this function within a storefront implementation. */}

```typescript
code example
```

## getCountries

The `getCountries` function is used to get a list of countries.

```ts
import { getCountries } from '@/checkout/api/getCountries';
```

### Example

See the following example for usage details:

```ts
getCountries();
```

## getCustomer

Add function description...

### Example

See the following example for usage details:

{/* Add useful example of using this function within a storefront implementation. */}

```typescript
code example
```

## getRegions

A function that returns "Howdy World!".

```ts
import { getRegions } from '@/checkout/api/getRegions';
```

### Example

See the following example for usage details:

```ts
getRegions();
```

# getStoreConfig

The storeConfig query defines information about a store's configuration. You can query a non-default store by changing the header in your GraphQL request.

See [storeConfig query](https://developer.adobe.com/commerce/webapi/graphql/schema/store/queries/store-config/)

```ts
import { storeConfig } from '@/checkout/api/store/storeConfig';
```

### Example

See the following example for usage details:

```ts
import { storeConfig } from '@/checkout/api/store/storeConfig';

storeConfig();
```

## initializeCheckout

Add function description...

### Example

See the following example for usage details:

{/* Add useful example of using this function within a storefront implementation. */}

```typescript
code example
```

## isEmailAvailable

The isEmailAvailable query checks whether the specified email has already been used to create a customer account. A value of true indicates the email address is available, and the customer can use the email address to create an account.

See [isEmailAvailable query](https://developer.adobe.com/commerce/webapi/graphql/schema/customer/queries/is-email-available/)

```ts
import { isEmailAvailable } from '@/checkout/api/isEmailAvailable';
```

### Example

See the following example for usage details:

```ts
import { isEmailAvailable } from '@/checkout/api/isEmailAvailable';

isEmailAvailable('test@example.com');
```

## placeOrder

The `placeOrder` API is used to place an order.

See [placeOrder mutation](https://developer.adobe.com/commerce/webapi/graphql/schema/cart/mutations/place-order/) for more details.

```ts
import { placeOrder } from '@/checkout/api/placeOrder';
```

### Example

See the following example for usage details:

```ts
placeOrder('4L7E1H0muABuggqWxbzBCBWoeG0rNxDh');
```

## redirect

A function that redirects the user to a URL.

```ts
import { redirect } from '@/checkout/api/redirect';
```

### Example

See the following example for usage details:

```ts
redirect('https://example.com');
```

## resetCheckout

Add function description...

### Example

See the following example for usage details:

{/* Add useful example of using this function within a storefront implementation. */}

```typescript
code example
```

## resetCustomer

Add function description...

### Example

See the following example for usage details:

{/* Add useful example of using this function within a storefront implementation. */}

```typescript
code example
```

## setBillingAddress

The `setBillingAddress` mutation sets the billing address for a specific cart.

See [setBillingAddressOnCart mutation](https://developer.adobe.com/commerce/webapi/graphql/schema/cart/mutations/set-billing-address/) for more details.

```ts
import { setBillingAddress } from '@/checkout/api/setBillingAddress';
```

### Example

See the following example for usage details:

To set the billing address as the same as the shipping address:

```ts
import { setBillingAddress } from '@/checkout/api/setBillingAddress';
setBillingAddress({
  same_as_shipping: true,
});
```

To set an specific billing address:

```ts
import { setBillingAddress } from '@/checkout/api/setBillingAddress';
setBillingAddress({
  address: {
    firstname: 'John',
    lastname: 'Doe',
    street: ['123 Main St', 'Apt 1'],
    city: 'New York',
    state: 'NY',
    postal_code: '10001',
    country: 'US',
    telephone: '555-555-5555',
  },
  same_as_shipping: false,
});
```

## setGuestEmailOnCart

A logged-in customer specifies an email address when they create an account. Therefore, you can place the order without explicitly setting the email.

See [setGuestEmailOnCart query](https://developer.adobe.com/commerce/webapi/graphql/schema/cart/mutations/set-guest-email/)

```ts
import { setGuestEmailOnCart } from '@/checkout/api/setGuestEmailOnCart';
```

### Example

See the following example for usage details:

```ts
import { setGuestEmailOnCart } from '@/checkout/api/setGuestEmailOnCart';

setGuestEmailOnCart({
  email: 'test@example.com',
});
```

## setPaymentMethod

A function that sets the provided payment method to the current cart.

```ts
import { setPaymentMethod } from '@/checkout/api/setPaymentMethod';
```

### Example

See the following example for usage details:

```ts
setPaymentMethod('payment-method-code');
```

## setShippingAddress

A function that calls `setShippingAddressesOnCart` mutation that sets one or more shipping addresses.

See [`setShippingAddressesOnCart` mutation](https://developer.adobe.com/commerce/webapi/graphql/schema/cart/mutations/set-shipping-address/)

```ts
import { setShippingAddress } from '@/checkout/api/setShippingAddress';
```

### Example

See the following example for usage details:

```ts
setShippingAddress({
  address: {
    city: 'San Francisco',
    country_code: 'US',
    firstname: 'John',
    lastname: 'Doe',
    postcode: '94103',
    region: {
      region_code: 'CA',
      region: 'California',
    },
    street: ['1234 Main Street'],
    telephone: '555-555-5555',
  },
});
```

## setShippingMethodsOnCart

The `setShippingMethodsOnCart` mutation sets one or more delivery methods on a cart.

See [setShippingMethodsOnCart mutation](https://developer.adobe.com/commerce/webapi/graphql/schema/cart/mutations/set-shipping-method/)

```ts
import { setShippingMethodsOnCart } from '@/checkout/api/setShippingMethods';
```

### Example

See the following example for usage details:

```ts
import { setShippingMethodsOnCart } from '@/checkout/api/setShippingMethods';

setShippingMethodsOnCart([
  {
    carrier_code: 'flatrate',
    method_code: 'flatrate',
  },
]);
```