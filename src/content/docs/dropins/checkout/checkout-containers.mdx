---
title: Checkout containers and configuration
description: Learn about the containers and configurations for the Checkout dropin.
---

import Diagram from '@components/Diagram.astro';
import Callouts from '@components/Callouts.astro';
import OptionsTable from '@components/OptionsTable.astro';
import Aside from '@components/Aside.astro';
import { Steps } from '@astrojs/starlight/components';

This section describes the containers and configuration options for the Checkout dropin.

## Big Picture

{/* Replace `placeholder.webp` with a diagram identifying all its containers. */}

<Diagram caption="DROPIN_NAME containers">
  ![DROPIN_NAME containers](@images/placeholder.webp)
</Diagram>

{/* Use `Callouts` control to name the containers in the dropin. */}

<Callouts>

{/* Use a numbered list to identify the slot names. */}

1. **BillingForm**: Provides the DESCRIBE_PURPOSE.
1. **BillToShippingAddress**: Provides the DESCRIBE_PURPOSE.
1. **ErrorBanner:**: Provides the DESCRIBE_PURPOSE.
1. **EstimateShipping:**: Provides the DESCRIBE_PURPOSE.
1. **LoginForm:**: Provides the DESCRIBE_PURPOSE.
1. **MergedCartBanner:**: Provides the DESCRIBE_PURPOSE.
1. **OutOfStock:**: Provides the DESCRIBE_PURPOSE.
1. **PaymentMethods:**: Provides the DESCRIBE_PURPOSE.
1. **PlaceOrder:**: Provides the DESCRIBE_PURPOSE.
1. **ServerError:**: Provides the DESCRIBE_PURPOSE.
1. **ShippingForm:**: Provides the DESCRIBE_PURPOSE.
1. **ShippingMethods:**: Provides the DESCRIBE_PURPOSE.
   {/* Example 1: **ProductDetails**: Provides the layout and configuration options for displaying the details of a product. */}

</Callouts>

## BillingForm configurations

The `BillingForm` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['addressType', 'object', 'Yes', 'Specifies the type of address form, set to AddressFormType.BILLING for billing addresses.'],
    ['autoFill', 'boolean', 'Yes', 'Determines if the form should auto-fill based on existing address data.'],
    ['headingId', 'string', 'Yes', 'ID for the heading element, used for accessibility and localization.'],
    ['name', 'string', 'Yes', 'Name of the form, set to BILLING_FORM_NAME.'],
    ['ref', 'object', 'Yes', 'Reference to the AddressForm component, used to trigger form actions programmatically.'],
    ['saveAddressHandler', 'function', 'Yes', 'Callback function to handle saving the address.'],
    ['style', 'string', 'Yes', 'Inline styles for the form container, used to control visibility.'],
  ]}
/>

### How to change configurations

{/* Use the Steps control to add numbered steps that describe how to change container configurations. */}

<Steps>

1. Add instructions.
1. Add instructions.
1. Add instructions.
1. Add instructions.

</Steps>

### Example of changing configurations

{/* Provide a code example for the changes */}

```json
{
  "PROPERTY_NAME": "New value for the container's configuration property."
}
```

{/* Replace `placeholder.webp` with side-by-side screenshots of the before and after differences. */}

<Diagram caption="DROPIN_NAME containers">
  ![DROPIN_NAME containers](@images/placeholder.webp)
</Diagram>

## CONTAINER_NAME_1 configurations

The CONTAINER_NAME_1 container provides the following configuration options:

{/* EXAMPLE: */}
{/* <OptionsTable compact options={[ */}
{/* ['Option', 'Type', 'Req?', 'Description'], */}
{/* ['hideSku', 'boolean', 'No', 'Optional boolean to hide the SKU display.'], */}
{/* ]}/> */}