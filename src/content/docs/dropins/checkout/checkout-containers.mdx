---
title: Checkout containers and configuration
description: Learn about the containers and configurations for the Checkout dropin.
---

import Diagram from '@components/Diagram.astro';
import Callouts from '@components/Callouts.astro';
import OptionsTable from '@components/OptionsTable.astro';
import Aside from '@components/Aside.astro';
import { Steps } from '@astrojs/starlight/components';

This section describes the containers and configuration options for the Checkout dropin.

## Big Picture

The Checkout dropin contains a single container that provides the layout and functionality for the checkout process. It is designed to be flexible and customizable, allowing you to create a checkout experience that meets your business needs.

<Diagram caption="Checkout containers">![Checkout containers](@images/placeholder.webp)</Diagram>

<Callouts>

1. **ErrorBanner**: Displays error messages to the user.
1. **EstimateShipping**: Estimates shipping costs.
1. **LoginForm**: Allows the user to log in or create an account.
1. **MergedCartBanner**: Displays a banner when the cart is merged.
1. **OutOfStock**: Displays out-of-stock messages.
1. **PaymentMethods**: Displays available payment methods.
1. **PlaceOrder**: Allows the user to place an order.
1. **ServerError**: Displays server error messages.
1. **ShippingMethods**: Displays available shipping methods.

</Callouts>

## ErrorBanner configurations

The `ErrorBanner` container accepts a subset of properties defined by the `AlertBannerProps` interface. Here are the configuration options for this container:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['className', 'string', 'No', 'CSS class to apply to the container.'],
    ['data-testid', 'string', 'No', 'Identifier for the banner for testing purposes.'],
    ['icon', 'JSX.Element', 'No', 'Icon to display in the banner.'],
    ['message', 'string', 'Yes', 'Message to display in the banner.'],
    ['aria-label', 'string', 'No', 'Accessibility label for the banner.'],
    ['onDismiss', 'function', 'No', 'Function to call when the banner is dismissed.'],
    ['variant', 'string', 'No', 'Variant of the banner indicating the type of alert.'],
  ]}
/>

## EstimateShipping configurations

The `EstimateShipping` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['slot', 'object', 'No', 'An object that allows you to define custom content to be rendered within specific slots of the OrderSummary container.']
  ]}
/>

## LoginForm configurations

The `LoginForm` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['email', 'string', 'No', 'The email address to prefill in the email field.'],
    ['onSignInClick', 'function', 'No', 'A function that takes an email (string or null) as an argument and is called when the sign-in button is clicked.'],
    ['onSignOutClick', 'function', 'No', 'A function that is called when the sign-out button is clicked.'],
    ['hideOnEmptyCart', 'boolean', 'No', 'If set to true, the component will be hidden when the shopping cart is empty.'],
    ['hideOnVirtualCart', 'boolean', 'No', 'If set to true, the component will be hidden when the shopping cart contains only virtual items (for example, digital products, services).']
  ]}
/>

## MergedCartBanner configurations

The `MergedCartBanner` container accepts a subset of properties defined by the `AlertBannerProps` interface. Here are the configuration options for this container:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['className', 'string', 'No', 'CSS class to apply to the container.'],
    ['data-testid', 'string', 'No', 'Identifier for the banner for testing purposes.'],
    ['icon', 'JSX.Element', 'No', 'Icon to display in the banner.'],
    ['message', 'string', 'Yes', 'Message to display in the banner.'],
    ['aria-label', 'string', 'No', 'Accessibility label for the banner.'],
    ['onDismiss', 'function', 'No', 'Function to call when the banner is dismissed.'],
    ['variant', 'string', 'No', 'Variant of the banner indicating the type of alert.'],
    ['type', 'string', 'No', 'Type of alert.'],
    ['dismissable', 'boolean', 'No', 'Whether the banner can be dismissed.'],
    ['duration', 'number', 'No', 'Duration in milliseconds before the banner is automatically dismissed.']
  ]}
/>

## OutOfStock configurations

The `OutOfStock` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['routeCart', 'string', 'No', 'A function that returns the URL to the cart page.'],
    ['onCartProductsUpdate', 'function', 'No', 'A callback function that is called when the cart products are updated.']
  ]}
/>

## PaymentMethods configurations

The `PaymentMethods` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['slots', 'object', 'No', 'An object containing slot configurations for the main payment methods container and payment method handlers.']
  ]}
/>

## PlaceOrder configurations

The `PlaceOrder` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['onPlaceOrder', 'function', 'No', 'An asynchronous callback function that handles the place order action, including showing and hiding an overlay spinner and logging any errors that occur.']
  ]}
/>

## ServerError configurations

The `ServerError` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['onServerError', 'function', 'No', 'A  callback function that is called when a server error occurs.']
  ]}
/>

## ShippingMethods configurations

The `ShippingMethods` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['onCheckoutDataUpdate', 'function', 'No', 'An optional callback function that is called when the checkout data is updated.'],
    ['onShippingMethodSelect', 'function', 'No', 'An optional callback function that handles the selection of a shipping method.'],
    ['preSelectedMethod', 'object', 'No', 'An optional object representing the pre-selected shipping method with carrier and method codes.'],
    ['shippingMethodsSlot', 'object', 'No', 'An optional slot configuration for the shipping methods.'],
    ['HTMLAttributes', 'HTMLAttributes', 'No', 'Any standard HTML attributes applicable to a div element, such as className, id, and style.' ]
  ]}
/>

## Example configuration

The following example demonstrates how to configure the Checkout container:

```ts
tbd
```

## How to change configurations

In general, the steps for applying configuration options to containers is the same assuming that you're using the boilerplate code provided in the [Create your storefront tutorial](/get-started) section:

<Steps>

1. Open the file where the container is being rendered.

   For the `Checkout` container, it's the `/blocks/commerce-checkout.js` file.

1. Locate the configuration option(s) you want to change.

1. Modify an existing option or add a new one.

   The boilerplate code doesn't always show all options by default, so you may need to add them manually.

1. Save your changes and start the server. [Editorial note: What if the server is already running? Will these changes be picked up automatically?]

   ```bash
   npm start
   ```

1. Test the changes in your browser.

   {/* Replace `placeholder.webp` with side-by-side screenshots of the before and after differences. */}

   <Diagram caption="Checkin container configuration">
     ![Checkin containers](@images/placeholder.webp)
   </Diagram>

</Steps>
