---
title: Checkout data & events
description: Learn about the events used by the Checkout and the data available within the events.
sidebar:
  label: Events
  order: 5
---

{/*
  ⚠️ TEMPLATE USAGE GUIDE ⚠️
  
  This template is used by scripts/generate-events-docs.js to generate event documentation.
  
  WHAT THE SCRIPT USES FROM THIS TEMPLATE:
  - Section headings and descriptive text (used as-is)
  - Import statements
  - The REPEAT_FOR_EACH_EVENT block (replicated for each event)
  - Placeholders: Checkout, Checkout, EVENT_NAME, etc.
  
  WHAT THE SCRIPT GENERATES INDEPENDENTLY:
  - Table contents between START/END markers (example rows are for reference only)
  - Event-specific data (names, descriptions, payloads from source repos)
  
  TO CHANGE:
  - Section text/descriptions → Edit this template
  - Table structure (columns, links, formatting) → Edit scripts/generate-events-docs.js
  - Both template and script must be kept in sync for table structure changes
*/}

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

<Aside type="note" title="Auto-generated on 2025-10-20. Do not edit this page directly." />

The **Checkout** drop-in uses the [Event Bus](/sdk/reference/events/) to emit and listen to events for communication between drop-ins and external integrations.

## Emits

These events are produced by the **Checkout** drop-in. Subscribe to these events to react to state changes, trigger custom logic, integrate with third-party services, or coordinate with other drop-ins and components in your storefront.

{/* EMITS_TABLE_START */}
<TableWrapper nowrap={[0, 1]}>

| Event | Direction | Description |
|-------|-----------|-------------|
| [checkout/values](#checkoutvalues) | Emits | Emitted when form or configuration values change |

</TableWrapper>
{/* EMITS_TABLE_END */}

## Listens

These events are consumed by the **Checkout** drop-in. Emit these events from your storefront application, other drop-ins, or external integrations to communicate with and control this drop-in's behavior.

{/* LISTENS_TABLE_START */}
<TableWrapper nowrap={[0, 1]}>

| Event | Direction | Description |
|-------|-----------|-------------|
| [cart/initialized](#cartinitialized) | Listens | Fired by Cart (`cart`) when the component completes initialization |
| [cart/merged](#cartmerged) | Listens | Fired by Cart (`cart`) when data is merged |
| [cart/reset](#cartreset) | Listens | Fired by Cart (`cart`) when the component state is reset |
| [locale](#locale) | Listens | Fired by external source when locale/language changes |

</TableWrapper>
{/* LISTENS_TABLE_END */}

## Emits and Listens

These events flow in both directions, enabling a bidirectional data channel between the **Checkout** drop-in and external code:

- **The drop-in emits** these events when its internal state changes (for example, when a user interacts with the UI)
- **The drop-in listens** for these same events from external sources (your storefront code, other drop-ins, or integrations) to update its internal state programmatically

This pattern allows you to both **react to** the drop-in's state changes (by subscribing to events it emits) and **control** the drop-in's state (by emitting events that it listens for). Common use cases include server synchronization, cross-tab updates, analytics integration, and coordinating multiple drop-ins.

{/* BIDIRECTIONAL_TABLE_START */}
<TableWrapper nowrap={[0, 1]}>

| Event | Direction | Description |
|-------|-----------|-------------|
| [authenticated](#authenticated) | Emits and Listens | Triggered when authentication state changes |
| [cart/data](#cartdata) | Emits and Listens | Triggered when data is available or changes |
| [checkout/error](#checkouterror) | Emits and Listens | Triggered when an error occurs |
| [checkout/initialized](#checkoutinitialized) | Emits and Listens | Triggered when the component completes initialization |
| [checkout/updated](#checkoutupdated) | Emits and Listens | Triggered when the component state is updated |
| [shipping/estimate](#shippingestimate) | Emits and Listens | Triggered when an estimate is calculated |

</TableWrapper>
{/* BIDIRECTIONAL_TABLE_END */}


## checkout/values

**Direction**: Emits

Emitted when form or configuration values change

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const checkoutValuesListener = events.on('checkout/values', (data) => {
  console.log('checkout/values event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
checkoutValuesListener.off();
```

## cart/initialized

**Direction**: Listens

Fired by Cart (`cart`) when the component completes initialization

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const cartInitializedListener = events.on('cart/initialized', (data) => {
  console.log('cart/initialized event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
cartInitializedListener.off();
```

## cart/merged

**Direction**: Listens

Fired by Cart (`cart`) when data is merged

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const cartMergedListener = events.on('cart/merged', (data) => {
  console.log('cart/merged event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
cartMergedListener.off();
```

## cart/reset

**Direction**: Listens

Fired by Cart (`cart`) when the component state is reset

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const cartResetListener = events.on('cart/reset', (data) => {
  console.log('cart/reset event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
cartResetListener.off();
```

## locale

**Direction**: Listens

Fired by external source when locale/language changes

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const localeListener = events.on('locale', (data) => {
  console.log('locale event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
localeListener.off();
```

## authenticated

**Direction**: Emits and Listens

Triggered when authentication state changes

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const authenticatedListener = events.on('authenticated', (data) => {
  console.log('authenticated event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
authenticatedListener.off();
```

## cart/data

**Direction**: Emits and Listens

Triggered when data is available or changes

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const cartDataListener = events.on('cart/data', (data) => {
  console.log('cart/data event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
cartDataListener.off();
```

## checkout/error

**Direction**: Emits and Listens

Triggered when an error occurs

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const checkoutErrorListener = events.on('checkout/error', (data) => {
  console.log('checkout/error event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
checkoutErrorListener.off();
```

## checkout/initialized

**Direction**: Emits and Listens

Triggered when the component completes initialization

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const checkoutInitializedListener = events.on('checkout/initialized', (data) => {
  console.log('checkout/initialized event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
checkoutInitializedListener.off();
```

## checkout/updated

**Direction**: Emits and Listens

Triggered when the component state is updated

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const checkoutUpdatedListener = events.on('checkout/updated', (data) => {
  console.log('checkout/updated event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
checkoutUpdatedListener.off();
```

## shipping/estimate

**Direction**: Emits and Listens

Triggered when an estimate is calculated

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const shippingEstimateListener = events.on('shipping/estimate', (data) => {
  console.log('shipping/estimate event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
shippingEstimateListener.off();
```

