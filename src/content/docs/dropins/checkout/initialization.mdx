---
title: Checkout initialization
description: Configure the Checkout drop-in with language definitions, custom data models, and drop-in-specific options.
sidebar:
  label: Initialization
  order: 3
---

import TableWrapper from '@components/TableWrapper.astro';
import { Aside } from '@astrojs/starlight/components';

The **Checkout initializer** configures the checkout process, including shipping methods, payment options, address handling, and B2B features like negotiable quotes. Use initialization to set default behaviors, filter available options, and customize cart and customer data models displayed during checkout.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>



## Configuration options

The following table describes the configuration options available for the **Checkout** initializer:

<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `langDefinitions` | `LangDefinitions` | No | Language definitions for internationalization (i18n). Override dictionary keys for localization or branding. |
| `models` | `Record<string, any>` | No | Custom data models for type transformations. Extend or modify default models with custom fields and transformers. |
| `defaults` | `` | No | Configuration for defaults. |
| `shipping` | `` | No | Configuration for shipping. |
| `features` | `` | No | Configuration for features. |

</TableWrapper>

## Basic initialization

Initialize the drop-in with default settings:

```javascript title="scripts/initializers/checkout.js"
import { initializers } from '@dropins/tools/initializer.js';
import { initialize } from '@dropins/storefront-checkout';

await initializers.mountImmediately(initialize, {});
```

## Language definitions

Override dictionary keys for localization or branding. The `langDefinitions` object maps locale keys to custom strings that override the drop-in's default text.

```javascript title="scripts/initializers/checkout.js"
import { initializers } from '@dropins/tools/initializer.js';
import { initialize } from '@dropins/storefront-checkout';

const customStrings = {
  'AddToCart': 'Add to Bag',
  'Checkout': 'Complete Purchase',
  'Price': 'Cost',
};

const langDefinitions = {
  default: customStrings,
};

await initializers.mountImmediately(initialize, { langDefinitions });
```

<Aside type="tip">
For complete dictionary customization including all available keys and multi-language support, see the [Checkout Dictionary](/dropins/checkout/dictionary/) page.
</Aside>

## Customizing data models

Extend or transform data models by providing custom transformer functions. Use the `models` option to add custom fields or modify existing data structures returned from the backend.

The following example shows how to customize the `CartModel` model for the **Checkout** drop-in:

```javascript title="scripts/initializers/checkout.js"
import { initializers } from '@dropins/tools/initializer.js';
import { initialize } from '@dropins/storefront-checkout';

const models = {
  CartModel: {
    transformer: (data) => ({
      // Add custom fields from backend data
      customField: data?.custom_field,
      promotionBadge: data?.promotion?.label,
      // Transform existing fields
      displayPrice: data?.price?.value ? `$${data.price.value}` : 'N/A',
    }),
  },
};

await initializers.mountImmediately(initialize, { models });
```

### Available models

The following models can be customized through the `models` configuration option:


<TableWrapper nowrap={[0]}>

| Model | Description |
|---|---|
| [`CartModel`](#cartmodel) | Transforms cart data during the checkout process including items, totals, shipping, and billing information. Use this to add custom fields or modify the cart data structure presented in checkout. |
| [`CustomerModel`](#customermodel) | Transforms customer data during checkout including name, email, and saved addresses. Use this to add custom fields or modify customer information displayed in checkout forms. |

</TableWrapper>

## Drop-in-specific configuration

The **Checkout** drop-in provides additional configuration options beyond the standard `langDefinitions` and `models`. These options customize drop-in-specific behaviors and features.

```javascript title="scripts/initializers/checkout.js"
import { initializers } from '@dropins/tools/initializer.js';
import { initialize } from '@dropins/storefront-checkout';

await initializers.mountImmediately(initialize, {
  // Drop-in-specific configuration
  defaults: {},
  shipping: {},
  features: {},
});
```

<Aside type="note">
Refer to the [Configuration options](#configuration-options) table for descriptions of each option.
</Aside>




## Model definitions

The following TypeScript definitions show the structure of each customizable model:

### CartModel

```typescript
export interface CartAddress extends Address {}

```

### CustomerModel

```typescript
export interface Customer {
  firstName: string;
  lastName: string;
  email: string;
}
```

## Related resources

- [Checkout Dictionary](/dropins/checkout/dictionary/) - Customize user-facing text and labels
- [Checkout overview](/dropins/checkout/) - Learn about Checkout features and capabilities
- [Installation guide](/dropins/checkout/installation/) - Install and set up the Checkout drop-in

{/* This documentation is auto-generated from: REPO_URL */}
