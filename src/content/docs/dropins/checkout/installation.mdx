---
title: Checkout Installation
description: Package information and requirements for installing the Checkout drop-in.
sidebar:
  label: Installation
  order: 2
---

import { Aside } from '@astrojs/starlight/components';

{/* 
TEMPLATE INSTRUCTIONS:
This simplified template links to the shared installation guide while providing drop-in-specific details.

PLACEHOLDERS:
- Checkout: Display name (for example, "Cart", "Checkout")
- checkout: URL-safe slug (for example, "cart", "checkout")
- @dropins/storefront-checkout: Full package name (for example, "@dropins/storefront-cart")
- 2.1.0: Package version (for example, "1.5.1")
- AddressValidation: Name of a primary container for examples
- git@github.com:adobe-commerce/storefront-checkout: Source repository URL
- The Checkout drop-in component provides a customizable UI for the checkout process. The checkout component is designed to be integrated into your storefront and provides a seamless checkout experience for customers.
: Optional custom introductory content from enrichment
- ## Installation

Since the checkout component relies on containers from several other drop-in components, you must install and configure those components before you can use the checkout component.

The [Commerce boilerplate template](https://github.com/hlxsites/aem-boilerplate-commerce) includes all of the necessary drop-in components and configurations to help you get started quickly, so Adobe recommends relying on the boilerplate instead of installing, configuring, and integrating the drop-in components individually.

## Admin configuration

Before you can use the checkout component on your storefront, you must enable and configure [payment providers](https://experienceleague.adobe.com/en/docs/commerce-admin/stores-sales/payments/payments) and [checkout options](https://experienceleague.adobe.com/en/docs/commerce-admin/stores-sales/point-of-purchase/checkout/checkout-process) in the Adobe Commerce Admin.

:::note
The checkout [overview](/dropins/checkout/) provides a summary of supported Adobe Commerce features.
:::
: Optional custom sections before installation link
- : Optional custom sections after installation link
*/}

The Checkout drop-in component provides a customizable UI for the checkout process. The checkout component is designed to be integrated into your storefront and provides a seamless checkout experience for customers.


<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Installation

Since the checkout component relies on containers from several other drop-in components, you must install and configure those components before you can use the checkout component.

The [Commerce boilerplate template](https://github.com/hlxsites/aem-boilerplate-commerce) includes all of the necessary drop-in components and configurations to help you get started quickly, so Adobe recommends relying on the boilerplate instead of installing, configuring, and integrating the drop-in components individually.

## Admin configuration

Before you can use the checkout component on your storefront, you must enable and configure [payment providers](https://experienceleague.adobe.com/en/docs/commerce-admin/stores-sales/payments/payments) and [checkout options](https://experienceleague.adobe.com/en/docs/commerce-admin/stores-sales/point-of-purchase/checkout/checkout-process) in the Adobe Commerce Admin.

:::note
The checkout [overview](/dropins/checkout/) provides a summary of supported Adobe Commerce features.
:::


## Getting started

The Checkout drop-in is included in the [Commerce boilerplate](https://github.com/hlxsites/aem-boilerplate-commerce)—no installation needed.

**See the [Using drop-ins](/dropins/all/installing/) guide** for complete instructions on importing and using drop-ins in your blocks.

## Package details

Use this information when working with the Checkout drop-in:

- Package name: `@dropins/storefront-checkout`
- Current version: 2.1.0
- Example container: `AddressValidation`
- Initializer: `scripts/initializers/checkout.js`

### Quick example

This example shows the basic pattern for using the Checkout drop-in:

```js
// 1. Import initializer (handles all setup)
import '../../scripts/initializers/checkout.js';

// 2. Import the container you need
import AddressValidation from '@dropins/storefront-checkout/containers/AddressValidation.js';

// 3. Import the provider
import { render as provider } from '@dropins/storefront-checkout/render.js';

// 4. Render in your block
export default async function decorate(block) {
  await provider.render(AddressValidation, {
    // Add configuration options here
  })(block);
}
```



## Next steps

Now that you understand the basics, explore these resources:

- [Using drop-ins](/dropins/all/installing/) - Complete guide with examples.
- [API functions](/dropins/checkout/functions/) - Control drop-in behavior.
- [Slots](/dropins/checkout/slots/) - Customize the UI.
- [Events](/dropins/checkout/events/) - Integrate with other components.

{/* This documentation is auto-generated from: git@github.com:adobe-commerce/storefront-checkout */}

