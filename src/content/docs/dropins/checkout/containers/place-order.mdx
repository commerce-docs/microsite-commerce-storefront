---
title: PlaceOrder Container
description: Learn about the PlaceOrder container in the Checkout drop-in.
sidebar:
  label: PlaceOrder
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The PlaceOrder container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `PlaceOrder` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `disabled` | `boolean` | No | Disables d |
| `active` | `boolean` | No | Configuration for active |
| `handleValidation` | `() => boolean` | No | Callback function for handle validation |
| `handlePlaceOrder` | `(ctx: HandlePlaceOrderContext) => Promise<void>` | Yes | Callback function for handle place order |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ Content?: SlotProps<ContentSlotContext>` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `PlaceOrder` container:

```jsx
import { PlaceOrder } from '@dropins/storefront-checkout';

export default function MyComponent() {
  return (
    <PlaceOrder
      handlePlaceOrder={() => {}}
      disabled={true}
      active={true}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
