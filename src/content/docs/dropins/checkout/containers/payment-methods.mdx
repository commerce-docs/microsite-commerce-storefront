---
title: PaymentMethods Container
description: Learn about the PaymentMethods container in the Checkout drop-in.
sidebar:
  label: PaymentMethods
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The PaymentMethods container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `PaymentMethods` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| No configurations | - | - | - |

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ Methods?: PaymentMethodsSlot` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `PaymentMethods` container:

```jsx
import { PaymentMethods } from '@dropins/storefront-checkout';

export default function MyComponent() {
  return (
    <PaymentMethods/>
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
