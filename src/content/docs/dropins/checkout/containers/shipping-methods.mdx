---
title: ShippingMethods Container
description: Learn about the ShippingMethods container in the Checkout drop-in.
sidebar:
  label: ShippingMethods
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The ShippingMethods container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `ShippingMethods` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `UIComponentType` | `UIComponentType` | No | Configuration for u i component type |
| `active` | `boolean` | No | Configuration for active |
| `autoSync` | `boolean` | No | Configuration for auto sync |
| `onCartSyncError` | `(error: CartSyncError) => void` | No | Callback function triggered when cart sync error |
| `onSelectionChange` | `(method: ShippingMethod) => void` | No | Callback function triggered when selection change |

</TableWrapper>

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `ShippingMethods` container:

```jsx
import { ShippingMethods } from '@dropins/storefront-checkout';

export default function MyComponent() {
  return (
    <ShippingMethods
      UIComponentType={UIComponentType}
      active={true}
      autoSync={true}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
