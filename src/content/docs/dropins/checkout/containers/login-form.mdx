---
title: LoginForm Container
description: Learn about the LoginForm container in the Checkout drop-in.
sidebar:
  label: LoginForm
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The LoginForm container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `LoginForm` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `active` | `boolean` | No | Configuration for active |
| `autoSync` | `boolean` | No | Configuration for auto sync |
| `displayHeadingContent` | `boolean` | No | Configuration for display heading content |
| `onSignInClick` | `(email: string) => void` | No | Callback function triggered when sign in click |
| `onSignOutClick` | `() => void` | No | Callback function triggered when sign out click |
| `onCartSyncError` | `(error: CartSyncError) => void` | No | Callback function triggered when cart sync error |
| `onValidationError` | `(error: ValidationError) => void` | No | Callback function triggered when validation error |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ Heading?: SlotProps<{ authenticated: boolean` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `LoginForm` container:

```jsx
import { LoginForm } from '@dropins/storefront-checkout';

export default function MyComponent() {
  return (
    <LoginForm
      active={true}
      autoSync={true}
      displayHeadingContent={true}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
