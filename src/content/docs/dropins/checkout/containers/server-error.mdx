---
title: ServerError Container
description: Learn about the ServerError container in the Checkout drop-in.
sidebar:
  label: ServerError
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The ServerError container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `ServerError` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `autoScroll` | `boolean` | No | Configuration for auto scroll |
| `onRetry` | `() => void` | No | Callback function triggered when retry |
| `onServerError` | `(error: string) => void` | No | Callback function triggered when server error |
| `active` | `boolean` | No | Configuration for active |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `ServerError` container:

```jsx
import { ServerError } from '@dropins/storefront-checkout';

export default function MyComponent() {
  return (
    <ServerError/>
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
