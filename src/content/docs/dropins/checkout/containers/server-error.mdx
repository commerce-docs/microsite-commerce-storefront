{/*
  Template for Drop-in Container Documentation
  
  This template is used by scripts/@generate-container-docs.js to generate container documentation
  for each container from the source repositories.
  
  Placeholders used in this template:
  - Checkout → Display name (e.g., "Cart", "Checkout")
  - @dropins/storefront-checkout → Package name (e.g., "cart", "checkout")
  - ServerError → Container name (e.g., "MiniCart", "OrderSummary")
  - ServerError → Display name for container (e.g., "Mini Cart")
  - 2.1.0 → Version number (e.g., "1.5.1")
  - The ServerError container component for the drop-in. → Brief description of what the container does
  - | `autoScroll` | `boolean` | No | Configuration for auto scroll |
| `onRetry` | `() => void` | No | Callback function triggered when retry |
| `onServerError` | `(error: string) => void` | No | Callback function triggered when server error |
| `active` | `boolean` | No | Configuration for active | → Generated markdown table of all Props
  - This container does not currently expose any customizable slots. → Generated slots sections with examples
  - ```jsx
import { ServerError } from '@dropins/storefront-checkout';

export default function MyComponent() {
  return (
    <ServerError/>
  );
}
``` → Basic usage example with code
  - https://github.com/adobe-commerce/storefront-checkout → GitHub repository URL
  
  The script handles:
  - Extracting container Props from TypeScript interfaces
  - Generating configuration tables with types and descriptions
  - Extracting and documenting all slots
  - Creating basic usage examples
  
  The template and script must be kept in sync.
*/}

---
title: Checkout - ServerError Container
description: Learn about the ServerError container in the Checkout drop-in.
sidebar:
  label: ServerError
  order: auto
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The ServerError container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `ServerError` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `autoScroll` | `boolean` | No | Configuration for auto scroll |
| `onRetry` | `() => void` | No | Callback function triggered when retry |
| `onServerError` | `(error: string) => void` | No | Callback function triggered when server error |
| `active` | `boolean` | No | Configuration for active |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `ServerError` container:

```jsx
import { ServerError } from '@dropins/storefront-checkout';

export default function MyComponent() {
  return (
    <ServerError/>
  );
}
```

{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-checkout */}
