---
title: EstimateShipping container
description: Configure the EstimateShipping container to display shipping costs during checkout.
---

import OptionsTable from '@components/OptionsTable.astro';

The `EstimateShipping` container is designed to estimate and display shipping costs during the checkout process. You can configure it to handle various scenarios, such as displaying free shipping and handling different tax display settings.

## EstimateShipping configurations

The `EstimateShipping` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['estimated', 'boolean', 'No', 'A boolean indicating whether the shipping cost is estimated.'],
    ['price', 'VNode', 'Yes', 'The rendered price component, which can be free shipping, the price including tax, or the price excluding tax.'],
    ['priceExclTax', 'VNode', 'No', 'The rendered price component excluding tax, if applicable.'],
    ['taxExcluded', 'boolean', 'No', 'A boolean indicating whether the tax is excluded from the shipping price.'],
    ['taxIncluded', 'boolean', 'No', 'A boolean indicating whether the tax is included in the shipping price.'],
  ]}
/>