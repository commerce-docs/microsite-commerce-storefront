---
title: MergedCartBanner Container
description: Learn about the MergedCartBanner container in the Checkout drop-in.
sidebar:
  label: MergedCartBanner
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The MergedCartBanner container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `MergedCartBanner` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `active` | `boolean` | No | Configuration for active |

</TableWrapper>

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `MergedCartBanner` container:

```jsx
import { MergedCartBanner } from '@dropins/storefront-checkout';

export default function MyComponent() {
  return (
    <MergedCartBanner
      active={true}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
