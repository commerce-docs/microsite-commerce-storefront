---
title: BillToShippingAddress Container
description: Learn about the BillToShippingAddress container in the Checkout drop-in.
sidebar:
  label: BillToShippingAddress
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The BillToShippingAddress container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `BillToShippingAddress` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `active` | `boolean` | No | Configuration for active |
| `autoSync` | `boolean` | No | Configuration for auto sync |
| `onCartSyncError` | `(error: CartSyncError) => void` | No | Callback function triggered when cart sync error |
| `onChange` | `(checked: boolean) => void` | No | Callback function triggered when change |

</TableWrapper>

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `BillToShippingAddress` container:

```jsx
import { BillToShippingAddress } from '@dropins/storefront-checkout';

export default function MyComponent() {
  return (
    <BillToShippingAddress
      active={true}
      autoSync={true}
      onCartSyncError={() => console.log('CartSyncError')}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
