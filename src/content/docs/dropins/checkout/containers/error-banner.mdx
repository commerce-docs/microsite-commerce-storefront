---
title: ErrorBanner container
description: Customize the ErrorBanner container to display error messages with various configurations.
---

import OptionsTable from '@components/OptionsTable.astro';

The `ErrorBanner` container is designed to display error messages to the user, such as network errors, validation errors, or other issues that prevent the user from completing their purchase. You can customize the appearance of the banner, the error message, and the behavior of the banner (for example, whether it can be dismissed).

## ErrorBanner configurations

The `ErrorBanner` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['className', 'string', 'No', 'CSS class to apply to the container.'],
    ['data-testid', 'string', 'No', 'Identifier for the banner for testing purposes.'],
    ['icon', 'JSX.Element', 'No', 'Icon to display in the banner.'],
    ['message', 'string', 'Yes', 'Message to display in the banner.'],
    ['aria-label', 'string', 'No', 'Accessibility label for the banner.'],
    ['onDismiss', 'function', 'No', 'Function to call when the banner is dismissed.'],
    ['variant', 'string', 'No', 'Variant of the banner indicating the type of alert.'],
  ]}
/>