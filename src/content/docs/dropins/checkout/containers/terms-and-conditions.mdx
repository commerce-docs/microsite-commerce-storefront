---
title: TermsAndConditions Container
description: Learn about the TermsAndConditions container in the Checkout drop-in.
sidebar:
  label: TermsAndConditions
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The TermsAndConditions container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `TermsAndConditions` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `active` | `boolean` | No | Configuration for active |
| `mode` | `AgreementMode` | Yes | Configuration for mode |
| `translationId` | `string` | No | Unique identifier for translation |
| `text` | `string` | No | Configuration for text |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ Agreements?: SlotProps<{ appendAgreement: SlotMethod<{ name: string` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `TermsAndConditions` container:

```jsx
import { TermsAndConditions } from '@dropins/storefront-checkout';

export default function MyComponent() {
  return (
    <TermsAndConditions
      mode={mode}
      active={true}
      translationId="abc-123"
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
