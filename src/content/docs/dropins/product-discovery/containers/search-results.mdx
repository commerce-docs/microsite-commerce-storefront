---
title: SearchResults Container
description: Learn about the SearchResults container in the Product Discovery drop-in.
sidebar:
  label: SearchResults
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The SearchResults container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `SearchResults` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `routeProduct` | `(product: Product) => string` | No | Callback function for route product |
| `scope` | `Scope` | No | Configuration for scope |
| `imageWidth` | `number` | No | imageWidth in pixels |
| `imageHeight` | `number` | No | imageHeight in pixels |
| `skeletonCount` | `number` | No | Number of skeleton |
| `onSearchResult` | `(payload: Product\[\]) => void` | No | Callback function triggered when search result |
| `ProductPrice` | `SlotProps<SlotDefaultContext>` | No | Configuration for product price |
| `ProductName` | `SlotProps<SlotDefaultContext>` | No | Configuration for product name |
| `ProductImage` | `SlotProps<SlotDefaultContext & { defaultImageProps: ImageProps }>` | No | Configuration for product image |
| `NoResults` | `SlotProps<{ error: string \| null` | No | Configuration for no results |
| `variables` | `SearchVariables \| null }>` | Yes | Configuration for variables |
| `Header` | `SlotProps<{ products: Product\[\]` | No | Configuration for header |
| `variables` | `SearchVariables \| null }>` | Yes | Configuration for variables |
| `Footer` | `SlotProps<{ products: Product\[\]` | No | Configuration for footer |
| `variables` | `SearchVariables \| null }>` | Yes | Configuration for variables |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ ProductActions?: SlotProps<SlotDefaultContext>` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `SearchResults` container:

```jsx
import { SearchResults } from '@dropins/storefront-product-discovery';

export default function MyComponent() {
  return (
    <SearchResults
      variables={variables}
      variables={variables}
      variables={variables}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
