---
title: SortBy Container
description: Learn about the SortBy container in the Product Discovery drop-in.
sidebar:
  label: SortBy
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The SortBy container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `SortBy` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `scope` | `Scope` | No | Configuration for scope |
| `onSortChange` | `(value: SortOrder \| null) => void` | No | Callback function triggered when sort change |

</TableWrapper>

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `SortBy` container:

```jsx
import { SortBy } from '@dropins/storefront-product-discovery';

export default function MyComponent() {
  return (
    <SortBy
      scope={scope}
      onSortChange={onSortChange}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
