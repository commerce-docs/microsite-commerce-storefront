---
title: ResultsInfo Container
description: Learn about the ResultsInfo container and configurations for the Product Discovery drop-in.
---

import OptionsTable from '@components/OptionsTable.astro';

The `ResultsInfo` container displays the search phrase or category path, and the count of matching products based on the results of the search query.

## Configurations

The `ResultsInfo` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['slots', 'function', 'No', 'Allows passing a container or custom component.'],
  ]}
/>

The `ProductList` container supports the following slots:

* SearchPhrase
* TotalCount