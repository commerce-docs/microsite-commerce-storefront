---
title: Facets Container
description: Learn about the Facets container and configurations for the Product Discovery drop-in.
---

import OptionsTable from '@components/OptionsTable.astro';

The `Facets` container displays a list of `Facet` components, as well as the list of selected facets to render the filters applicable to the results of the search query.

## Configurations

The `Facets` container provides the following configuration options:

<OptionsTable
  compact
  options={[
    ['Option', 'Type', 'Req?', 'Description'],
    ['slots', 'function', 'No', 'Allows passing a container or custom component.'],
  ]}
/>

The `ProductList` container supports the following slots:

* Facets
* Facet
* SelectedFacets