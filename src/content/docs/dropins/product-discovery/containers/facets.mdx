---
title: Facets Container
description: Learn about the Facets container in the Product Discovery drop-in.
sidebar:
  label: Facets
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The Facets container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>

## Configuration

The `Facets` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `scope` | `Scope` | No | Configuration for scope |
| `SelectedFacets` | `SlotProps<{ data: SearchFacet\[\] }>` | No | Configuration for selected facets |
| `Facets` | `SlotProps<{ data: SearchFacet\[\] }>` | No | Configuration for facets |
| `FacetBucket` | `SlotProps<{ data: FacetBucket }>` | No | Configuration for facet bucket |
| `FacetBucketLabel` | `SlotProps<{ data: FacetBucket }>` | No | Configuration for facet bucket label |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ Facet?: SlotProps<{ data: SearchFacet }>` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `Facets` container:

```jsx
import { Facets } from '@dropins/storefront-product-discovery';

export default function MyComponent() {
  return (
    <Facets
      scope={scope}
      SelectedFacets={[]}
      Facets={[]}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
