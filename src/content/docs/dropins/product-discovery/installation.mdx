---
title: Product Discovery Installation
description: Package information and requirements for installing the Product Discovery drop-in.
sidebar:
  label: Installation
  order: 2
---

import { Aside } from '@astrojs/starlight/components';

{/* 
TEMPLATE INSTRUCTIONS:
This simplified template links to the shared installation guide while providing drop-in-specific details.

PLACEHOLDERS:
- Product Discovery: Display name (for example, "Cart", "Checkout")
- product-discovery: URL-safe slug (for example, "cart", "checkout")
- @dropins/storefront-product-discovery: Full package name (for example, "@dropins/storefront-cart")
- 2.1.0: Package version (for example, "1.5.1")
- Facets: Name of a primary container for examples
- git@github.com:adobe-commerce/storefront-search-dropin: Source repository URL
- : Optional custom introductory content from enrichment
- : Optional custom sections before installation link
- : Optional custom sections after installation link
*/}



<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.0</strong>
</div>



## Getting started

The Product Discovery drop-in is included in the [Commerce boilerplate](https://github.com/hlxsites/aem-boilerplate-commerce)—no installation needed.

**See the [Using drop-ins](/dropins/all/installing/) guide** for complete instructions on importing and using drop-ins in your blocks.

## Package details

Use this information when working with the Product Discovery drop-in:

- Package name: `@dropins/storefront-product-discovery`
- Current version: 2.1.0
- Example container: `Facets`
- Initializer: `scripts/initializers/search.js`

### Quick example

This example shows the basic pattern for using the Product Discovery drop-in:

```js
// 1. Import initializer (handles all setup)
import '../../scripts/initializers/search.js';

// 2. Import the container you need
import Facets from '@dropins/storefront-product-discovery/containers/Facets.js';

// 3. Import the provider
import { render as provider } from '@dropins/storefront-product-discovery/render.js';

// 4. Render in your block
export default async function decorate(block) {
  await provider.render(Facets, {
    // Add configuration options here
  })(block);
}
```



## Next steps

Now that you understand the basics, explore these resources:

- [Using drop-ins](/dropins/all/installing/) - Complete guide with examples.
- [API functions](/dropins/product-discovery/functions/) - Control drop-in behavior.
- [Slots](/dropins/product-discovery/slots/) - Customize the UI.
- [Events](/dropins/product-discovery/events/) - Integrate with other components.

{/* This documentation is auto-generated from: git@github.com:adobe-commerce/storefront-search-dropin */}

