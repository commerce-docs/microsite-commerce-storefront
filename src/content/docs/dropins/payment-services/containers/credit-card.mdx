{/*
  Template for Drop-in Container Documentation
  
  This template is used by scripts/@generate-container-docs.js to generate container documentation
  for each container from the source repositories.
  
  Placeholders used in this template:
  - Payment Services → Display name (e.g., "Cart", "Checkout")
  - @dropins/storefront-payment-services → Package name (e.g., "cart", "checkout")
  - CreditCard → Container name (e.g., "MiniCart", "OrderSummary")
  - CreditCard → Display name for container (e.g., "Mini Cart")
  - 1.0.2 → Version number (e.g., "1.5.1")
  - The CreditCard container component for the drop-in. → Brief description of what the container does
  - | `https` | `//example.com/graphql". \*/ apiUrl: string` | Yes | Configuration for https |
| `getCartId` | `() => Promise<string>` | Yes | Should return a promise that resolves to the shopper`s cart ID. |
| `see` | `https://developer.adobe.com/commerce/webapi/graphql/usage/authorization-tokens/. \*/ getCustomerToken?: (() => string \| null) \| null` | Yes | Configuration for see |
| `current` | `null` | Yes | Configuration for current | → Generated markdown table of all Props
  - This container does not currently expose any customizable slots. → Generated slots sections with examples
  - ```jsx
import { CreditCard } from '@dropins/storefront-payment-services';

export default function MyComponent() {
  return (
    <CreditCard
      https="example"
      getCartId="example"
      see="example"
    />
  );
}
``` → Basic usage example with code
  - https://github.com/adobe-commerce/storefront-payment-services → GitHub repository URL
  
  The script handles:
  - Extracting container Props from TypeScript interfaces
  - Generating configuration tables with types and descriptions
  - Extracting and documenting all slots
  - Creating basic usage examples
  
  The template and script must be kept in sync.
*/}

---
title: Payment Services - CreditCard Container
description: Learn about the CreditCard container in the Payment Services drop-in.
sidebar:
  label: CreditCard
  order: auto
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The CreditCard container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.0.2</strong>
</div>

## Configuration

The `CreditCard` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `https` | `//example.com/graphql". \*/ apiUrl: string` | Yes | Configuration for https |
| `getCartId` | `() => Promise<string>` | Yes | Should return a promise that resolves to the shopper`s cart ID. |
| `see` | `https://developer.adobe.com/commerce/webapi/graphql/usage/authorization-tokens/. \*/ getCustomerToken?: (() => string \| null) \| null` | Yes | Configuration for see |
| `current` | `null` | Yes | Configuration for current |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `CreditCard` container:

```jsx
import { CreditCard } from '@dropins/storefront-payment-services';

export default function MyComponent() {
  return (
    <CreditCard
      https="example"
      getCartId="example"
      see="example"
    />
  );
}
```

{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-payment-services */}
