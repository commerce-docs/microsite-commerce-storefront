---
title: CreditCard Container
description: Learn about the CreditCard container in the Payment Services drop-in.
sidebar:
  label: CreditCard
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

... See 'CardType' from @adobe-commerce/payment-services-sdk/payment.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.0.3</strong>
</div>

## Configuration

The `CreditCard` container provides the following configuration options:

{/* 
  IMPORTANT: Do not wrap <TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `apiUrl` | `string` | Yes | The URL to the Adobe Commerce GraphQL endpoint, such as "https://example.com/graphql". |
| `getCartId` | `() => Promise<string>` | Yes | Should return a promise that resolves to the shopper\`s cart ID. |
| `getCustomerToken` | `(() => string \| null) \| null` | No | Should return a promise that resolves to the shopper\`s cart ID. |
| `creditCardFormRef` | `RefObject<CreditCardFormRef>` | Yes | Credit card form reference. Initially, \{ current: null \} should be passed. Once rendered, the credit card container will set the 'current' property to a \{ validate: () =&gt; boolean; submit: () =&gt; Promise&lt;void&gt; \} object, which parent containers should use to (programmatically) validate and submit the credit card form. |
| `onSuccess` | `() => void` | Yes | Called when payment flow is successful. |
| `onError` | `(error: Error) => void` | Yes | Called when payment flow was aborted due to an error. |

</TableWrapper> in TableWrapper or add a table header.
  The generator's generatePropertyTable() function already includes both.
  Adding them here will cause nested TableWrapper components and duplicate headers.
*/}
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `apiUrl` | `string` | Yes | The URL to the Adobe Commerce GraphQL endpoint, such as "https://example.com/graphql". |
| `getCartId` | `() => Promise<string>` | Yes | Should return a promise that resolves to the shopper\`s cart ID. |
| `getCustomerToken` | `(() => string \| null) \| null` | No | Should return a promise that resolves to the shopper\`s cart ID. |
| `creditCardFormRef` | `RefObject<CreditCardFormRef>` | Yes | Credit card form reference. Initially, \{ current: null \} should be passed. Once rendered, the credit card container will set the 'current' property to a \{ validate: () =&gt; boolean; submit: () =&gt; Promise&lt;void&gt; \} object, which parent containers should use to (programmatically) validate and submit the credit card form. |
| `onSuccess` | `() => void` | Yes | Called when payment flow is successful. |
| `onError` | `(error: Error) => void` | Yes | Called when payment flow was aborted due to an error. |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `CreditCard` container:

```js
import { render as provider } from '@dropins/storefront-payment-services/render.js';
import { CreditCard } from '@dropins/storefront-payment-services/containers/CreditCard.js';

await provider.render(CreditCard, {
  apiUrl: "https://example.com",
  getCartId: () => {},
  creditCardFormRef: {},
})(block);
```



{/* This documentation is auto-generated from: git@github.com:adobe-commerce/storefront-payment-services */}
