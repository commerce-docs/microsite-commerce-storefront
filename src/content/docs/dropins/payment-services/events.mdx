---
title: Payment Services Data & Events
description: Learn about the events used by the Payment Services and the data available within the events.
sidebar:
  label: Events
  order: 5
---

import { Aside } from '@astrojs/starlight/components';

The **Payment Services** drop-in uses the [Event Bus](/sdk/reference/events/) for communication between drop-ins and external integrations.

## Events

This drop-in does not emit or listen to any drop-in-specific events. This drop-in focuses on UI presentation and data display, relying on function calls rather than event-driven communication for its core functionality. It uses only common events for standard cross-component functionality like localization and error handling.

For information about common events like `locale`, `error`, and `authenticated`, see the [Common Events Reference](/sdk/reference/common-events/).
