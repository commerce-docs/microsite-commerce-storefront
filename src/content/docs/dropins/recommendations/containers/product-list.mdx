---
title: ProductList Container
description: Learn about the ProductList container in the Recommendations drop-in.
sidebar:
  label: ProductList
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The ProductList container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.1.1</strong>
</div>

## Configuration

The `ProductList` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| No configurations | - | - | - |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `ProductList` container:

```jsx
import { ProductList } from '@dropins/storefront-recommendations';

export default function MyComponent() {
  return (
    <ProductList/>
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
