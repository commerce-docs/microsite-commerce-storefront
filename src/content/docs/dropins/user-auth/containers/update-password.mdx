---
title: UpdatePassword Container
description: Learn about the UpdatePassword container in the User Auth drop-in.
sidebar:
  label: UpdatePassword
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The UpdatePassword container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.5</strong>
</div>

## Configuration

The `UpdatePassword` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| No configurations | - | - | - |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `UpdatePassword` container:

```jsx
import { UpdatePassword } from '@dropins/storefront-auth';

export default function MyComponent() {
  return (
    <UpdatePassword/>
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
