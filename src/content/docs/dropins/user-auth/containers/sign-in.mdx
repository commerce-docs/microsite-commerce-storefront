---
title: SignIn Container
description: Learn about the SignIn container in the User Auth drop-in.
sidebar:
  label: SignIn
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The SignIn container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.1.1</strong>
</div>

## Configuration

The `SignIn` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| No configurations | - | - | - |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `SignIn` container:

```jsx
import { SignIn } from '@dropins/storefront-auth';

export default function MyComponent() {
  return (
    <SignIn/>
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
