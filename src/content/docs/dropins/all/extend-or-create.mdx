---
title: Extend or create?
description: Guidelines for when NOT to create a new drop-in, with alternatives like extending existing drop-ins or using simpler customization solutions.
sidebar:
  label: Extend or create a drop-in
---

import Vocabulary from '@components/Vocabulary.astro';
import Diagram from '@components/Diagram.astro';
import Aside from '@components/Aside.astro';
import Callouts from '@components/Callouts.astro';
import { Steps } from '@astrojs/starlight/components';
import Tasks from '@components/Tasks.astro';
import Task from '@components/Task.astro';
import { Tabs, TabItem } from '@astrojs/starlight/components';

Start with the simplest solution first. Most customizations can be achieved with basic styling, slots, or configuration changes. If those aren't enough, try [extending existing drop-ins](/dropins/all/extending/). Only create a new drop-in as a last resort. This guide shows you when NOT to create new drop-ins and what to try instead.

## Vocabulary

<Vocabulary>

### Extending a drop-in

Adding functionality to a drop-in using existing extensibility mechanisms like slots, configuration, styling, and events.

### Building a new drop-in

Creating a new drop-in from scratch using the [drop-in template](https://github.com/adobe-commerce/dropin-template) and SDK.

### Slots

Declarative anchors where custom UI content can be injected into a drop-in.

</Vocabulary>

## Do NOT build a drop-in for these scenarios

| Scenario | Why not? | Use instead |
|----------|------------------------------|-------------|
| **Third-party components** | They come with pre-built UI and JavaScript | Use the vendor's provided scripts (e.g., BV ratings) |
| **Simple content additions** | Overkill for basic content placement | HTML/CSS or slots |
| **Styling and UI changes** | Existing drop-ins can be customized extensively | Modify layouts, replace components, or use slots |
| **Merchant-specific logic** | Not reusable across different stores | Application-level conditional logic or configuration |
| **Experimental A/B variants** | Temporary and not yet proven for reuse | Feature flags or temporary code branches |

## Need a new extension point?

If existing drop-ins don't provide the slots or events you need:

<Steps>
1. **Document your use case** - Explain what you're trying to achieve
1. **Identify the gap** - What specific slot or event is missing?
1. **Submit a request** - Open an issue with your requirements
</Steps>

**TODO**: Add link to request a new slot or event

## FAQs

**Q: Are slots the *only* extensibility mechanism?**  
No. While slots are the primary mechanism, you can also:
- Augment GraphQL with overrides
- Modify transformers to change how a drop-in uses data
- Listen to data events and customize behavior within or outside of slots

If these options don't meet your needs, you can request additional extensibility options through a formal feature request. 

**TODO**: Add a link or instructions for submitting a formal feature request.

**Q: Can a third-party developer fully replace an out-of-the-box (OOTB) drop-in?**  
You *can* build your UI against the APIs directly rather than using drop-ins. However, you will have to re-implement all logic/functionality currently provided by an OOTB drop-in. We don't recommend this approach.
