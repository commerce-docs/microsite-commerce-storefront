---
title: Cart Installation
description: Package information and requirements for installing the Cart drop-in.
sidebar:
  label: Installation
  order: 2
---

import { Aside } from '@astrojs/starlight/components';

{/* 
TEMPLATE INSTRUCTIONS:
This simplified template links to the shared installation guide while providing drop-in-specific details.

PLACEHOLDERS:
- Cart: Display name (for example, "Cart", "Checkout")
- cart: URL-safe slug (for example, "cart", "checkout")
- @dropins/storefront-cart: Full package name (for example, "@dropins/storefront-cart")
- 1.5.1: Package version (for example, "1.5.1")
- CartSummaryGrid: Name of a primary container for examples
- git@github.com:adobe-commerce/storefront-cart: Source repository URL
- The Cart drop-in is one of the most commonly used components in the Commerce boilerplate. It provides a complete shopping cart experience with features like product management, coupon codes, gift cards, and shipping estimates.
: Optional custom introductory content from enrichment
- : Optional custom sections before installation link
- : Optional custom sections after installation link
*/}

The Cart drop-in is one of the most commonly used components in the Commerce boilerplate. It provides a complete shopping cart experience with features like product management, coupon codes, gift cards, and shipping estimates.


<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.5.1</strong>
</div>



## Getting started

The Cart drop-in is included in the [Commerce boilerplate](https://github.com/hlxsites/aem-boilerplate-commerce)—no installation needed.

**See the [Using drop-ins](/dropins/all/installing/) guide** for complete instructions on importing and using drop-ins in your blocks.

## Package details

Use this information when working with the Cart drop-in:

- Package name: `@dropins/storefront-cart`
- Current version: 1.5.1
- Example container: `CartSummaryGrid`
- Initializer: `scripts/initializers/cart.js`

### Quick example

This example shows the basic pattern for using the Cart drop-in:

```js
// 1. Import initializer (handles all setup)
import '../../scripts/initializers/cart.js';

// 2. Import the container you need
import CartSummaryGrid from '@dropins/storefront-cart/containers/CartSummaryGrid.js';

// 3. Import the provider
import { render as provider } from '@dropins/storefront-cart/render.js';

// 4. Render in your block
export default async function decorate(block) {
  await provider.render(CartSummaryGrid, {
    // Add configuration options here
  })(block);
}
```



## Next steps

Now that you understand the basics, explore these resources:

- [Using drop-ins](/dropins/all/installing/) - Complete guide with examples.
- [API functions](/dropins/cart/functions/) - Control drop-in behavior.
- [Slots](/dropins/cart/slots/) - Customize the UI.
- [Events](/dropins/cart/events/) - Integrate with other components.

{/* This documentation is auto-generated from: git@github.com:adobe-commerce/storefront-cart */}

