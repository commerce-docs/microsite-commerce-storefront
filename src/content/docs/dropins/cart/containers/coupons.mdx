---
title: Coupons Container
description: Learn about the Coupons container in the Cart drop-in.
sidebar:
  label: Coupons
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The Coupons container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.5.1</strong>
</div>

## Configuration

The `Coupons` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `Coupons` | `Container<CouponsProps> = ({ children` | Yes | Configuration for coupons |
| `applyButton` | `'Cart.PriceSummary.coupon.applyAction'` | Yes | Configuration for apply button |
| `placeholder` | `'Cart.PriceSummary.coupon.placeholder'` | Yes | Configuration for placeholder |
| `ariaLabelRemove` | `'Cart.PriceSummary.coupon.ariaLabelRemove'` | Yes | Configuration for aria label remove |

</TableWrapper>

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `Coupons` container:

```jsx
import { Coupons } from '@dropins/storefront-cart';

export default function MyComponent() {
  return (
    <Coupons
      Coupons={{}}
      applyButton={applyButton}
      placeholder={placeholder}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
