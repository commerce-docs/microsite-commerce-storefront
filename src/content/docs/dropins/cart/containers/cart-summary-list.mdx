---
title: CartSummaryList Container
description: Learn about the CartSummaryList container in the Cart drop-in.
sidebar:
  label: CartSummaryList
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The CartSummaryList container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.5.1</strong>
</div>

## Configuration

The `CartSummaryList` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `hideHeading` | `boolean` | No | Controls hiding of heading |
| `hideFooter` | `boolean` | No | Controls hiding of footer |
| `routeProduct` | `(item: CartModel\['items'\]\[0\]) => string` | No | Callback function for route product |
| `routeEmptyCartCTA` | `() => string` | No | Callback function for route empty cart c t a |
| `routeCart` | `() => string` | No | Callback function for route cart |
| `onItemUpdate` | `({ item }: { item: CartModel\['items'\]\[0\] }) => void` | No | Callback function triggered when item update |
| `onItemRemove` | `({ item }: { item: CartModel\['items'\]\[0\] }) => void` | No | Callback function triggered when item remove |
| `maxItems` | `number` | No | Array of items for max |
| `showMaxItems` | `boolean` | No | Controls visibility of max items |
| `attributesToHide` | `SwitchableAttributes\[\]` | No | Configuration for attributes to hide |
| `EmptyCart` | `SlotProps` | No | Configuration for empty cart |
| `Footer` | `SlotProps` | No | Configuration for footer |
| `Thumbnail` | `SlotProps<{ item: CartModel\['items'\]\[number\]` | No | Configuration for thumbnail |
| `defaultImageProps` | `ImageProps` | Yes | Configuration for default image props |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ Heading?: SlotProps` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `CartSummaryList` container:

```jsx
import { CartSummaryList } from '@dropins/storefront-cart';

export default function MyComponent() {
  return (
    <CartSummaryList
      defaultImageProps={defaultImageProps}
      hideHeading={true}
      hideFooter={true}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
