---
title: MiniCart Container
description: Learn about the MiniCart container in the Cart drop-in.
sidebar:
  label: MiniCart
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The MiniCart container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.5.1</strong>
</div>

## Configuration

The `MiniCart` container provides the following configuration options:

{/* 
  IMPORTANT: Do not wrap <TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `routeProduct` | `(item: CartModel['items'][0]) => string` | No | Callback function for route product |
| `routeCart` | `() => string` | No | Callback function for route cart |
| `routeCheckout` | `() => string` | No | Callback function for route checkout |
| `routeEmptyCartCTA` | `() => string` | No | Callback function for route empty cart c t a |
| `ProductListFooter` | `SlotProps` | No | Configuration for product list footer |
| `PreCheckoutSection` | `SlotProps` | No | Configuration for pre checkout section |
| `Thumbnail` | `SlotProps<{ item: CartModel['items'][number]` | No | Configuration for thumbnail |
| `defaultImageProps` | `ImageProps` | Yes | Configuration for default image props |
| `Heading` | `SlotProps` | No | Configuration for heading |
| `EmptyCart` | `SlotProps` | No | Configuration for empty cart |
| `Footer` | `SlotProps` | No | Configuration for footer |
| `ProductAttributes` | `SlotProps` | No | Configuration for product attributes |
| `CartSummaryFooter` | `SlotProps` | No | Configuration for cart summary footer |
| `CartItem` | `SlotProps` | No | Configuration for cart item |
| `UndoBanner` | `SlotProps<{ item: CartModel['items'][0]` | No | Configuration for undo banner |
| `loading` | `boolean` | Yes | Configuration for loading |
| `error` | `string` | No | Configuration for error |
| `onUndo` | `() => void` | Yes | Callback function triggered when undo |
| `onDismiss` | `() => void` | Yes | Callback function triggered when dismiss |
| `ItemTitle` | `SlotProps<{ item: CartModel['items'][number] }>` | No | Configuration for item title |
| `ItemPrice` | `SlotProps<{ item: CartModel['items'][number] }>` | No | Configuration for item price |
| `ItemQuantity` | `SlotProps<{ item: CartModel['items'][number]` | No | Configuration for item quantity |
| `enableUpdateItemQuantity` | `boolean` | Yes | Enables or disables update item quantity |
| `handleItemQuantityUpdate` | `( item: CartModel['items'][number]` | Yes | Configuration for handle item quantity update |
| `quantity` | `number ) => void` | Yes | Callback function for quantity |
| `itemsLoading` | `Set<string>` | Yes | Configuration for items loading |
| `handleItemsError` | `(uid: string` | Yes | Configuration for handle items error |
| `message` | `string) => void` | No | Callback function for message |
| `handleItemsLoading` | `(uid: string` | Yes | Configuration for handle items loading |
| `state` | `boolean) => void` | Yes | Callback function for state |
| `onItemUpdate` | `({ item }: { item: CartModel['items'][number] }) => void` | No | Callback function triggered when item update |
| `ItemTotal` | `SlotProps<{ item: CartModel['items'][number] }>` | No | Configuration for item total |
| `ItemSku` | `SlotProps<{ item: CartModel['items'][number] }>` | No | Configuration for item sku |
| `ItemRemoveAction` | `SlotProps<{ item: CartModel['items'][number]` | No | Configuration for item remove action |
| `enableRemoveItem` | `boolean` | Yes | Enables or disables remove item |
| `handleItemQuantityUpdate` | `( item: CartModel['items'][number]` | Yes | Configuration for handle item quantity update |
| `quantity` | `number ) => void` | Yes | Callback function for quantity |
| `handleItemsError` | `(uid: string` | Yes | Configuration for handle items error |
| `message` | `string) => void` | No | Callback function for message |
| `handleItemsLoading` | `(uid: string` | Yes | Configuration for handle items loading |
| `state` | `boolean) => void` | Yes | Callback function for state |
| `onItemUpdate` | `({ item }: { item: CartModel['items'][number] }) => void` | No | Callback function triggered when item update |
| `itemsLoading` | `Set<string>` | Yes | Configuration for items loading |
| `hideFooter` | `boolean` | No | Controls hiding of footer |
| `displayAllItems` | `boolean` | No | Array of items for display all |
| `showDiscount` | `boolean` | No | Controls visibility of discount |
| `showSavings` | `boolean` | No | Controls visibility of savings |
| `enableItemRemoval` | `boolean` | No | Enables or disables item removal |
| `enableQuantityUpdate` | `boolean` | No | Enables or disables quantity update |
| `hideHeading` | `boolean` | No | Controls hiding of heading |
| `undo` | `boolean` | No | Configuration for undo |

</TableWrapper> in TableWrapper or add a table header.
  The generator's generatePropertyTable() function already includes both.
  Adding them here will cause nested TableWrapper components and duplicate headers.
*/}
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `routeProduct` | `(item: CartModel['items'][0]) => string` | No | Callback function for route product |
| `routeCart` | `() => string` | No | Callback function for route cart |
| `routeCheckout` | `() => string` | No | Callback function for route checkout |
| `routeEmptyCartCTA` | `() => string` | No | Callback function for route empty cart c t a |
| `ProductListFooter` | `SlotProps` | No | Configuration for product list footer |
| `PreCheckoutSection` | `SlotProps` | No | Configuration for pre checkout section |
| `Thumbnail` | `SlotProps<{ item: CartModel['items'][number]` | No | Configuration for thumbnail |
| `defaultImageProps` | `ImageProps` | Yes | Configuration for default image props |
| `Heading` | `SlotProps` | No | Configuration for heading |
| `EmptyCart` | `SlotProps` | No | Configuration for empty cart |
| `Footer` | `SlotProps` | No | Configuration for footer |
| `ProductAttributes` | `SlotProps` | No | Configuration for product attributes |
| `CartSummaryFooter` | `SlotProps` | No | Configuration for cart summary footer |
| `CartItem` | `SlotProps` | No | Configuration for cart item |
| `UndoBanner` | `SlotProps<{ item: CartModel['items'][0]` | No | Configuration for undo banner |
| `loading` | `boolean` | Yes | Configuration for loading |
| `error` | `string` | No | Configuration for error |
| `onUndo` | `() => void` | Yes | Callback function triggered when undo |
| `onDismiss` | `() => void` | Yes | Callback function triggered when dismiss |
| `ItemTitle` | `SlotProps<{ item: CartModel['items'][number] }>` | No | Configuration for item title |
| `ItemPrice` | `SlotProps<{ item: CartModel['items'][number] }>` | No | Configuration for item price |
| `ItemQuantity` | `SlotProps<{ item: CartModel['items'][number]` | No | Configuration for item quantity |
| `enableUpdateItemQuantity` | `boolean` | Yes | Enables or disables update item quantity |
| `handleItemQuantityUpdate` | `( item: CartModel['items'][number]` | Yes | Configuration for handle item quantity update |
| `quantity` | `number ) => void` | Yes | Callback function for quantity |
| `itemsLoading` | `Set<string>` | Yes | Configuration for items loading |
| `handleItemsError` | `(uid: string` | Yes | Configuration for handle items error |
| `message` | `string) => void` | No | Callback function for message |
| `handleItemsLoading` | `(uid: string` | Yes | Configuration for handle items loading |
| `state` | `boolean) => void` | Yes | Callback function for state |
| `onItemUpdate` | `({ item }: { item: CartModel['items'][number] }) => void` | No | Callback function triggered when item update |
| `ItemTotal` | `SlotProps<{ item: CartModel['items'][number] }>` | No | Configuration for item total |
| `ItemSku` | `SlotProps<{ item: CartModel['items'][number] }>` | No | Configuration for item sku |
| `ItemRemoveAction` | `SlotProps<{ item: CartModel['items'][number]` | No | Configuration for item remove action |
| `enableRemoveItem` | `boolean` | Yes | Enables or disables remove item |
| `handleItemQuantityUpdate` | `( item: CartModel['items'][number]` | Yes | Configuration for handle item quantity update |
| `quantity` | `number ) => void` | Yes | Callback function for quantity |
| `handleItemsError` | `(uid: string` | Yes | Configuration for handle items error |
| `message` | `string) => void` | No | Callback function for message |
| `handleItemsLoading` | `(uid: string` | Yes | Configuration for handle items loading |
| `state` | `boolean) => void` | Yes | Callback function for state |
| `onItemUpdate` | `({ item }: { item: CartModel['items'][number] }) => void` | No | Callback function triggered when item update |
| `itemsLoading` | `Set<string>` | Yes | Configuration for items loading |
| `hideFooter` | `boolean` | No | Controls hiding of footer |
| `displayAllItems` | `boolean` | No | Array of items for display all |
| `showDiscount` | `boolean` | No | Controls visibility of discount |
| `showSavings` | `boolean` | No | Controls visibility of savings |
| `enableItemRemoval` | `boolean` | No | Enables or disables item removal |
| `enableQuantityUpdate` | `boolean` | No | Enables or disables quantity update |
| `hideHeading` | `boolean` | No | Controls hiding of heading |
| `undo` | `boolean` | No | Configuration for undo |

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `\{ ProductList?: SlotProps` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `MiniCart` container:

```jsx
import { MiniCart } from '@dropins/storefront-cart';

export default function MyComponent() {
  return (
    <MiniCart
      defaultImageProps={defaultImageProps}
      loading={true}
      onUndo={() => console.log('Undo')}
    />
  );
}
```

{/* This documentation is auto-generated from: git@github.com:adobe-commerce/storefront-cart */}
