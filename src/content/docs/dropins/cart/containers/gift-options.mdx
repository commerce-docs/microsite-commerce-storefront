---
title: GiftOptions Container
description: Learn about the GiftOptions container in the Cart drop-in.
sidebar:
  label: GiftOptions
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The GiftOptions container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.5.1</strong>
</div>

## Configuration

The `GiftOptions` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `item` | `Item \| ProductGiftOptionsConfig` | Yes | Configuration for item |
| `view` | `GiftOptionsViewProps` | No | Configuration for view |
| `readOnlyFormOrderView` | `GiftOptionsReadOnlyViewProps` | Yes | Configuration for read only form order view |
| `dataSource` | `GiftOptionsDataSourcesProps` | No | Configuration for data source |
| `isEditable` | `boolean` | No | Whether the editable state is active |
| `initialLoading` | `boolean` | No | Configuration for initial loading |
| `handleItemsLoading` | `(uid: string` | No | Configuration for handle items loading |
| `state` | `boolean) => void` | Yes | Configuration for state |
| `handleItemsError` | `(uid: string` | No | Configuration for handle items error |
| `message` | `string) => void` | No | Configuration for message |
| `onItemUpdate` | `({ item` | No | Callback function triggered when item update |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `GiftOptions` container:

```jsx
import { GiftOptions } from '@dropins/storefront-cart';

export default function MyComponent() {
  return (
    <GiftOptions
      item={data}
      readOnlyFormOrderView={data}
      state={true}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
