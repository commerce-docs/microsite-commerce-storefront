---
title: EstimateShipping Container
description: Learn about the EstimateShipping container in the Cart drop-in.
sidebar:
  label: EstimateShipping
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The EstimateShipping container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.5.1</strong>
</div>

## Configuration

The `EstimateShipping` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `showDefaultEstimatedShippingCost` | `boolean` | Yes | Controls visibility of default estimated shipping cost |

</TableWrapper>

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `EstimateShipping` container:

```jsx
import { EstimateShipping } from '@dropins/storefront-cart';

export default function MyComponent() {
  return (
    <EstimateShipping
      showDefaultEstimatedShippingCost={true}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
