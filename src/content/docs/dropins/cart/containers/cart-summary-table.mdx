---
title: CartSummaryTable Container
description: Learn about the CartSummaryTable container in the Cart drop-in.
sidebar:
  label: CartSummaryTable
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The CartSummaryTable container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.5.1</strong>
</div>

## Configuration

The `CartSummaryTable` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `initialData` | `CartModel \| null` | No | Initial data for the cart |
| `className` | `string` | No | Optional CSS class name for custom styling |
| `Price` | `SlotProps<{ item: CartModel\['items'\]\[number\] }>` | No | Configuration for price |
| `Quantity` | `SlotProps<{ item: CartModel\['items'\]\[number\]` | No | Slot for customizing the quantity cell content |
| `isUpdating` | `boolean` | Yes | Slot for customizing the quantity cell content |
| `quantityInputValue` | `number` | Yes | Slot for customizing the quantity cell content |
| `handleInputChange` | `(e: Event) => void` | Yes | Slot for customizing the quantity cell content |
| `itemUpdateErrors` | `Map<string` | Yes | Slot for customizing the quantity cell content |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ Item?: SlotProps<{ item: CartModel\['items'\]\[number\] }>` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `CartSummaryTable` container:

```jsx
import { CartSummaryTable } from '@dropins/storefront-cart';

export default function MyComponent() {
  return (
    <CartSummaryTable
      isUpdating={true}
      quantityInputValue={1}
      handleInputChange={() => {}}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
