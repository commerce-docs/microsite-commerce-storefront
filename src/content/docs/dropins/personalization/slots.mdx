---
title: Personalization Slots
description: Customize UI sections in the Personalization drop-in using slots.
sidebar:
  label: Slots
  order: 5
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 2
---

import TableWrapper from '@components/TableWrapper.astro';
import { Aside } from '@astrojs/starlight/components';

The Personalization drop-in exposes **1 slot** in **1 container** for customizing specific UI sections. Use slots to replace or extend container components. For default properties available to all slots, see [Extending drop-in components](/dropins/all/extending/).

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.0.1</strong>
</div>

<table>
<thead>
<tr>
<th style="white-space: nowrap;">Container</th>
<th>Slots</th>
</tr>
</thead>
<tbody>
<tr>
<td style="white-space: nowrap;"><a href="#targetedblock-slots"><code>TargetedBlock</code></a></td>
<td><code>Content</code></td>
</tr>
</tbody>
</table>


## Using slots

This example customizes the `Content` slot in `TargetedBlock`:

```js
import { render as provider } from '@dropins/storefront-personalization/render.js';
import TargetedBlock from '@dropins/storefront-personalization/containers/TargetedBlock.js';

provider.render(TargetedBlock, {
  slots: {
    Content: (ctx) => {
      const div = document.createElement('div');
      div.className = 'targeted-block__content';
      div.textContent = 'Custom Content';
      ctx.appendChild(div);
    }
  }
})(document.querySelector('.targeted-block'));
```


## TargetedBlock slots

```js {4}
interface TargetedBlockProps

slots?: {
  Content: SlotProps
};
```

---



{/* This documentation is auto-generated from: REPO_URL */}
