---
title: User Account Installation
description: Package information and requirements for installing the User Account drop-in.
sidebar:
  label: Installation
  order: 2
---

import { Aside } from '@astrojs/starlight/components';

{/* 
TEMPLATE INSTRUCTIONS:
This simplified template links to the shared installation guide while providing drop-in-specific details.

PLACEHOLDERS:
- User Account: Display name (for example, "Cart", "Checkout")
- @dropins/storefront-account: Full package name (for example, "@dropins/storefront-cart")
- 1.2.0: Package version (for example, "1.5.1")
- AddressForm: Name of a primary container for examples
- git@github.com:adobe-commerce/storefront-account: Source repository URL
- : Optional custom introductory content from enrichment
- : Optional custom sections before installation link
- : Optional custom sections after installation link
*/}



<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.2.0</strong>
</div>



## Getting started

The User Account drop-in is included in the [Commerce boilerplate](https://github.com/hlxsites/aem-boilerplate-commerce)—no installation needed.

**See the [Using drop-ins](/dropins/all/installing/) guide** for complete instructions on importing and using drop-ins in your blocks.

## Package details

Use this information when working with the User Account drop-in:

- Package name: `@dropins/storefront-account`
- Current version: 1.2.0
- Example container: `AddressForm`
- Initializer: `scripts/initializers/account.js`

### Quick example

This example shows the basic pattern for using the User Account drop-in:

```js
// 1. Import initializer (handles all setup)
import '../../scripts/initializers/account.js';

// 2. Import the container you need
import AddressForm from '@dropins/storefront-account/containers/AddressForm.js';

// 3. Import the provider
import { render as provider } from '@dropins/storefront-account/render.js';

// 4. Render in your block
export default async function decorate(block) {
  await provider.render(AddressForm, {
    // Add configuration options here
  })(block);
}
```



## Next steps

Now that you understand the basics, explore these resources:

- [Using drop-ins](/dropins/all/installing/) - Complete guide with examples.
- [API functions](/dropins/User Account/functions/) - Control drop-in behavior.
- [Slots](/dropins/User Account/slots/) - Customize the UI.
- [Events](/dropins/User Account/events/) - Integrate with other components.

{/* This documentation is auto-generated from: git@github.com:adobe-commerce/storefront-account */}

