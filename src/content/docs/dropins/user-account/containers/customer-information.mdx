{/*
  Template for Drop-in Container Documentation
  
  This template is used by scripts/@generate-container-docs.js to generate container documentation
  for each container from the source repositories.
  
  Placeholders used in this template:
  - User Account → Display name (e.g., "Cart", "Checkout")
  - @dropins/storefront-account → Package name (e.g., "cart", "checkout")
  - CustomerInformation → Container name (e.g., "MiniCart", "OrderSummary")
  - CustomerInformation → Display name for container (e.g., "Mini Cart")
  - 1.2.0 → Version number (e.g., "1.5.1")
  - The CustomerInformation container component for the drop-in. → Brief description of what the container does
  - | No configurations | - | - | - | → Generated markdown table of all Props
  - This container does not currently expose any customizable slots. → Generated slots sections with examples
  - ```jsx
import { CustomerInformation } from '@dropins/storefront-account';

export default function MyComponent() {
  return (
    <CustomerInformation/>
  );
}
``` → Basic usage example with code
  - https://github.com/adobe-commerce/storefront-account → GitHub repository URL
  
  The script handles:
  - Extracting container Props from TypeScript interfaces
  - Generating configuration tables with types and descriptions
  - Extracting and documenting all slots
  - Creating basic usage examples
  
  The template and script must be kept in sync.
*/}

---
title: User Account - CustomerInformation Container
description: Learn about the CustomerInformation container in the User Account drop-in.
sidebar:
  label: CustomerInformation
  order: auto
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The CustomerInformation container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.2.0</strong>
</div>

## Configuration

The `CustomerInformation` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| No configurations | - | - | - |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `CustomerInformation` container:

```jsx
import { CustomerInformation } from '@dropins/storefront-account';

export default function MyComponent() {
  return (
    <CustomerInformation/>
  );
}
```

{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-account */}
