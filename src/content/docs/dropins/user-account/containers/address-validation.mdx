---
title: AddressValidation Container
description: Learn about the AddressValidation container in the User Account drop-in.
sidebar:
  label: AddressValidation
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The AddressValidation container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.2.0</strong>
</div>

## Configuration

The `AddressValidation` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `selectedAddress` | `'suggested' \| 'original' \| null` | No | Configuration for selected address |
| `suggestedAddress` | `Partial<CustomerAddressesModel> \| null` | Yes | Configuration for suggested address |
| `originalAddress` | `CustomerAddressesModel \| null` | Yes | Configuration for original address |
| `handleSelectedAddress` | `(payload: { selection: 'suggested' \| 'original'` | No | Configuration for handle selected address |
| `address` | `CustomerAddressesModel \| null \| undefined` | Yes | Configuration for address |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `AddressValidation` container:

```jsx
import { AddressValidation } from '@dropins/storefront-account';

export default function MyComponent() {
  return (
    <AddressValidation
      suggestedAddress={data}
      originalAddress={data}
      address={data}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
