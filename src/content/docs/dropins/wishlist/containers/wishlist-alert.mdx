---
title: WishlistAlert Container
description: Learn about the WishlistAlert container in the Wishlist drop-in.
sidebar:
  label: WishlistAlert
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The WishlistAlert container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.0.1</strong>
</div>

## Configuration

The `WishlistAlert` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `action` | `'add' \| 'remove' \| 'move' \| 'addError' \| 'removeError'` | Yes | Configuration for action |
| `item` | `{ product: { name: string } }` | No | Configuration for item |
| `routeToWishlist` | `string` | No | Configuration for route to wishlist |

</TableWrapper>

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `WishlistAlert` container:

```jsx
import { WishlistAlert } from '@dropins/storefront-wishlist';

export default function MyComponent() {
  return (
    <WishlistAlert
      action={action}
      item="example"
      routeToWishlist="example"
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
