---
title: WishlistItem Container
description: Learn about the WishlistItem container in the Wishlist drop-in.
sidebar:
  label: WishlistItem
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The WishlistItem container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.0.1</strong>
</div>

## Configuration

The `WishlistItem` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `item` | `Item` | Yes | Configuration for item |
| `getProductData` | `(sku: string) => Promise<Product \| null>` | No | Data object for get product |
| `getRefinedProduct` | `( sku: string` | No | Configuration for get refined product |
| `optionUIDs` | `string\[\]` | Yes | Configuration for option u i ds |
| `anchorOptions` | `string\[\]` | No | Available options for anchor |
| `raw` | `boolean ) => Promise<Product \| null>` | No | Callback function for raw |
| `moveProdToCart` | `( products: { sku: string` | Yes | Configuration for move prod to cart |
| `quantity` | `number` | Yes | Configuration for quantity |
| `optionsUIDs` | `\[\]` | No | Configuration for options u i ds |
| `enteredOptions` | `\[\]` | No | Available options for entered |

</TableWrapper>

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `WishlistItem` container:

```jsx
import { WishlistItem } from '@dropins/storefront-wishlist';

export default function MyComponent() {
  return (
    <WishlistItem
      item={item}
      optionUIDs=[]
      moveProdToCart="example"
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
