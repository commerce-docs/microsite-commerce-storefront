---
title: WishlistToggle Container
description: Learn about the WishlistToggle container in the Wishlist drop-in.
sidebar:
  label: WishlistToggle
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The WishlistToggle container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.0.1</strong>
</div>

## Configuration

The `WishlistToggle` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `product` | `Product` | Yes | Configuration for product |
| `iconWishlisted` | `VNode<HTMLAttributes<SVGSVGElement>>` | No | Configuration for icon wishlisted |
| `iconToWishlist` | `VNode<HTMLAttributes<SVGSVGElement>>` | No | Configuration for icon to wishlist |
| `size` | `'medium' \| 'large'` | No | Configuration for size |
| `variant` | `'primary' \| 'secondary' \| 'tertiary'` | No | Configuration for variant |
| `disabled` | `boolean` | No | Disables d |
| `labelToWishlist` | `string` | No | Configuration for label to wishlist |
| `labelWishlisted` | `string` | No | Configuration for label wishlisted |
| `onClick` | `() => void` | No | Callback function triggered when click |
| `removeProdFromCart` | `( product: { uid: string` | No | Configuration for remove prod from cart |
| `quantity` | `number }\[\] ) => Promise<any>` | Yes | Callback function for quantity |

</TableWrapper>

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `WishlistToggle` container:

```jsx
import { WishlistToggle } from '@dropins/storefront-wishlist';

export default function MyComponent() {
  return (
    <WishlistToggle
      product={product}
      quantity={[]}
      iconWishlisted={iconWishlisted}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
