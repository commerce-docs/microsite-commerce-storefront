---
title: Wishlist Container
description: Learn about the Wishlist container in the Wishlist drop-in.
sidebar:
  label: Wishlist
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The Wishlist container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.0.1</strong>
</div>

## Configuration

The `Wishlist` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `routeEmptyWishlistCTA` | `() => string` | No | Callback function for route empty wishlist c t a |
| `routeToWishlist` | `string` | No | Configuration for route to wishlist |
| `moveProdToCart` | `( products: { sku: string` | Yes | Configuration for move prod to cart |
| `quantity` | `number }\[\] ) => Promise<any>` | Yes | Callback function for quantity |
| `routeProdDetailPage` | `(product: Product) => string` | Yes | Callback function for route prod detail page |
| `getProductData` | `(sku: string) => Promise<Product \| null>` | No | Data object for get product |
| `getRefinedProduct` | `( sku: string` | No | Configuration for get refined product |
| `optionUIDs` | `string\[\]` | Yes | Configuration for option u i ds |
| `anchorOptions` | `string\[\]` | No | Available options for anchor |
| `raw` | `boolean ) => Promise<Product \| null>` | No | Callback function for raw |
| `item` | `Item` | Yes | Configuration for item |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ image?: SlotProps<{ defaultImageProps: ImageProps` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `Wishlist` container:

```jsx
import { Wishlist } from '@dropins/storefront-wishlist';

export default function MyComponent() {
  return (
    <Wishlist
      moveProdToCart="example"
      quantity={[]}
      routeProdDetailPage=() => {}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
