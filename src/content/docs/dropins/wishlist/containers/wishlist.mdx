{/*
  Template for Drop-in Container Documentation
  
  This template is used by scripts/@generate-container-docs.js to generate container documentation
  for each container from the source repositories.
  
  Placeholders used in this template:
  - Wishlist → Display name (e.g., "Cart", "Checkout")
  - @dropins/storefront-wishlist → Package name (e.g., "cart", "checkout")
  - Wishlist → Container name (e.g., "MiniCart", "OrderSummary")
  - Wishlist → Display name for container (e.g., "Mini Cart")
  - 2.0.1 → Version number (e.g., "1.5.1")
  - The Wishlist container component for the drop-in. → Brief description of what the container does
  - | No configurations | - | - | - | → Generated markdown table of all Props
  - This container does not currently expose any customizable slots. → Generated slots sections with examples
  - ```jsx
import { Wishlist } from '@dropins/storefront-wishlist';

export default function MyComponent() {
  return (
    <Wishlist/>
  );
}
``` → Basic usage example with code
  - https://github.com/adobe-commerce/storefront-wishlist → GitHub repository URL
  
  The script handles:
  - Extracting container Props from TypeScript interfaces
  - Generating configuration tables with types and descriptions
  - Extracting and documenting all slots
  - Creating basic usage examples
  
  The template and script must be kept in sync.
*/}

---
title: Wishlist - Wishlist Container
description: Learn about the Wishlist container in the Wishlist drop-in.
sidebar:
  label: Wishlist
  order: auto
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The Wishlist container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.0.1</strong>
</div>

## Configuration

The `Wishlist` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| No configurations | - | - | - |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `Wishlist` container:

```jsx
import { Wishlist } from '@dropins/storefront-wishlist';

export default function MyComponent() {
  return (
    <Wishlist/>
  );
}
```

{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-wishlist */}
