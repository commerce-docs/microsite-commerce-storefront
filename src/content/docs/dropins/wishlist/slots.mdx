{/*
  Template for Drop-in Slots Documentation
  
  This template is used by scripts/@generate-slot-docs.js to generate slot documentation
  for each drop-in from the source repositories.
  
  Placeholders used in this template:
  - Wishlist → Display name (e.g., "Cart", "Checkout")
  - @dropins/storefront-wishlist → Package name (e.g., "@dropins/storefront-cart")
  - 2.0.1 → Version number (e.g., "1.5.1")
  - 1 → Number of containers with slots
  - ## Wishlist slots

The slots for the `Wishlist` container allow you to customize its appearance and behavior.

```js
interface WishlistProps

slots?: {
image?: SlotProps<{
      defaultImageProps: ImageProps;
      item: Item;
    }>;
};
```

### image slot

The `image` slot allows you to customize the image section of the `Wishlist` container.

```js
 provider.render(Wishlist, {
  slots: {
    image: (ctx) => {
      // Your custom implementation
      const element = document.createElement('div');
      element.innerText = 'Custom image';
      ctx.appendChild(element);
    }
  }
});
```

---

 → Generated slots content from source repositories
  - https://github.com/adobe-commerce/storefront-wishlist → GitHub repository URL
  
  The script handles:
  - Extracting container names and their slots from .tsx files
  - Generating TypeScript interface definitions
  - Creating placeholder sections for each slot
  
  The template and script must be kept in sync.
*/}

---
title: Wishlist Slots
description: Learn about the slots provided in the Wishlist drop-in for customization.
sidebar:
  label: Slots
  order: 5
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 3
---

import { Aside } from '@astrojs/starlight/components';

## Overview

Learn about the slots provided in the **Wishlist** drop-in component for customizing container appearance and behavior.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.0.1</strong>
</div>

<Aside type="tip">
[Extending drop-in components](/dropins/all/extending/) describes default properties available to all slots.
</Aside>

## What are Slots?

Slots are customization points that allow you to replace or extend parts of a container's UI. Each container can expose multiple slots for different sections of its interface.

## Wishlist slots

The slots for the `Wishlist` container allow you to customize its appearance and behavior.

```js
interface WishlistProps

slots?: {
image?: SlotProps<{
      defaultImageProps: ImageProps;
      item: Item;
    }>;
};
```

### image slot

The `image` slot allows you to customize the image section of the `Wishlist` container.

```js
 provider.render(Wishlist, {
  slots: {
    image: (ctx) => {
      // Your custom implementation
      const element = document.createElement('div');
      element.innerText = 'Custom image';
      ctx.appendChild(element);
    }
  }
});
```

---



{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-wishlist */}
