---
title: Wishlist Slots
description: Learn about the slots provided in the Wishlist drop-in for customization.
sidebar:
  label: Slots
  order: 5
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 2
---

import { Aside } from '@astrojs/starlight/components';

The Wishlist drop-in exposes slots in 1 container, allowing you to customize specific sections of the UI. Slots let you replace or extend parts of a container with your own implementations. For details on default properties available to all slots, see [Extending drop-in components](/dropins/all/extending/).

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 2.0.1</strong>
</div>

<table>
<thead>
<tr>
<th style="white-space: nowrap;">Container</th>
<th>Slots</th>
</tr>
</thead>
<tbody>
<tr>
<td style="white-space: nowrap;"><a href="#wishlist-slots"><code>Wishlist</code></a></td>
<td><code>image</code></td>
</tr>
</tbody>
</table>


## Example

Here's how to customize the `image` slot in the `Wishlist` container:

```js
import Wishlist from '@dropins/storefront-wishlist/containers/Wishlist.js';

provider.render(Wishlist, {
  slots: {
    image: (ctx) => {
      const div = document.createElement('div');
      div.className = 'custom-image';
      div.textContent = 'Custom image';
      ctx.appendChild(div);
    }
  }
});
```


## Wishlist slots

```js {4}
interface WishlistProps

slots?: {
  image?: SlotProps<{
    defaultImageProps: ImageProps;
    item: Item;
  }>;
};
```

---



{/* This documentation is auto-generated from: REPO_URL */}
