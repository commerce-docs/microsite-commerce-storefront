---
title: ProductGallery Container
description: Learn about the ProductGallery container in the Product Details drop-in.
sidebar:
  label: ProductGallery
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The ProductGallery container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.3.5</strong>
</div>

## Configuration

The `ProductGallery` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `scope` | `string` | No | Configuration for scope |
| `controls` | `'thumbnailsRow' \| 'thumbnailsColumn' \| 'dots' \| null` | No | Configuration for controls |
| `loop` | `boolean` | No | Configuration for loop |
| `peak` | `boolean` | No | Configuration for peak |
| `gap` | `'small' \| 'medium' \| 'large' \| null` | No | Configuration for gap |
| `arrows` | `boolean` | No | Configuration for arrows |
| `arrowsOnMainImage` | `boolean` | No | Configuration for arrows on main image |
| `imageParams` | `ResolveImageUrlOptions` | No | Configuration for image params |
| `thumbnailParams` | `ResolveImageUrlOptions` | No | Configuration for thumbnail params |
| `zoom` | `{ closeButton: boolean } \| boolean` | No | Configuration for zoom |
| `CarouselMainImage` | `SlotProps< DefaultSlotContext & { defaultImageProps: ImageProps } >` | No | Configuration for carousel main image |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ CarouselThumbnail?: SlotProps< DefaultSlotContext & { defaultImageProps: ImageProps } >` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `ProductGallery` container:

```jsx
import { ProductGallery } from '@dropins/storefront-pdp';

export default function MyComponent() {
  return (
    <ProductGallery
      scope="example"
      controls={controls}
      loop={true}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
