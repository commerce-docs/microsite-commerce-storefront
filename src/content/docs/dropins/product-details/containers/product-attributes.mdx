---
title: ProductAttributes Container
description: Learn about the ProductAttributes container in the Product Details drop-in.
sidebar:
  label: ProductAttributes
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The ProductAttributes container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.3.5</strong>
</div>

## Configuration

The `ProductAttributes` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `scope` | `string` | No | Configuration for scope |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ Attributes?: SlotProps<DefaultSlotContext>` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `ProductAttributes` container:

```jsx
import { ProductAttributes } from '@dropins/storefront-pdp';

export default function MyComponent() {
  return (
    <ProductAttributes
      scope="example"
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
