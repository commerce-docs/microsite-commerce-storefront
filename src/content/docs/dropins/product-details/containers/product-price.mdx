---
title: ProductPrice Container
description: Learn about the ProductPrice container in the Product Details drop-in.
sidebar:
  label: ProductPrice
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The ProductPrice container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.3.5</strong>
</div>

## Configuration

The `ProductPrice` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `scope` | `string` | No | Configuration for scope |

</TableWrapper>

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `ProductPrice` container:

```jsx
import { ProductPrice } from '@dropins/storefront-pdp';

export default function MyComponent() {
  return (
    <ProductPrice
      scope="example"
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
