---
title: ProductOptions Container
description: Learn about the ProductOptions container in the Product Details drop-in.
sidebar:
  label: ProductOptions
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The ProductOptions container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.3.5</strong>
</div>

## Configuration

The `ProductOptions` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `scope` | `string` | No | Configuration for scope |
| `hideSelectedValue` | `boolean` | No | Controls hiding of selected value |
| `onValues` | `(optionsUIDs: string\[\]) => void` | No | Callback function triggered when values |
| `onErrors` | `(errors: { \[id: string\]: string }) => void` | No | Callback function triggered when errors |
| `optionsUIDs` | `string\[\] }>` | Yes | Configuration for options u i ds |
| `SwatchImage` | `SlotProps<{ data: ProductModel \| null` | No | Configuration for swatch image |
| `optionsUIDs` | `string\[\]` | Yes | Configuration for options u i ds |
| `imageSwatchContext` | `ImageNodeRenderProps\['imageSwatchContext'\]` | Yes | Configuration for image swatch context |
| `defaultImageProps` | `ImageProps` | Yes | Configuration for default image props |

</TableWrapper>

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `{ Swatches?: SlotProps<{ data: ProductModel \| null` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `ProductOptions` container:

```jsx
import { ProductOptions } from '@dropins/storefront-pdp';

export default function MyComponent() {
  return (
    <ProductOptions
      optionsUIDs=[]
      optionsUIDs=[]
      imageSwatchContext={imageSwatchContext}
    />
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
