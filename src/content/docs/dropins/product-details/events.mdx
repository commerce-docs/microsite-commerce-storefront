---
title: Product Details data & events
description: Learn about the events used by the Product Details and the data available within the events.
sidebar:
  label: Events
  order: 5
---

{/*
  ⚠️ TEMPLATE USAGE GUIDE ⚠️
  
  This template is used by scripts/generate-events-docs.js to generate event documentation.
  
  WHAT THE SCRIPT USES FROM THIS TEMPLATE:
  - Section headings and descriptive text (used as-is)
  - Import statements
  - The REPEAT_FOR_EACH_EVENT block (replicated for each event)
  - Placeholders: Product Details, Product Details, EVENT_NAME, etc.
  
  WHAT THE SCRIPT GENERATES INDEPENDENTLY:
  - Table contents between START/END markers (example rows are for reference only)
  - Event-specific data (names, descriptions, payloads from source repos)
  
  TO CHANGE:
  - Section text/descriptions → Edit this template
  - Table structure (columns, links, formatting) → Edit scripts/generate-events-docs.js
  - Both template and script must be kept in sync for table structure changes
*/}

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

The **Product Details** drop-in uses the [Event Bus](/sdk/reference/events/) to emit and listen to events for communication between drop-ins and external integrations. For common events shared across multiple drop-ins (such as `locale`, `error`, `authenticated`, etc.), see the [Common Events Reference](/sdk/reference/common-events/).

<Aside type="note" title="Auto-generated on 2025-10-21. Do not edit this page directly." />

## Emits Reference

{/* EMITS_TABLE_START */}
Events produced by this drop-in that you can subscribe to.

<TableWrapper nowrap={[0, 1]}>

| Event | Direction | Description |
|-------|-----------|-------------|
| [pdp/valid](#pdpvalid) | Emits | Emitted when validation state changes |

</TableWrapper>
{/* EMITS_TABLE_END */}

## Listens Reference

{/* LISTENS_TABLE_START */}
Events this drop-in listens for from external sources.

<TableWrapper nowrap={[0, 1]}>

| Event | Direction | Description |
|-------|-----------|-------------|
| [pdp/setValues](#pdpsetvalues) | Listens | Fired by Pdp (`pdp`) when values are set programmatically |

</TableWrapper>
{/* LISTENS_TABLE_END */}

## Emits and Listens Reference

{/* BIDIRECTIONAL_TABLE_START */}
Bidirectional events that both emit state changes and listen for external updates.

<TableWrapper nowrap={[0, 1]}>

| Event | Direction | Description |
|-------|-----------|-------------|
| [pdp/data](#pdpdata) | Emits and Listens | Triggered when data is available or changes |
| [pdp/values](#pdpvalues) | Emits and Listens | Triggered when form or configuration values change |

</TableWrapper>
{/* BIDIRECTIONAL_TABLE_END */}

## Event Details

The following sections provide detailed information about each event, including its direction, data payload structure, and usage examples.


<h3 id="pdpvalid">pdp/valid (emits)</h3>

Emitted when validation state changes

#### Data payload

```typescript
boolean
```

<Aside type="note">
Refer to the TypeScript definition above for the complete payload structure.
</Aside>


#### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const pdpValidListener = events.on('pdp/valid', (data) => {
  console.log('pdp/valid event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
pdpValidListener.off();
```

<h3 id="pdpsetvalues">pdp/setValues (listens)</h3>

Fired by Pdp (`pdp`) when values are set programmatically

#### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

#### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const pdpSetValuesListener = events.on('pdp/setValues', (data) => {
  console.log('pdp/setValues event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
pdpSetValuesListener.off();
```

<h3 id="pdpdata">pdp/data (emits and listens)</h3>

Triggered when data is available or changes

#### Data payload

```typescript
ProductModel | null
```

<Aside type="tip">
**ProductModel** is a complex type with multiple properties. Key properties typically include `id`, domain-specific data fields, and metadata.
</Aside>


#### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const pdpDataListener = events.on('pdp/data', (data) => {
  console.log('pdp/data event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
pdpDataListener.off();
```

<h3 id="pdpvalues">pdp/values (emits and listens)</h3>

Triggered when form or configuration values change

#### Data payload

```typescript
ValuesModel
```

<Aside type="tip">
**ValuesModel** is a complex type with multiple properties. Key properties typically include `id`, domain-specific data fields, and metadata.
</Aside>


#### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const pdpValuesListener = events.on('pdp/values', (data) => {
  console.log('pdp/values event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
pdpValuesListener.off();
```

