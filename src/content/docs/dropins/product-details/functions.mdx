---
title: Product Details Functions
description: API functions provided by the Product Details drop-in for programmatic control and customization.
sidebar:
  label: Functions
  order: 6
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 2
---

{/*
  ⚠️ TEMPLATE USAGE GUIDE ⚠️
  
  This template is used by scripts/@generate-function-docs.js to generate API function documentation.
  
  Placeholders used in this template:
  - DROPIN_NAME → Display name (e.g., "Cart", "Checkout")
  - DROPIN_DISPLAY_NAME → Display name for use in text (e.g., "Cart", "Checkout")
  - DROPIN_VERSION → Version number (e.g., "1.5.1")
  - FUNCTIONS_TABLE → Table listing all functions with brief descriptions
  - FUNCTIONS_CONTENT → All function documentation (generated from source .mdx files)
  
  The script handles:
  - Reading function .mdx files from src/api directories in source repositories
  - Cleaning Storybook imports and metadata
  - Combining all functions into a single documentation page
  
  The template and script must be kept in sync.
  
  HEADING HIERARCHY:
  H1: "DROPIN_NAME functions" (from title)
  H2: Individual function names
  H3: Examples, Events, Returns (subsection headings)
  Content flow: [Signature code block] → [Parameters table] → Examples → Events → Returns
*/}

import TableWrapper from '@components/TableWrapper.astro';
import { Aside } from '@astrojs/starlight/components';

The Product Details drop-in provides API functions that enable you to programmatically control behavior, fetch data, and integrate with Adobe Commerce backend services.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.3.5</strong>
</div>

{/* AUTO-GENERATED CONTENT - Do not edit below this line */}

<TableWrapper nowrap={[0]}>

| Function | Description |
| --- | --- |
| [`fetchProductData`](#fetchproductdata) | Fetches product data for a given SKU.. |
| [`getFetchedProductData`](#getfetchedproductdata) | It uses the Adobe Commerce event bus to either return the most recent pdp/data event payload immediately, or wait for the next event if none exists.. |
| [`getProductConfigurationValues`](#getproductconfigurationvalues) | It uses the Adobe Commerce event bus to get the most recent pdp/values event payload.. |
| [`getProductData`](#getproductdata) | This function returns the product data for a given product sku. |
| [`getRefinedProduct`](#getrefinedproduct) | A function that returns refined product's data. |
| [`isProductConfigurationValid`](#isproductconfigurationvalid) | It uses the Adobe Commerce event bus to get the most recent pdp/valid event payload.. |
| [`setProductConfigurationValid`](#setproductconfigurationvalid) | It uses a callback pattern to determine the new validity state based on the previous state, then emits a pdp/valid event with the updated value.. |
| [`setProductConfigurationValues`](#setproductconfigurationvalues) | It uses a callback pattern to determine the new configuration state based on the previous state, then emits a pdp/values event with the updated values.. |

</TableWrapper>

## fetchProductData

Fetches product data for a given SKU.

```ts
const fetchProductData = async (
  sku: string,
  options?: Options
): Promise<any>
```

<TableWrapper nowrap={[0, 1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `sku` | `string` | Yes | See function signature above |
| `options` | `Options` | No | See function signature above |

</TableWrapper>

---

## getFetchedProductData

It uses the Adobe Commerce event bus to either return the most recent `pdp/data` event payload immediately, or wait for the next event if none exists.

```ts
const getFetchedProductData = async (
  {
  scope,
}: { scope?: string } = {}
): Promise<ProductModel | null>
```

<TableWrapper nowrap={[0, 1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `options` | `string } = {}` | Yes | See function signature above |

</TableWrapper>

### Returns

Returns [`ProductModel`](#productmodel) or `null` (see structure below).

---

## getProductConfigurationValues

It uses the Adobe Commerce event bus to get the most recent `pdp/values` event payload.

```ts
const getProductConfigurationValues = async (
  {
  scope,
}: { scope?: string } = {}
): ValuesModel | null
```

<TableWrapper nowrap={[0, 1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `options` | `string } = {}` | Yes | See function signature above |

</TableWrapper>

### Returns

Returns [`ValuesModel`](#valuesmodel) or `null` (see structure below).

---

## getProductData

This function returns the product data for a given product `sku`. It takes `sku` as a parameter.

```ts
const getProductData = async (
  sku: string,
  options?: {
    preselectFirstOption?: boolean;
    optionsUIDs?: string[];
  },
  raw?: boolean
): Promise<ProductModel | null>
```

<TableWrapper nowrap={[0, 1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `sku` | `string` | Yes | See function signature above |
| `options` | `object` | No | See function signature above |
| `raw` | `boolean` | No | See function signature above |

</TableWrapper>

### Returns

Returns [`ProductModel`](#productmodel) or `null` (see structure below).

---

## getRefinedProduct

A function that returns refined product's data. It takes `sku`, `optionUIDs`, and `isBundle?` as parameters.

```ts
const getRefinedProduct = async (
  sku: string,
  optionUIDs: string[],
  anchorOptions?: string[],
  raw?: boolean
): Promise<ProductModel | null>
```

<TableWrapper nowrap={[0, 1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `sku` | `string` | Yes | See function signature above |
| `optionUIDs` | `string[]` | Yes | See function signature above |
| `anchorOptions` | `string[]` | No | See function signature above |
| `raw` | `boolean` | No | See function signature above |

</TableWrapper>

### Returns

Returns [`ProductModel`](#productmodel) or `null` (see structure below).

---

## isProductConfigurationValid

It uses the Adobe Commerce event bus to get the most recent `pdp/valid` event payload.

```ts
const isProductConfigurationValid = async (
  {
  scope,
}: { scope?: string } = {}
): boolean | null
```

<TableWrapper nowrap={[0, 1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `options` | `string } = {}` | Yes | See function signature above |

</TableWrapper>

### Returns

Returns `boolean | null`.

---

## setProductConfigurationValid

It uses a callback pattern to determine the new validity state based on the previous state, then emits a `pdp/valid` event with the updated value.

```ts
const setProductConfigurationValid = async (
  callback: (prev: boolean
): any
```

<TableWrapper nowrap={[0, 1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `callback` | `(prev: boolean` | Yes | See function signature above |

</TableWrapper>

---

## setProductConfigurationValues

It uses a callback pattern to determine the new configuration state based on the previous state, then emits a `pdp/values` event with the updated values.

```ts
const setProductConfigurationValues = async (
  callback: (prev: ValuesModel
): any
```

<TableWrapper nowrap={[0, 1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `callback` | `(prev: ValuesModel` | Yes | See function signature above |

</TableWrapper>

---

## Data Models

The following data models are returned by multiple functions in this drop-in.

### ProductModel

The `ProductModel` object is returned by the following functions: [`getFetchedProductData`](#getfetchedproductdata), [`getProductData`](#getproductdata), [`getRefinedProduct`](#getrefinedproduct).

```ts
interface ProductModel {
  name: string;
  sku: string;
  isBundle: boolean;
  addToCartAllowed: boolean;
  inStock: boolean | null;
  shortDescription?: string;
  metaDescription?: string;
  metaKeyword?: string;
  metaTitle?: string;
  description?: string;
  images?: Image[];
  prices: Prices;
  attributes?: Attribute[];
  options?: Option[];
  optionUIDs?: string[];
  url?: string;
  urlKey?: string;
  externalId?: string;
  externalParentId?: string;
  variantSku?: string;
  productType?: ProductType | undefined;
}
```

### ValuesModel

The `ValuesModel` object is returned by the following functions: [`getProductConfigurationValues`](#getproductconfigurationvalues).

```ts
interface ValuesModel {
  sku: string;
  quantity: number;
  optionsUIDs?: string[];
  enteredOptions?: Array<{ uid: string; value: string }>;
}
```



{/* This documentation is auto-generated from the drop-in source repository: REPO_URL */}
