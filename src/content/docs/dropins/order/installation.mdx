---
title: Order Installation
description: Package information and requirements for installing the Order drop-in.
sidebar:
  label: Installation
  order: 2
---

import { Aside } from '@astrojs/starlight/components';

{/* 
TEMPLATE INSTRUCTIONS:
This simplified template links to the shared installation guide while providing drop-in-specific details.

PLACEHOLDERS:
- Order: Display name (for example, "Cart", "Checkout")
- @dropins/storefront-order: Full package name (for example, "@dropins/storefront-cart")
- 1.4.0: Package version (for example, "1.5.1")
- CreateReturn: Name of a primary container for examples
- git@github.com:adobe-commerce/storefront-order: Source repository URL
- : Optional custom introductory content from enrichment
- : Optional custom sections before installation link
- : Optional custom sections after installation link
*/}



<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.4.0</strong>
</div>



## Getting started

The Order drop-in is included in the [Commerce boilerplate](https://github.com/hlxsites/aem-boilerplate-commerce)—no installation needed.

**See the [Using drop-ins](/dropins/all/installing/) guide** for complete instructions on importing and using drop-ins in your blocks.

## Package details

Use this information when working with the Order drop-in:

- Package name: `@dropins/storefront-order`
- Current version: 1.4.0
- Example container: `CreateReturn`
- Initializer: `scripts/initializers/order.js`

### Quick example

This example shows the basic pattern for using the Order drop-in:

```js
// 1. Import initializer (handles all setup)
import '../../scripts/initializers/order.js';

// 2. Import the container you need
import CreateReturn from '@dropins/storefront-order/containers/CreateReturn.js';

// 3. Import the provider
import { render as provider } from '@dropins/storefront-order/render.js';

// 4. Render in your block
export default async function decorate(block) {
  await provider.render(CreateReturn, {
    // Add configuration options here
  })(block);
}
```



## Next steps

Now that you understand the basics, explore these resources:

- [Using drop-ins](/dropins/all/installing/) - Complete guide with examples.
- [API functions](/dropins/Order/functions/) - Control drop-in behavior.
- [Slots](/dropins/Order/slots/) - Customize the UI.
- [Events](/dropins/Order/events/) - Integrate with other components.

{/* This documentation is auto-generated from: git@github.com:adobe-commerce/storefront-order */}

