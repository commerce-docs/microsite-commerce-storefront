---
title: ShippingStatus Container
description: Learn about the ShippingStatus container in the Order drop-in.
sidebar:
  label: ShippingStatus
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The ShippingStatus container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.4.0</strong>
</div>

## Configuration

The `ShippingStatus` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| No configurations | - | - | - |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `ShippingStatus` container:

```jsx
import { ShippingStatus } from '@dropins/storefront-order';

export default function MyComponent() {
  return (
    <ShippingStatus/>
  );
}
```

{/* This documentation is auto-generated from: REPO_URL */}
