{/*
  Template for Drop-in Container Documentation
  
  This template is used by scripts/@generate-container-docs.js to generate container documentation
  for each container from the source repositories.
  
  Placeholders used in this template:
  - Order → Display name (e.g., "Cart", "Checkout")
  - @dropins/storefront-order → Package name (e.g., "cart", "checkout")
  - OrderSearch → Container name (e.g., "MiniCart", "OrderSummary")
  - OrderSearch → Display name for container (e.g., "Mini Cart")
  - 1.4.0 → Version number (e.g., "1.5.1")
  - The OrderSearch container component for the drop-in. → Brief description of what the container does
  - | No configurations | - | - | - | → Generated markdown table of all Props
  - This container does not currently expose any customizable slots. → Generated slots sections with examples
  - ```jsx
import { OrderSearch } from '@dropins/storefront-order';

export default function MyComponent() {
  return (
    <OrderSearch/>
  );
}
``` → Basic usage example with code
  - https://github.com/adobe-commerce/storefront-order → GitHub repository URL
  
  The script handles:
  - Extracting container Props from TypeScript interfaces
  - Generating configuration tables with types and descriptions
  - Extracting and documenting all slots
  - Creating basic usage examples
  
  The template and script must be kept in sync.
*/}

---
title: Order - OrderSearch Container
description: Learn about the OrderSearch container in the Order drop-in.
sidebar:
  label: OrderSearch
  order: auto
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The OrderSearch container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.4.0</strong>
</div>

## Configuration

The `OrderSearch` container provides the following configuration options:

<TableWrapper nowrap={[0]}>

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| No configurations | - | - | - |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `OrderSearch` container:

```jsx
import { OrderSearch } from '@dropins/storefront-order';

export default function MyComponent() {
  return (
    <OrderSearch/>
  );
}
```

{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-order */}
