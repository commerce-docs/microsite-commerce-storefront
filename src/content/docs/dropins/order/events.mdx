---
title: Order data & events
description: Learn about the events used by the Order and the data available within the events.
sidebar:
  label: Events
  order: 5
---

{/*
  ⚠️ TEMPLATE USAGE GUIDE ⚠️
  
  This template is used by scripts/generate-events-docs.js to generate event documentation.
  
  WHAT THE SCRIPT USES FROM THIS TEMPLATE:
  - Section headings and descriptive text (used as-is)
  - Import statements
  - The REPEAT_FOR_EACH_EVENT block (replicated for each event)
  - Placeholders: Order, Order, EVENT_NAME, etc.
  
  WHAT THE SCRIPT GENERATES INDEPENDENTLY:
  - Table contents between START/END markers (example rows are for reference only)
  - Event-specific data (names, descriptions, payloads from source repos)
  
  TO CHANGE:
  - Section text/descriptions → Edit this template
  - Table structure (columns, links, formatting) → Edit scripts/generate-events-docs.js
  - Both template and script must be kept in sync for table structure changes
*/}

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

<Aside type="note" title="Auto-generated on 2025-10-19. Do not edit this page directly." />

The **Order** drop-in uses the [Event Bus](/sdk/reference/events/) to emit and listen to events for communication between drop-ins and external integrations.

## Emits

These events are produced by the **Order** drop-in. Subscribe to these events to react to state changes, trigger custom logic, integrate with third-party services, or coordinate with other drop-ins and components in your storefront.

{/* EMITS_TABLE_START */}
<TableWrapper nowrap={[0, 1]}>

| Event | Direction | Description |
|-------|-----------|-------------|
| [cart/reset](#cartreset) | Emits | Emitted when the component state is reset |
| [order/error](#ordererror) | Emits | Emitted when an error occurs |
| [order/placed](#orderplaced) | Emits | Emitted when an order is placed |

</TableWrapper>
{/* EMITS_TABLE_END */}

## Listens

These events are consumed by the **Order** drop-in. Emit these events from your storefront application, other drop-ins, or external integrations to communicate with and control this drop-in's behavior.

{/* LISTENS_TABLE_START */}
<TableWrapper nowrap={[0, 1]}>

| Event | Direction | Description |
|-------|-----------|-------------|
| [authenticated](#authenticated) | Listens | Fired by external source when authentication state changes |
| [locale](#locale) | Listens | Fired by external source when locale/language changes |

</TableWrapper>
{/* LISTENS_TABLE_END */}

## Emits and Listens

These events flow in both directions, enabling a bidirectional data channel between the **Order** drop-in and external code:

- **The drop-in emits** these events when its internal state changes (for example, when a user interacts with the UI)
- **The drop-in listens** for these same events from external sources (your storefront code, other drop-ins, or integrations) to update its internal state programmatically

This pattern allows you to both **react to** the drop-in's state changes (by subscribing to events it emits) and **control** the drop-in's state (by emitting events that it listens for). Common use cases include server synchronization, cross-tab updates, analytics integration, and coordinating multiple drop-ins.

{/* BIDIRECTIONAL_TABLE_START */}
<TableWrapper nowrap={[0, 1]}>

| Event | Direction | Description |
|-------|-----------|-------------|
| [order/data](#orderdata) | Emits and Listens | Triggered when data is available or changes |

</TableWrapper>
{/* BIDIRECTIONAL_TABLE_END */}


## cart/reset

**Direction**: Emits

Emitted when the component state is reset

### Data payload

```typescript
void
```

<Aside type="note">
Refer to the TypeScript definition above for the complete payload structure.
</Aside>


### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const cartResetListener = events.on('cart/reset', (data) => {
  console.log('cart/reset event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
cartResetListener.off();
```

## order/error

**Direction**: Emits

Emitted when an error occurs

### Data payload

```typescript
{ source: string; type: string; error: Error | string }
```

| Property | Type | Description |
|----------|------|-------------|
| `source` | `string` | See type definition in source code |
| `type` | `string` | See type definition in source code |
| `error` | `Error \| string` | See type definition in source code |

<Aside type="tip">
Refer to the TypeScript definition above for detailed property descriptions and nested type definitions.
</Aside>


### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const orderErrorListener = events.on('order/error', (data) => {
  console.log('order/error event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
orderErrorListener.off();
```

## order/placed

**Direction**: Emits

Emitted when an order is placed

### Data payload

```typescript
OrderDataModel
```

<Aside type="tip">
**OrderDataModel** is a complex type with multiple properties. Key properties typically include `id`, domain-specific data fields, and metadata.
</Aside>


### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const orderPlacedListener = events.on('order/placed', (data) => {
  console.log('order/placed event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
orderPlacedListener.off();
```

## authenticated

**Direction**: Listens

Fired by external source when authentication state changes

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const authenticatedListener = events.on('authenticated', (data) => {
  console.log('authenticated event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
authenticatedListener.off();
```

## locale

**Direction**: Listens

Fired by external source when locale/language changes

### Data payload

<Aside type="caution">
No TypeScript definition available. Refer to the event implementation for payload structure details.
</Aside>

### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const localeListener = events.on('locale', (data) => {
  console.log('locale event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
localeListener.off();
```

## order/data

**Direction**: Emits and Listens

Triggered when data is available or changes

### Data payload

```typescript
OrderDataModel
```

<Aside type="tip">
**OrderDataModel** is a complex type with multiple properties. Key properties typically include `id`, domain-specific data fields, and metadata.
</Aside>


### Usage

Listen to this event in your storefront:

```javascript
import { events } from '@dropins/tools/event-bus.js';

const orderDataListener = events.on('order/data', (data) => {
  console.log('order/data event received:', data);
  // Add your custom logic here
});

// Later, when you want to stop listening
orderDataListener.off();
```

