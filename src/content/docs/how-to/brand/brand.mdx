---
title: Brand drop-ins
description: How to override the drop-in design tokens to match your brand.
prerequisites:
  html: true
  css: true
---

import { Tabs, TabItem, Code } from '@astrojs/starlight/components';
import Prerequisites from '@components/Prerequisites.astro';
import Summary from '@components/Summary.astro';
import Diagram from '@components/Diagram.astro';
import CodeImport from '@components/CodeImport.astro';
import CodeExample from '@components/CodeExample.astro';
import Icon from '@components/overrides/Icon.astro';
import base from './base.css?raw';
import Steps from '@components/Steps.astro';
import Concepts from '@components/Concepts.astro';
import tokenOverridesHTML from './tokenOverridesHTML.html?raw';
import tokenOverridesEDS from './tokenOverridesEDS.css?raw';

This section teaches you how to override the **drop-in design token values** to match your brand colors, typography, spacing, shapes, and layouts. But first, let's define the vocabulary and concepts required to understand this process.

## Concepts

<Concepts>

### Design tokens

CSS variables with default values. Our design tokens provide a standard set of CSS properties and default values for colors, typography, spacing, shapes, and layouts. We use them in all of our component CSS classes to avoid hard-coded values that cannot be easily changed. With this strategy, you can restyle our drop-ins to match your brand simply by changing the default token values.

<CodeImport code={base} title="All default design tokens" lang="css" frame="none" />

### Library components

Basic components (like `Button`, `Checkbox`, and `Carousel`) used to build drop-ins. Design tokens are embedded in the CSS classes of these components to ensure fast, flexible styling to match your brand.

### Drop-in design system

The collection of design tokens, library components, and conventions we use to style our drop-ins.

### Branding

The process of changing the design token default values to match your brand colors, typography, spacing, shapes, and layouts.

</Concepts>

## Diagram

The following diagram shows a small branding change. When we override the default value of a single shape token, we override the default border-radius of the library Button component, which changes the look and feel of drop-ins that use it.

<Diagram caption="How to override the dropin-design tokens.">
  ![Diagram for how to brand drop-ins](./howtobrand.svg)
</Diagram>

## Code example

The examples below show where and how to change the existing dropin-design tokens.

<Tabs>
  <TabItem value="Boilerplate" label="Boilerplate">
    <Code code={tokenOverrides} lang="css" title="styles/styles.css" />
  </TabItem>
  <TabItem value="HTML" label="HTML">
    <Code code={tokenOverrides} lang="html" title="index.html" />
  </TabItem>
</Tabs>

## Step-by-step

The steps below will guide you through the process of overriding the drop-in design tokens to match your brand.

<Steps>

### Drop-in design tokens

### Merchant brand overrides

### Branding applied

Design token override results...

</Steps>

## Playground

Insert a playground here to allow users to experiment with the design token overrides.
