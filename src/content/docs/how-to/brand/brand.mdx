---
title: Brand drop-ins
description: How to override the drop-in design tokens to match your brand.
prerequisites:
  html: true
  css: true
---

import { Tabs, TabItem, Code } from '@astrojs/starlight/components';
import Prerequisites from '@components/Prerequisites.astro';
import Summary from '@components/Summary.astro';
import Diagram from '@components/Diagram.astro';
import CodeImport from '@components/CodeImport.astro';
import CodeExample from '@components/CodeExample.astro';
import base from './base.css?raw';
import Steps from '@components/Steps.astro';

This section teaches you how to override the drop-in design token values to match your brand's colors, typography, spacing, shapes, and layouts. But first, let's define the vocabulary and concepts required to understand this process.

## Concepts

**Design tokens**: CSS variables with default values. Our design tokens provide a standard set of CSS properties and default values for colors, typography, spacing, shapes, and layouts. We use them in all of our component CSS classes to avoid hard-coded values that cannot be easily changed. With this strategy, you can restyle our drop-ins to match your brand simply by changing the default token values.

**Library components**: Basic components like `Accordion`, `Button`, `Checkbox`, `Card`, `Carousel` used to build drop-ins. Design tokens are embedded in the CSS classes of these components to ensure fast, flexible styling to match your brand.

**Drop-in design system**: The collection of design tokens, library components, and style conventions we use to style our drop-ins.

**Branding**: The process of changing the design token default values to match your brand's colors, typography, spacing, shapes, and layouts.

## Diagrams

The diagram below shows the basic concept for overriding the default value of one design token — the shape (border-radius) applied to the default Button.

<Diagram caption="How to override the Base design tokens.">
  ![Diagram for how to brand drop-ins](./howtobrand.svg)
</Diagram>

## Code examples

The code example below shows how to override the Base design tokens to match your brand.

<CodeExample title="Override the Base design tokens" code={`insert code here`} />

**Drop-in design tokens and default values:**

<CodeImport code={base} title="All drop-in design tokens" lang="css" frame="none" />

## Step-by-step

The steps below will guide you through the process of overriding the drop-in design tokens to match your brand.

<Steps>

### Drop-in design tokens

### Merchant brand overrides

### Branding applied

Design token override results...

</Steps>

## Playground

Insert a playground here to allow users to experiment with the design token overrides.
