---
title: Company Hierarchy Quick Start
description: Quick reference and getting started guide for the CompanyHierarchy container.
sidebar:
  label: Quick Start
  order: 2
---

import Link from '@components/Link.astro';

Get started with the `CompanyHierarchy` container to let company administrators view and reorganize parent-child company relationships.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Package: @dropins/storefront-company-management</strong>
</div>

## Quick example

The `CompanyHierarchy` container ships in the <Link href="https://github.com/adobe-commerce/storefront-company-management" text="storefront-company-management" /> package and uses the same initializer as other Company Management containers. This example shows the basic pattern:

```js
// 1. Import initializer (handles all setup)
import '../../scripts/initializers/company-management.js';

// 2. Import the container
import { CompanyHierarchy } from '@dropins/storefront-company-management/containers/CompanyHierarchy.js';

// 3. Import the provider
import { render as provider } from '@dropins/storefront-company-management/render.js';

// 4. Render in your block
export default async function decorate(block) {
  await provider.render(CompanyHierarchy, {
    withHeader: true,
    showAdminBadge: true,
  })(block);
}
```

**New to drop-ins?** See the [Using drop-ins](/dropins/all/quick-start/) guide for complete step-by-step instructions.

## Quick reference

**Import paths:**
- Initializer: `import '../../scripts/initializers/company-management.js'`
- Container: `import { CompanyHierarchy } from '@dropins/storefront-company-management/containers/CompanyHierarchy.js'`
- Provider: `import { render } from '@dropins/storefront-company-management/render.js'`

**Package:** `@dropins/storefront-company-management`

**Version:** 1.2.0 (verify compatibility with your Commerce instance)

**Example container:** `CompanyHierarchy`

## Learn more

- [Containers](/dropins-b2b/company-hierarchy/containers/) — configuration options and usage examples
- [Initialization](/dropins-b2b/company-hierarchy/initialization/) — shared Company Management initializer settings
- [Functions](/dropins-b2b/company-hierarchy/functions/) — programmatic hierarchy API helpers
- [Slots](/dropins-b2b/company-hierarchy/slots/) — extend the toolbar with custom actions
