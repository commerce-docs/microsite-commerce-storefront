---
title: PurchaseOrderApprovalFlow Container
description: Learn about the PurchaseOrderApprovalFlow container in the Purchase Order drop-in.
sidebar:
  label: PurchaseOrderApprovalFlow
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The PurchaseOrderApprovalFlow container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 0.0.7-alpha1</strong>
</div>

## Configuration

The `PurchaseOrderApprovalFlow` container provides the following configuration options:

{/* 
  IMPORTANT: Do not wrap | No configurations | - | - | - | in TableWrapper or add a table header.
  The generator's generatePropertyTable() function already includes both.
  Adding them here will cause nested TableWrapper components and duplicate headers.
*/}
| No configurations | - | - | - |

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `PurchaseOrderApprovalFlow` container:

```jsx
import { PurchaseOrderApprovalFlow } from '@dropins/storefront-purchase-order';

export default function MyComponent() {
  return (
    <PurchaseOrderApprovalFlow/>
  );
}
```

{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-purchase-order */}
