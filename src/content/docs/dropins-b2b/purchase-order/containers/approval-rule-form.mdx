---
title: ApprovalRuleForm Container
description: Learn about the ApprovalRuleForm container in the Purchase Order drop-in.
sidebar:
  label: ApprovalRuleForm
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';



<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.0.0-beta2</strong>
</div>

## Configuration

The `ApprovalRuleForm` container provides the following configuration options:

<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `withHeader` | `boolean` | No | Shows/hides header section. Set false when embedding in layouts with their own headers. |
| `withWrapper` | `boolean` | No | Wraps container in styled wrapper. Set false for custom styling or embedded use. |
| `className` | `string` | No | CSS classes for custom styling. |
| `approvalRuleID` | `string` | No | ID of the approval rule to edit. Omit for creating new rules. |
| `routeApprovalRulesList` | `function` | Yes | Function to generate URL for navigating back to approval rules list. |
| `onSubmit` | `function` | No | Callback triggered when form is submitted. Use for custom success handling or navigation. |
| `onChange` | `function` | No | Callback triggered when form values change. Use for real-time validation or tracking. |

</TableWrapper>

## Slots

This container does not expose any customizable slots.

## Usage

The following example demonstrates how to use the `ApprovalRuleForm` container:

```js
import { render as provider } from '@dropins/storefront-purchase-order/render.js';
import { ApprovalRuleForm } from '@dropins/storefront-purchase-order/containers/ApprovalRuleForm.js';

await provider.render(ApprovalRuleForm, {
  routeApprovalRulesList: routeApprovalRulesList,
  withHeader: true,
  withWrapper: true,
})(block);
```



{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-purchase-order */}
