---
title: RequisitionListSelector Container
description: Learn about the RequisitionListSelector container in the Requisition List drop-in.
sidebar:
  label: RequisitionListSelector
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The RequisitionListSelector container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: main</strong>
</div>

## Configuration

The `RequisitionListSelector` container provides the following configuration options:

{/* 
  IMPORTANT: Do not wrap <TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `canCreate` | `boolean` | No | Configuration for can create |
| `sku` | `string` | Yes | Configuration for sku |
| `selectedOptions` | `string[]` | No | Available options for selected |
| `quantity` | `number` | No | Configuration for quantity |
| `beforeAddProdToReqList` | `() => Promise<void> \| void` | No | List of before add prod to req |

</TableWrapper> in TableWrapper or add a table header.
  The generator's generatePropertyTable() function already includes both.
  Adding them here will cause nested TableWrapper components and duplicate headers.
*/}
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `canCreate` | `boolean` | No | Configuration for can create |
| `sku` | `string` | Yes | Configuration for sku |
| `selectedOptions` | `string[]` | No | Available options for selected |
| `quantity` | `number` | No | Configuration for quantity |
| `beforeAddProdToReqList` | `() => Promise<void> \| void` | No | List of before add prod to req |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `RequisitionListSelector` container:

```jsx
import { RequisitionListSelector } from '@dropins/storefront-requisition-list';

export default function MyComponent() {
  return (
    <RequisitionListSelector
      sku="PRODUCT-SKU-123"
      canCreate={true}
      selectedOptions=[]
    />
  );
}
```

{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-requisition-list */}
