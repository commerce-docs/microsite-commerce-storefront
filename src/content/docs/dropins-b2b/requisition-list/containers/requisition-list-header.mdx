---
title: RequisitionListHeader Container
description: Learn about the RequisitionListHeader container in the Requisition List drop-in.
sidebar:
  label: RequisitionListHeader
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The RequisitionListHeader container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: main</strong>
</div>

## Configuration

The `RequisitionListHeader` container provides the following configuration options:

{/* 
  IMPORTANT: Do not wrap <TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `requisitionList` | `RequisitionList` | Yes | List of requisition |
| `routeRequisitionListGrid` | `() => string \| void` | No | Callback function for route requisition list grid |
| `onUpdate` | `(updatedList: RequisitionList) => void \| Promise<void>` | No | Callback function triggered when update |

</TableWrapper> in TableWrapper or add a table header.
  The generator's generatePropertyTable() function already includes both.
  Adding them here will cause nested TableWrapper components and duplicate headers.
*/}
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `requisitionList` | `RequisitionList` | Yes | List of requisition |
| `routeRequisitionListGrid` | `() => string \| void` | No | Callback function for route requisition list grid |
| `onUpdate` | `(updatedList: RequisitionList) => void \| Promise<void>` | No | Callback function triggered when update |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `RequisitionListHeader` container:

```jsx
import { RequisitionListHeader } from '@dropins/storefront-requisition-list';

export default function MyComponent() {
  return (
    <RequisitionListHeader
      requisitionList={requisitionList}
      routeRequisitionListGrid=() => {}
      onUpdate={() => console.log('Update')}
    />
  );
}
```

{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-requisition-list */}
