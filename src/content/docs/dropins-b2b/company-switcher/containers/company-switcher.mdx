---
title: CompanySwitcher Container
description: Learn about the CompanySwitcher container in the Company Switcher drop-in.
sidebar:
  label: CompanySwitcher
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

Dropdown picker component that allows users to switch between companies they have access to. Automatically hides when a user has access to only one company.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 1.0.5</strong>
</div>

## Configuration

The `CompanySwitcher` container provides the following configuration options:

<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `ariaLabel` | `string` | No | Custom accessibility label for the company picker dropdown. Use to provide context-specific screen reader instructions. |

</TableWrapper>

## Slots

This container does not expose any customizable slots.

## Usage

The following example demonstrates how to use the `CompanySwitcher` container:

```js
import { render as provider } from '@dropins/storefront-company-switcher/render.js';
import { CompanySwitcher } from '@dropins/storefront-company-switcher/containers/CompanySwitcher.js';

await provider.render(CompanySwitcher, {
  ariaLabel: "Label",
})(block);
```



{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-company-switcher */}
