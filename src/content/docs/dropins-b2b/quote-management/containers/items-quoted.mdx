---
title: ItemsQuoted Container
description: Learn about the ItemsQuoted container in the Quote Management drop-in.
sidebar:
  label: ItemsQuoted
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The ItemsQuoted container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: main</strong>
</div>

## Configuration

The `ItemsQuoted` container provides the following configuration options:

{/* 
  IMPORTANT: Do not wrap <TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `quoteData` | `NegotiableQuoteModel` | No | Data object for quote |
| `onItemCheckboxChange` | `(item: NegotiableQuoteCartItem` | No | Callback function triggered when item checkbox change |
| `isSelected` | `boolean) => void` | Yes | Whether the selected state is active |
| `onItemDropdownChange` | `(item: NegotiableQuoteCartItem` | No | Callback function triggered when item dropdown change |
| `action` | `string) => void` | Yes | Callback function for action |
| `onUpdate` | `(e: SubmitEvent) => void` | No | Callback function triggered when update |
| `canEdit` | `boolean` | Yes | Configuration for can edit |
| `onItemCheckboxChange` | `(item: NegotiableQuoteCartItem` | No | Callback function triggered when item checkbox change |
| `isSelected` | `boolean) => void` | Yes | Whether the selected state is active |
| `onItemDropdownChange` | `(item: NegotiableQuoteCartItem` | No | Callback function triggered when item dropdown change |
| `action` | `string) => void` | Yes | Callback function for action |
| `onUpdate` | `(e: SubmitEvent) => void` | No | Callback function triggered when update |
| `QuotePricesSummary` | `SlotProps<{ items: NegotiableQuoteModel['items']` | No | Configuration for quote prices summary |
| `prices` | `NegotiableQuoteModel['prices']` | Yes | Configuration for prices |

</TableWrapper> in TableWrapper or add a table header.
  The generator's generatePropertyTable() function already includes both.
  Adding them here will cause nested TableWrapper components and duplicate headers.
*/}
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `quoteData` | `NegotiableQuoteModel` | No | Data object for quote |
| `onItemCheckboxChange` | `(item: NegotiableQuoteCartItem` | No | Callback function triggered when item checkbox change |
| `isSelected` | `boolean) => void` | Yes | Whether the selected state is active |
| `onItemDropdownChange` | `(item: NegotiableQuoteCartItem` | No | Callback function triggered when item dropdown change |
| `action` | `string) => void` | Yes | Callback function for action |
| `onUpdate` | `(e: SubmitEvent) => void` | No | Callback function triggered when update |
| `canEdit` | `boolean` | Yes | Configuration for can edit |
| `onItemCheckboxChange` | `(item: NegotiableQuoteCartItem` | No | Callback function triggered when item checkbox change |
| `isSelected` | `boolean) => void` | Yes | Whether the selected state is active |
| `onItemDropdownChange` | `(item: NegotiableQuoteCartItem` | No | Callback function triggered when item dropdown change |
| `action` | `string) => void` | Yes | Callback function for action |
| `onUpdate` | `(e: SubmitEvent) => void` | No | Callback function triggered when update |
| `QuotePricesSummary` | `SlotProps<{ items: NegotiableQuoteModel['items']` | No | Configuration for quote prices summary |
| `prices` | `NegotiableQuoteModel['prices']` | Yes | Configuration for prices |

</TableWrapper>

## Slots

This container exposes the following slots for customization:

| Slot | Type | Required | Description |
|------|------|----------|-------------|
| `slots` | `\{ ProductListTable?: SlotProps&lt;\{ items: NegotiableQuoteModel\['items'\]` | No | Custom slot for rendering slots |


## Usage

The following example demonstrates how to use the `ItemsQuoted` container:

```jsx
import { ItemsQuoted } from '@dropins/storefront-quote-management';

export default function MyComponent() {
  return (
    <ItemsQuoted
      isSelected={() => {}}
      action=() => {}
      canEdit={true}
    />
  );
}
```

{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-quote-management */}
