---
title: OrderSummary Container
description: Learn about the OrderSummary container in the Quote Management drop-in.
sidebar:
  label: OrderSummary
---

import { Aside } from '@astrojs/starlight/components';
import TableWrapper from '@components/TableWrapper.astro';

## Overview

The OrderSummary container component for the drop-in.

<div style="background-color: var(--sl-color-blue-low); border-left: 4px solid var(--sl-color-blue); padding: 0.75rem 1rem; border-radius: 0.25rem; margin: 1rem 0;">
<strong>Version: 0.0.1</strong>
</div>

## Configuration

The `OrderSummary` container provides the following configuration options:

{/* 
  IMPORTANT: Do not wrap <TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `showTotalSaved` | `boolean` | No | Controls visibility of total saved |
| `updateLineItems` | `( lineItems: Array<OrderSummaryLineItem> ) => Array<OrderSummaryLineItem>` | No | Array of items for update line |

</TableWrapper> in TableWrapper or add a table header.
  The generator's generatePropertyTable() function already includes both.
  Adding them here will cause nested TableWrapper components and duplicate headers.
*/}
<TableWrapper nowrap={[0,1]}>

| Parameter | Type | Req? | Description |
|---|---|---|---|
| `showTotalSaved` | `boolean` | No | Controls visibility of total saved |
| `updateLineItems` | `( lineItems: Array<OrderSummaryLineItem> ) => Array<OrderSummaryLineItem>` | No | Array of items for update line |

</TableWrapper>

## Slots

This container does not currently expose any customizable slots.

## Usage

The following example demonstrates how to use the `OrderSummary` container:

```jsx
import { OrderSummary } from '@dropins/storefront-quote-management';

export default function MyComponent() {
  return (
    <OrderSummary
      showTotalSaved={true}
      updateLineItems={[]}
    />
  );
}
```

{/* This documentation is auto-generated from: https://github.com/adobe-commerce/storefront-quote-management */}
