---
title: DROPIN_NAME labels
description: Learn how to change the labels within the DROPIN_NAME dropin.
---

import Diagram from '@components/Diagram.astro';
import Callouts from '@components/Callouts.astro';
import Aside from '@components/Aside.astro';
import { Steps } from '@astrojs/starlight/components';

This topic provides examples for how to override the labels in the DROPIN_NAME dropin.

## Big Picture

{/* Replace `placeholder.webp` in Diagram control with a screenshot that identifies (highlights) all the default labels rendered. */}

<Diagram caption="DROPIN_NAME labels">![DROPIN_NAME labels](@images/placeholder.webp)</Diagram>

{/* Use `Callouts` control to name the slots in the dropin. */}

<Callouts>

{/* Use a numbered list to identify each label by name. */}

1. **LABEL_1**
1. **LABEL_2**
   {/* Example 1: **Title**: Uses HTML elements to display the product's title. */}
   {/* Example 2: **RegularPrice**: Uses our `Price` component to display the regular price of the product. */}

</Callouts>

{/* Add one code example that shows how to override each label in the dropin. */}

## Changing labels

To change the labels in the DROPIN_NAME dropin, you can override the following file(s) in your project:

````json

{/* Add the dropin's JSON or Spreadsheet file that defines the labels for the dropin */}
```json
{
  "LABEL_1": "New label for LABEL_1",
  "LABEL_2": "New label for LABEL_2"
}
````
